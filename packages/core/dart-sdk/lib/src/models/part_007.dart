// GENERATED by tool/generate_models.mjs — do not edit.
part of '../models.dart';

/// The workflows execution's details.
class AdminWorkflowExecution {
  AdminWorkflowExecution({
    this.id,
    this.workflowId,
    this.transactionId,
    this.execution,
    this.context,
    this.state,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
  });

  /// The workflow execution's ID.
  final String? id;
  /// The ID of the workflow.
  final String? workflowId;
  /// The workflow execution's transaction ID.
  final String? transactionId;
  final AdminWorkflowExecutionExecution? execution;
  final WorkflowExecutionContext? context;
  /// The workflow execution's state.
  final String? state;
  /// The date the workflow execution was created.
  final DateTime? createdAt;
  /// The date the workflow execution was updated.
  final DateTime? updatedAt;
  /// The date the workflow execution was deleted.
  final DateTime? deletedAt;

  factory AdminWorkflowExecution.fromJson(Map<String, dynamic> json) => AdminWorkflowExecution(
    id: json["id"] as String?,
    workflowId: json["workflow_id"] as String?,
    transactionId: json["transaction_id"] as String?,
    execution: json["execution"] == null ? null : AdminWorkflowExecutionExecution.fromJson(json["execution"] as Map<String, dynamic>),
    context: json["context"] == null ? null : WorkflowExecutionContext.fromJson(json["context"] as Map<String, dynamic>),
    state: json["state"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (workflowId != null) "workflow_id": workflowId!,
    if (transactionId != null) "transaction_id": transactionId!,
    if (execution != null) "execution": execution!.toJson(),
    if (context != null) "context": context!.toJson(),
    if (state != null) "state": state!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
  };
}

/// The workflow execution's steps details.
class AdminWorkflowExecutionExecution {
  AdminWorkflowExecutionExecution({
    this.steps,
  });

  /// The execution's steps. Each object key is a step ID, and the value is the object whose properties are shown below.
  final Map<String, dynamic>? steps;

  factory AdminWorkflowExecutionExecution.fromJson(Map<String, dynamic> json) => AdminWorkflowExecutionExecution(
    steps: json["steps"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (steps != null) "steps": steps!,
  };
}

/// The workflow execution's details.
class AdminWorkflowExecutionResponse {
  AdminWorkflowExecutionResponse({
    this.workflowExecution,
  });

  final AdminWorkflowExecution? workflowExecution;

  factory AdminWorkflowExecutionResponse.fromJson(Map<String, dynamic> json) => AdminWorkflowExecutionResponse(
    workflowExecution: json["workflow_execution"] == null ? null : AdminWorkflowExecution.fromJson(json["workflow_execution"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (workflowExecution != null) "workflow_execution": workflowExecution!.toJson(),
  };
}

/// The API key's details.
class ApiKeyResponse {
  ApiKeyResponse({
    this.id,
    this.token,
    this.redacted,
    this.title,
    this.type,
    this.lastUsedAt,
    this.createdBy,
    this.createdAt,
    this.revokedBy,
    this.revokedAt,
  });

  /// The API key's ID.
  final String? id;
  /// The API key's token.
  final String? token;
  /// The redacted form of the API key's token. This is useful when showing portion of the token. For example `sk_...123`.
  final String? redacted;
  /// The API key's title.
  final String? title;
  /// The API key's type. `secret` API keys are used for authenticating admin users, and `publishable` API key are used for storefronts and similar clients to scope r
  final String? type;
  /// The date the API key was last used.
  final DateTime? lastUsedAt;
  /// The ID of the user that created the API key.
  final String? createdBy;
  /// The date the API key was created.
  final DateTime? createdAt;
  /// The ID of the user that revoked the API key.
  final String? revokedBy;
  /// The date the API key was revoked.
  final DateTime? revokedAt;

  factory ApiKeyResponse.fromJson(Map<String, dynamic> json) => ApiKeyResponse(
    id: json["id"] as String?,
    token: json["token"] as String?,
    redacted: json["redacted"] as String?,
    title: json["title"] as String?,
    type: json["type"] as String?,
    lastUsedAt: json["last_used_at"] == null ? null : DateTime.parse(json["last_used_at"] as String),
    createdBy: json["created_by"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    revokedBy: json["revoked_by"] as String?,
    revokedAt: json["revoked_at"] == null ? null : DateTime.parse(json["revoked_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (token != null) "token": token!,
    if (redacted != null) "redacted": redacted!,
    if (title != null) "title": title!,
    if (type != null) "type": type!,
    if (lastUsedAt != null) "last_used_at": lastUsedAt!.toIso8601String(),
    if (createdBy != null) "created_by": createdBy!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (revokedBy != null) "revoked_by": revokedBy!,
    if (revokedAt != null) "revoked_at": revokedAt!.toIso8601String(),
  };
}

/// The authenticated user's details.
class AuthAdminSessionResponse {
  AuthAdminSessionResponse({
    this.user,
  });

  /// The logged-in user.
  final AdminUser? user;

  factory AuthAdminSessionResponse.fromJson(Map<String, dynamic> json) => AuthAdminSessionResponse(
    user: json["user"] == null ? null : AdminUser.fromJson(json["user"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (user != null) "user": user!.toJson(),
  };
}

/// The authentication's details.
class AuthCallbackResponse {
  AuthCallbackResponse({
    this.location,
  });

  /// The location to redirect the user to for further authentication with the third-party provider.
  final String? location;

  factory AuthCallbackResponse.fromJson(Map<String, dynamic> json) => AuthCallbackResponse(
    location: json["location"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (location != null) "location": location!,
  };
}

/// The public information of a registered auth provider instance. This is safe to expose to unauthenticated clients and never contains provider options or secrets.
class AuthProvider {
  AuthProvider({
    this.id,
    this.identifier,
    this.displayName,
    this.flow,
  });

  /// The registered instance ID of the provider, used in authentication routes (for example, `okta` in `POST /auth/user/okta`).
  final String? id;
  /// The provider's static identifier (for example, `oidc` or `emailpass`).
  final String? identifier;
  /// The name to display for the provider in a frontend application.
  final String? displayName;
  /// The authentication flow the provider uses: - `credentials`: the client submits credentials (for example, email and password) directly to the authentication rout
  final String? flow;

  factory AuthProvider.fromJson(Map<String, dynamic> json) => AuthProvider(
    id: json["id"] as String?,
    identifier: json["identifier"] as String?,
    displayName: json["display_name"] as String?,
    flow: json["flow"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (identifier != null) "identifier": identifier!,
    if (displayName != null) "display_name": displayName!,
    if (flow != null) "flow": flow!,
  };
}

/// The response of listing the auth providers available.
class AuthProvidersListResponse {
  AuthProvidersListResponse({
    this.providers,
  });

  /// The list of auth providers.
  final List<AuthProvider>? providers;

  factory AuthProvidersListResponse.fromJson(Map<String, dynamic> json) => AuthProvidersListResponse(
    providers: (json["providers"] as List?)?.map((e) => e == null ? null : AuthProvider.fromJson(e as Map<String, dynamic>)).toList().cast<AuthProvider>(),
  );

  Map<String, dynamic> toJson() => {
    if (providers != null) "providers": providers!.map((e) => e.toJson()).toList(),
  };
}

/// The authentication's details.
class AuthResponse {
  AuthResponse({
    this.token,
  });

  /// The JWT token used for registration or authentication.
  final String? token;

  factory AuthResponse.fromJson(Map<String, dynamic> json) => AuthResponse(
    token: json["token"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (token != null) "token": token!,
  };
}

/// The authenticated customer's details.
class AuthStoreSessionResponse {
  AuthStoreSessionResponse({
    this.user,
  });

  /// The logged-in customer.
  final StoreCustomer? user;

  factory AuthStoreSessionResponse.fromJson(Map<String, dynamic> json) => AuthStoreSessionResponse(
    user: json["user"] == null ? null : StoreCustomer.fromJson(json["user"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (user != null) "user": user!.toJson(),
  };
}

/// The calculated price's details.
class BaseCalculatedPriceSet {
  BaseCalculatedPriceSet({
    this.id,
    this.isCalculatedPricePriceList,
    this.isCalculatedPriceTaxInclusive,
    this.calculatedAmount,
    this.calculatedAmountWithTax,
    this.calculatedAmountWithoutTax,
    this.isOriginalPricePriceList,
    this.isOriginalPriceTaxInclusive,
    this.originalAmount,
    this.currencyCode,
    this.calculatedPrice,
    this.originalPrice,
    this.originalAmountWithTax,
    this.originalAmountWithoutTax,
  });

  /// The calculated price's ID.
  final String? id;
  /// Whether the calculated price belongs to a price list.
  final bool? isCalculatedPricePriceList;
  /// Whether the calculated price is tax inclusive.
  final bool? isCalculatedPriceTaxInclusive;
  /// The amount of the calculated price, or `null` if there isn't a calculated price. This is the amount shown to the customer.
  final num? calculatedAmount;
  /// The calculated price's amount with taxes applied.
  final num? calculatedAmountWithTax;
  /// The calculated price's amount without taxes applied.
  final num? calculatedAmountWithoutTax;
  /// Whether the original price belongs to a price list.
  final bool? isOriginalPricePriceList;
  /// Whether the original price is tax inclusive.
  final bool? isOriginalPriceTaxInclusive;
  /// The amount of the original price, or `null` if there isn't an original price. This amount is useful to compare with the `calculated_amount`, such as to check fo
  final num? originalAmount;
  /// The calculated price's currency code.
  final String? currencyCode;
  /// The calculated price's details.
  final Map<String, dynamic>? calculatedPrice;
  /// The original price's details.
  final Map<String, dynamic>? originalPrice;
  /// The amount of the original price with taxes applied.
  final num? originalAmountWithTax;
  /// The amount of the original price without taxes.
  final num? originalAmountWithoutTax;

  factory BaseCalculatedPriceSet.fromJson(Map<String, dynamic> json) => BaseCalculatedPriceSet(
    id: json["id"] as String?,
    isCalculatedPricePriceList: json["is_calculated_price_price_list"] as bool?,
    isCalculatedPriceTaxInclusive: json["is_calculated_price_tax_inclusive"] as bool?,
    calculatedAmount: json["calculated_amount"] as num?,
    calculatedAmountWithTax: json["calculated_amount_with_tax"] as num?,
    calculatedAmountWithoutTax: json["calculated_amount_without_tax"] as num?,
    isOriginalPricePriceList: json["is_original_price_price_list"] as bool?,
    isOriginalPriceTaxInclusive: json["is_original_price_tax_inclusive"] as bool?,
    originalAmount: json["original_amount"] as num?,
    currencyCode: json["currency_code"] as String?,
    calculatedPrice: json["calculated_price"] as Map<String, dynamic>?,
    originalPrice: json["original_price"] as Map<String, dynamic>?,
    originalAmountWithTax: json["original_amount_with_tax"] as num?,
    originalAmountWithoutTax: json["original_amount_without_tax"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (isCalculatedPricePriceList != null) "is_calculated_price_price_list": isCalculatedPricePriceList!,
    if (isCalculatedPriceTaxInclusive != null) "is_calculated_price_tax_inclusive": isCalculatedPriceTaxInclusive!,
    if (calculatedAmount != null) "calculated_amount": calculatedAmount!,
    if (calculatedAmountWithTax != null) "calculated_amount_with_tax": calculatedAmountWithTax!,
    if (calculatedAmountWithoutTax != null) "calculated_amount_without_tax": calculatedAmountWithoutTax!,
    if (isOriginalPricePriceList != null) "is_original_price_price_list": isOriginalPricePriceList!,
    if (isOriginalPriceTaxInclusive != null) "is_original_price_tax_inclusive": isOriginalPriceTaxInclusive!,
    if (originalAmount != null) "original_amount": originalAmount!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (calculatedPrice != null) "calculated_price": calculatedPrice!,
    if (originalPrice != null) "original_price": originalPrice!,
    if (originalAmountWithTax != null) "original_amount_with_tax": originalAmountWithTax!,
    if (originalAmountWithoutTax != null) "original_amount_without_tax": originalAmountWithoutTax!,
  };
}

/// The details of a captured payment.
class BaseCapture {
  BaseCapture({
    this.id,
    this.amount,
    this.createdAt,
    this.createdBy,
    this.payment,
  });

  /// The payment capture's ID.
  final String? id;
  /// The captured amount.
  final num? amount;
  /// The date the capture was created.
  final DateTime? createdAt;
  /// The ID of the user that captured the payment.
  final String? createdBy;
  final Map<String, dynamic>? payment;

  factory BaseCapture.fromJson(Map<String, dynamic> json) => BaseCapture(
    id: json["id"] as String?,
    amount: json["amount"] as num?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    createdBy: json["created_by"] as String?,
    payment: json["payment"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (amount != null) "amount": amount!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (createdBy != null) "created_by": createdBy!,
    if (payment != null) "payment": payment!,
  };
}

/// The item's cart.
class BaseCart {
  BaseCart({
    this.id,
    this.region,
    this.regionId,
    this.customerId,
    this.salesChannelId,
    this.email,
    this.currencyCode,
    this.shippingAddress,
    this.billingAddress,
    this.items,
    this.shippingMethods,
    this.paymentCollection,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.originalItemTotal,
    this.originalItemSubtotal,
    this.originalItemTaxTotal,
    this.itemTotal,
    this.itemSubtotal,
    this.itemTaxTotal,
    this.originalTotal,
    this.originalSubtotal,
    this.originalTaxTotal,
    this.total,
    this.subtotal,
    this.taxTotal,
    this.discountTotal,
    this.discountTaxTotal,
    this.giftCardTotal,
    this.giftCardTaxTotal,
    this.shippingTotal,
    this.shippingSubtotal,
    this.shippingTaxTotal,
    this.originalShippingTotal,
    this.originalShippingSubtotal,
    this.originalShippingTaxTotal,
    this.completedAt,
  });

  /// The cart's ID.
  final String? id;
  final BaseRegion? region;
  /// The cart's region id.
  final String? regionId;
  /// The cart's customer id.
  final String? customerId;
  /// The cart's sales channel id.
  final String? salesChannelId;
  /// The cart's email.
  final String? email;
  /// The cart's currency code.
  final String? currencyCode;
  final BaseCartAddress? shippingAddress;
  final BaseCartAddress? billingAddress;
  /// The cart's items.
  final List<Map<String, dynamic>>? items;
  /// The cart's shipping methods.
  final List<BaseCartShippingMethod>? shippingMethods;
  final BasePaymentCollection? paymentCollection;
  /// The cart's metadata.
  final Map<String, dynamic>? metadata;
  /// The cart's created at.
  final DateTime? createdAt;
  /// The cart's updated at.
  final DateTime? updatedAt;
  /// The sum of all line items' original totals before discounts, including taxes.
  final num? originalItemTotal;
  /// The sum of all line items' original subtotals before discounts, excluding taxes.
  final num? originalItemSubtotal;
  /// The sum of all line items' original tax totals before discounts.
  final num? originalItemTaxTotal;
  /// The sum of all line items' totals after discounts, including taxes.
  final num? itemTotal;
  /// The sum of all line items' subtotals before discounts, excluding taxes.
  final num? itemSubtotal;
  /// The sum of all line items' tax totals after discounts.
  final num? itemTaxTotal;
  /// The cart's total before discounts, including taxes. Calculated as the sum of `original_item_total` and `original_shipping_total`.
  final num? originalTotal;
  /// The cart's subtotal before discounts, excluding taxes. Calculated as the sum of `original_item_subtotal` and `original_shipping_subtotal`.
  final num? originalSubtotal;
  /// The cart's tax total before discounts. Calculated as the sum of `original_item_tax_total` and `original_shipping_tax_total`.
  final num? originalTaxTotal;
  /// The cart's final total after discounts and credit lines, including taxes.
  final num? total;
  /// The cart's subtotal before discounts, excluding taxes. Calculated as the sum of `item_subtotal` and `shipping_subtotal`.
  final num? subtotal;
  /// The cart's tax total after discounts. Calculated as the sum of `item_tax_total` and `shipping_tax_total`.
  final num? taxTotal;
  /// The total amount of discounts applied to the cart, including the tax portion of discounts.
  final num? discountTotal;
  /// The total amount of discounts applied to the cart's tax. Represents the tax portion of discounts.
  final num? discountTaxTotal;
  /// The cart's gift card total.
  final num? giftCardTotal;
  /// The cart's gift card tax total.
  final num? giftCardTaxTotal;
  /// The sum of all shipping methods' totals after discounts, including taxes.
  final num? shippingTotal;
  /// The sum of all shipping methods' subtotals before discounts, excluding taxes.
  final num? shippingSubtotal;
  /// The sum of all shipping methods' tax totals after discounts.
  final num? shippingTaxTotal;
  /// The sum of all shipping methods' original totals before discounts, including taxes.
  final num? originalShippingTotal;
  /// The sum of all shipping methods' original subtotals before discounts, excluding taxes.
  final num? originalShippingSubtotal;
  /// The sum of all shipping methods' original tax totals before discounts.
  final num? originalShippingTaxTotal;
  /// The date the cart was completed.
  final DateTime? completedAt;

  factory BaseCart.fromJson(Map<String, dynamic> json) => BaseCart(
    id: json["id"] as String?,
    region: json["region"] == null ? null : BaseRegion.fromJson(json["region"] as Map<String, dynamic>),
    regionId: json["region_id"] as String?,
    customerId: json["customer_id"] as String?,
    salesChannelId: json["sales_channel_id"] as String?,
    email: json["email"] as String?,
    currencyCode: json["currency_code"] as String?,
    shippingAddress: json["shipping_address"] == null ? null : BaseCartAddress.fromJson(json["shipping_address"]),
    billingAddress: json["billing_address"] == null ? null : BaseCartAddress.fromJson(json["billing_address"]),
    items: (json["items"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    shippingMethods: (json["shipping_methods"] as List?)?.map((e) => e == null ? null : BaseCartShippingMethod.fromJson(e as Map<String, dynamic>)).toList().cast<BaseCartShippingMethod>(),
    paymentCollection: json["payment_collection"] == null ? null : BasePaymentCollection.fromJson(json["payment_collection"] as Map<String, dynamic>),
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    originalItemTotal: json["original_item_total"] as num?,
    originalItemSubtotal: json["original_item_subtotal"] as num?,
    originalItemTaxTotal: json["original_item_tax_total"] as num?,
    itemTotal: json["item_total"] as num?,
    itemSubtotal: json["item_subtotal"] as num?,
    itemTaxTotal: json["item_tax_total"] as num?,
    originalTotal: json["original_total"] as num?,
    originalSubtotal: json["original_subtotal"] as num?,
    originalTaxTotal: json["original_tax_total"] as num?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    taxTotal: json["tax_total"] as num?,
    discountTotal: json["discount_total"] as num?,
    discountTaxTotal: json["discount_tax_total"] as num?,
    giftCardTotal: json["gift_card_total"] as num?,
    giftCardTaxTotal: json["gift_card_tax_total"] as num?,
    shippingTotal: json["shipping_total"] as num?,
    shippingSubtotal: json["shipping_subtotal"] as num?,
    shippingTaxTotal: json["shipping_tax_total"] as num?,
    originalShippingTotal: json["original_shipping_total"] as num?,
    originalShippingSubtotal: json["original_shipping_subtotal"] as num?,
    originalShippingTaxTotal: json["original_shipping_tax_total"] as num?,
    completedAt: json["completed_at"] == null ? null : DateTime.parse(json["completed_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (region != null) "region": region!.toJson(),
    if (regionId != null) "region_id": regionId!,
    if (customerId != null) "customer_id": customerId!,
    if (salesChannelId != null) "sales_channel_id": salesChannelId!,
    if (email != null) "email": email!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (shippingAddress != null) "shipping_address": shippingAddress!.toJson(),
    if (billingAddress != null) "billing_address": billingAddress!.toJson(),
    if (items != null) "items": items!.map((e) => e).toList(),
    if (shippingMethods != null) "shipping_methods": shippingMethods!.map((e) => e.toJson()).toList(),
    if (paymentCollection != null) "payment_collection": paymentCollection!.toJson(),
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (originalItemTotal != null) "original_item_total": originalItemTotal!,
    if (originalItemSubtotal != null) "original_item_subtotal": originalItemSubtotal!,
    if (originalItemTaxTotal != null) "original_item_tax_total": originalItemTaxTotal!,
    if (itemTotal != null) "item_total": itemTotal!,
    if (itemSubtotal != null) "item_subtotal": itemSubtotal!,
    if (itemTaxTotal != null) "item_tax_total": itemTaxTotal!,
    if (originalTotal != null) "original_total": originalTotal!,
    if (originalSubtotal != null) "original_subtotal": originalSubtotal!,
    if (originalTaxTotal != null) "original_tax_total": originalTaxTotal!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (taxTotal != null) "tax_total": taxTotal!,
    if (discountTotal != null) "discount_total": discountTotal!,
    if (discountTaxTotal != null) "discount_tax_total": discountTaxTotal!,
    if (giftCardTotal != null) "gift_card_total": giftCardTotal!,
    if (giftCardTaxTotal != null) "gift_card_tax_total": giftCardTaxTotal!,
    if (shippingTotal != null) "shipping_total": shippingTotal!,
    if (shippingSubtotal != null) "shipping_subtotal": shippingSubtotal!,
    if (shippingTaxTotal != null) "shipping_tax_total": shippingTaxTotal!,
    if (originalShippingTotal != null) "original_shipping_total": originalShippingTotal!,
    if (originalShippingSubtotal != null) "original_shipping_subtotal": originalShippingSubtotal!,
    if (originalShippingTaxTotal != null) "original_shipping_tax_total": originalShippingTaxTotal!,
    if (completedAt != null) "completed_at": completedAt!.toIso8601String(),
  };
}

/// The cart's shipping address.
class BaseCartAddress {
  BaseCartAddress([this.value]);
  final dynamic value;
  factory BaseCartAddress.fromJson(dynamic json) => BaseCartAddress(json);
  dynamic toJson() => value;
}

/// The tax line's item.
class BaseCartLineItem {
  BaseCartLineItem({
    this.id,
    this.title,
    this.subtitle,
    this.thumbnail,
    this.quantity,
    this.product,
    this.productId,
    this.productTitle,
    this.productDescription,
    this.productSubtitle,
    this.productType,
    this.productCollection,
    this.productHandle,
    this.variant,
    this.variantId,
    this.variantSku,
    this.variantBarcode,
    this.variantTitle,
    this.variantOptionValues,
    this.requiresShipping,
    this.isDiscountable,
    this.isTaxInclusive,
    this.compareAtUnitPrice,
    this.unitPrice,
    this.taxLines,
    this.adjustments,
    this.cart,
    this.cartId,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.originalTotal,
    this.originalSubtotal,
    this.originalTaxTotal,
    this.itemTotal,
    this.itemSubtotal,
    this.itemTaxTotal,
    this.total,
    this.subtotal,
    this.taxTotal,
    this.discountTotal,
    this.discountTaxTotal,
  });

  /// The item's ID.
  final String? id;
  /// The item's title.
  final String? title;
  /// The item's subtitle.
  final String? subtitle;
  /// The item's thumbnail.
  final String? thumbnail;
  /// The item's quantity.
  final num? quantity;
  final BaseProduct? product;
  /// The item's product id.
  final String? productId;
  /// The item's product title.
  final String? productTitle;
  /// The item's product description.
  final String? productDescription;
  /// The item's product subtitle.
  final String? productSubtitle;
  /// The item's product type.
  final String? productType;
  /// The item's product collection.
  final String? productCollection;
  /// The item's product handle.
  final String? productHandle;
  final BaseProductVariant? variant;
  /// The item's variant id.
  final String? variantId;
  /// The item's variant sku.
  final String? variantSku;
  /// The item's variant barcode.
  final String? variantBarcode;
  /// The item's variant title.
  final String? variantTitle;
  /// The item's variant option values.
  final Map<String, dynamic>? variantOptionValues;
  /// The item's requires shipping.
  final bool? requiresShipping;
  /// The item's is discountable.
  final bool? isDiscountable;
  /// The item's is tax inclusive.
  final bool? isTaxInclusive;
  /// The item's compare at unit price.
  final num? compareAtUnitPrice;
  /// The item's unit price.
  final num? unitPrice;
  /// The item's tax lines.
  final List<Map<String, dynamic>>? taxLines;
  /// The item's adjustments.
  final List<BaseLineItemAdjustment>? adjustments;
  final BaseCart? cart;
  /// The item's cart id.
  final String? cartId;
  /// The item's metadata.
  final Map<String, dynamic>? metadata;
  /// The item's created at.
  final DateTime? createdAt;
  /// The item's updated at.
  final DateTime? updatedAt;
  /// The item's deleted at.
  final DateTime? deletedAt;
  /// The line item's original total before discounts, including taxes.
  final num? originalTotal;
  /// The line item's original subtotal before discounts, excluding taxes.
  final num? originalSubtotal;
  /// The line item's original tax total before discounts.
  final num? originalTaxTotal;
  /// The line item's total after discounts, including taxes.
  final num? itemTotal;
  /// The line item's subtotal before discounts, excluding taxes.
  final num? itemSubtotal;
  /// The line item's tax total after discounts.
  final num? itemTaxTotal;
  /// The line item's total after discounts, including taxes.
  final num? total;
  /// The line item's subtotal before discounts, excluding taxes.
  final num? subtotal;
  /// The line item's tax total after discounts.
  final num? taxTotal;
  /// The total amount of discounts applied to the line item, including the tax portion of discounts.
  final num? discountTotal;
  /// The total amount of discounts applied to the line item's tax. Represents the tax portion of discounts.
  final num? discountTaxTotal;

  factory BaseCartLineItem.fromJson(Map<String, dynamic> json) => BaseCartLineItem(
    id: json["id"] as String?,
    title: json["title"] as String?,
    subtitle: json["subtitle"] as String?,
    thumbnail: json["thumbnail"] as String?,
    quantity: json["quantity"] as num?,
    product: json["product"] == null ? null : BaseProduct.fromJson(json["product"] as Map<String, dynamic>),
    productId: json["product_id"] as String?,
    productTitle: json["product_title"] as String?,
    productDescription: json["product_description"] as String?,
    productSubtitle: json["product_subtitle"] as String?,
    productType: json["product_type"] as String?,
    productCollection: json["product_collection"] as String?,
    productHandle: json["product_handle"] as String?,
    variant: json["variant"] == null ? null : BaseProductVariant.fromJson(json["variant"] as Map<String, dynamic>),
    variantId: json["variant_id"] as String?,
    variantSku: json["variant_sku"] as String?,
    variantBarcode: json["variant_barcode"] as String?,
    variantTitle: json["variant_title"] as String?,
    variantOptionValues: json["variant_option_values"] as Map<String, dynamic>?,
    requiresShipping: json["requires_shipping"] as bool?,
    isDiscountable: json["is_discountable"] as bool?,
    isTaxInclusive: json["is_tax_inclusive"] as bool?,
    compareAtUnitPrice: json["compare_at_unit_price"] as num?,
    unitPrice: json["unit_price"] as num?,
    taxLines: (json["tax_lines"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    adjustments: (json["adjustments"] as List?)?.map((e) => e == null ? null : BaseLineItemAdjustment.fromJson(e as Map<String, dynamic>)).toList().cast<BaseLineItemAdjustment>(),
    cart: json["cart"] == null ? null : BaseCart.fromJson(json["cart"] as Map<String, dynamic>),
    cartId: json["cart_id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    originalTotal: json["original_total"] as num?,
    originalSubtotal: json["original_subtotal"] as num?,
    originalTaxTotal: json["original_tax_total"] as num?,
    itemTotal: json["item_total"] as num?,
    itemSubtotal: json["item_subtotal"] as num?,
    itemTaxTotal: json["item_tax_total"] as num?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    taxTotal: json["tax_total"] as num?,
    discountTotal: json["discount_total"] as num?,
    discountTaxTotal: json["discount_tax_total"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (title != null) "title": title!,
    if (subtitle != null) "subtitle": subtitle!,
    if (thumbnail != null) "thumbnail": thumbnail!,
    if (quantity != null) "quantity": quantity!,
    if (product != null) "product": product!.toJson(),
    if (productId != null) "product_id": productId!,
    if (productTitle != null) "product_title": productTitle!,
    if (productDescription != null) "product_description": productDescription!,
    if (productSubtitle != null) "product_subtitle": productSubtitle!,
    if (productType != null) "product_type": productType!,
    if (productCollection != null) "product_collection": productCollection!,
    if (productHandle != null) "product_handle": productHandle!,
    if (variant != null) "variant": variant!.toJson(),
    if (variantId != null) "variant_id": variantId!,
    if (variantSku != null) "variant_sku": variantSku!,
    if (variantBarcode != null) "variant_barcode": variantBarcode!,
    if (variantTitle != null) "variant_title": variantTitle!,
    if (variantOptionValues != null) "variant_option_values": variantOptionValues!,
    if (requiresShipping != null) "requires_shipping": requiresShipping!,
    if (isDiscountable != null) "is_discountable": isDiscountable!,
    if (isTaxInclusive != null) "is_tax_inclusive": isTaxInclusive!,
    if (compareAtUnitPrice != null) "compare_at_unit_price": compareAtUnitPrice!,
    if (unitPrice != null) "unit_price": unitPrice!,
    if (taxLines != null) "tax_lines": taxLines!.map((e) => e).toList(),
    if (adjustments != null) "adjustments": adjustments!.map((e) => e.toJson()).toList(),
    if (cart != null) "cart": cart!.toJson(),
    if (cartId != null) "cart_id": cartId!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (originalTotal != null) "original_total": originalTotal!,
    if (originalSubtotal != null) "original_subtotal": originalSubtotal!,
    if (originalTaxTotal != null) "original_tax_total": originalTaxTotal!,
    if (itemTotal != null) "item_total": itemTotal!,
    if (itemSubtotal != null) "item_subtotal": itemSubtotal!,
    if (itemTaxTotal != null) "item_tax_total": itemTaxTotal!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (taxTotal != null) "tax_total": taxTotal!,
    if (discountTotal != null) "discount_total": discountTotal!,
    if (discountTaxTotal != null) "discount_tax_total": discountTaxTotal!,
  };
}

/// A cart's shipping method.
class BaseCartShippingMethod {
  BaseCartShippingMethod({
    this.id,
    this.cartId,
    this.name,
    this.description,
    this.amount,
    this.isTaxInclusive,
    this.shippingOptionId,
    this.data,
    this.metadata,
    this.taxLines,
    this.adjustments,
    this.createdAt,
    this.updatedAt,
    this.originalTotal,
    this.originalSubtotal,
    this.originalTaxTotal,
    this.total,
    this.subtotal,
    this.taxTotal,
    this.discountTotal,
    this.discountTaxTotal,
  });

  /// The shipping method's ID.
  final String? id;
  /// The ID of the cart this shipping method belongs to.
  final String? cartId;
  /// The shipping method's name.
  final String? name;
  /// The shipping method's description.
  final String? description;
  /// The shipping method's amount.
  final num? amount;
  /// Whether the shipping method's amount is tax inclusive.
  final bool? isTaxInclusive;
  /// The ID of the shipping option this method was created from.
  final String? shippingOptionId;
  /// The shipping method's data, useful for fulfillment handling by third-party services.
  final Map<String, dynamic>? data;
  /// The shipping method's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The shipping method's tax lines.
  final List<BaseShippingMethodTaxLine>? taxLines;
  /// The shipping method's adjustments, such as applied promotions.
  final List<BaseShippingMethodAdjustment>? adjustments;
  /// The date the shipping method was created.
  final DateTime? createdAt;
  /// The date the shipping method was updated.
  final DateTime? updatedAt;
  /// The shipping method's original total before discounts, including taxes.
  final num? originalTotal;
  /// The shipping method's original subtotal before discounts, excluding taxes.
  final num? originalSubtotal;
  /// The shipping method's original tax total before discounts.
  final num? originalTaxTotal;
  /// The shipping method's total after discounts, including taxes.
  final num? total;
  /// The shipping method's subtotal before discounts, excluding taxes.
  final num? subtotal;
  /// The shipping method's tax total after discounts.
  final num? taxTotal;
  /// The total amount of discounts applied to the shipping method, including the tax portion of discounts.
  final num? discountTotal;
  /// The total amount of discounts applied to the shipping method's tax. Represents the tax portion of discounts.
  final num? discountTaxTotal;

  factory BaseCartShippingMethod.fromJson(Map<String, dynamic> json) => BaseCartShippingMethod(
    id: json["id"] as String?,
    cartId: json["cart_id"] as String?,
    name: json["name"] as String?,
    description: json["description"] as String?,
    amount: json["amount"] as num?,
    isTaxInclusive: json["is_tax_inclusive"] as bool?,
    shippingOptionId: json["shipping_option_id"] as String?,
    data: json["data"] as Map<String, dynamic>?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    taxLines: (json["tax_lines"] as List?)?.map((e) => e == null ? null : BaseShippingMethodTaxLine.fromJson(e as Map<String, dynamic>)).toList().cast<BaseShippingMethodTaxLine>(),
    adjustments: (json["adjustments"] as List?)?.map((e) => e == null ? null : BaseShippingMethodAdjustment.fromJson(e as Map<String, dynamic>)).toList().cast<BaseShippingMethodAdjustment>(),
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    originalTotal: json["original_total"] as num?,
    originalSubtotal: json["original_subtotal"] as num?,
    originalTaxTotal: json["original_tax_total"] as num?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    taxTotal: json["tax_total"] as num?,
    discountTotal: json["discount_total"] as num?,
    discountTaxTotal: json["discount_tax_total"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (cartId != null) "cart_id": cartId!,
    if (name != null) "name": name!,
    if (description != null) "description": description!,
    if (amount != null) "amount": amount!,
    if (isTaxInclusive != null) "is_tax_inclusive": isTaxInclusive!,
    if (shippingOptionId != null) "shipping_option_id": shippingOptionId!,
    if (data != null) "data": data!,
    if (metadata != null) "metadata": metadata!,
    if (taxLines != null) "tax_lines": taxLines!.map((e) => e.toJson()).toList(),
    if (adjustments != null) "adjustments": adjustments!.map((e) => e.toJson()).toList(),
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (originalTotal != null) "original_total": originalTotal!,
    if (originalSubtotal != null) "original_subtotal": originalSubtotal!,
    if (originalTaxTotal != null) "original_tax_total": originalTaxTotal!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (taxTotal != null) "tax_total": taxTotal!,
    if (discountTotal != null) "discount_total": discountTotal!,
    if (discountTaxTotal != null) "discount_tax_total": discountTaxTotal!,
  };
}

/// The claim item's details.
class BaseClaimItem {
  BaseClaimItem({
    this.id,
    this.claimId,
    this.orderId,
    this.itemId,
    this.quantity,
    this.reason,
    this.rawQuantity,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.item,
  });

  /// The claim item's ID.
  final String? id;
  /// The ID of the claim this item belongs to.
  final String? claimId;
  /// The ID of the order this item belongs to.
  final String? orderId;
  /// The ID of the item in the order.
  final String? itemId;
  /// The quantity claimed.
  final num? quantity;
  /// The claim's reason.
  final String? reason;
  /// The quantity claimed.
  final Map<String, dynamic>? rawQuantity;
  /// The item's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the claim was created.
  final DateTime? createdAt;
  /// The date the claim was updated.
  final DateTime? updatedAt;
  final AdminOrderLineItem? item;

  factory BaseClaimItem.fromJson(Map<String, dynamic> json) => BaseClaimItem(
    id: json["id"] as String?,
    claimId: json["claim_id"] as String?,
    orderId: json["order_id"] as String?,
    itemId: json["item_id"] as String?,
    quantity: json["quantity"] as num?,
    reason: json["reason"] as String?,
    rawQuantity: json["raw_quantity"] as Map<String, dynamic>?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    item: json["item"] == null ? null : AdminOrderLineItem.fromJson(json["item"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (claimId != null) "claim_id": claimId!,
    if (orderId != null) "order_id": orderId!,
    if (itemId != null) "item_id": itemId!,
    if (quantity != null) "quantity": quantity!,
    if (reason != null) "reason": reason!,
    if (rawQuantity != null) "raw_quantity": rawQuantity!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (item != null) "item": item!.toJson(),
  };
}

/// The product collection's details.
class BaseCollection {
  BaseCollection({
    this.id,
    this.title,
    this.handle,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.products,
    this.metadata,
    this.externalId,
  });

  /// The collection's ID.
  final String? id;
  /// The collection's title.
  final String? title;
  /// The collection's handle.
  final String? handle;
  /// The collection's creation date.
  final DateTime? createdAt;
  /// The collection's update date.
  final DateTime? updatedAt;
  /// The collection's deletion date.
  final DateTime? deletedAt;
  /// The collection's products.
  final List<Map<String, dynamic>>? products;
  /// The collection's metadata, used to store custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The collection's external ID used to link it with external systems.
  final String? externalId;

  factory BaseCollection.fromJson(Map<String, dynamic> json) => BaseCollection(
    id: json["id"] as String?,
    title: json["title"] as String?,
    handle: json["handle"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    products: (json["products"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    metadata: json["metadata"] as Map<String, dynamic>?,
    externalId: json["external_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (title != null) "title": title!,
    if (handle != null) "handle": handle!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (products != null) "products": products!.map((e) => e).toList(),
    if (metadata != null) "metadata": metadata!,
    if (externalId != null) "external_id": externalId!,
  };
}

/// The item's details.
class BaseExchangeItem {
  BaseExchangeItem({
    this.id,
    this.exchangeId,
    this.orderId,
    this.itemId,
    this.quantity,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.item,
  });

  /// The item's ID.
  final String? id;
  /// The ID of the exchange this item belongs to.
  final String? exchangeId;
  /// The ID of the order this item belongs to.
  final String? orderId;
  /// The ID of the item in the order.
  final String? itemId;
  /// The item's quantity.
  final num? quantity;
  /// The item's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the item was created.
  final DateTime? createdAt;
  /// The date the item was updated.
  final DateTime? updatedAt;
  final AdminOrderLineItem? item;

  factory BaseExchangeItem.fromJson(Map<String, dynamic> json) => BaseExchangeItem(
    id: json["id"] as String?,
    exchangeId: json["exchange_id"] as String?,
    orderId: json["order_id"] as String?,
    itemId: json["item_id"] as String?,
    quantity: json["quantity"] as num?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    item: json["item"] == null ? null : AdminOrderLineItem.fromJson(json["item"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (exchangeId != null) "exchange_id": exchangeId!,
    if (orderId != null) "order_id": orderId!,
    if (itemId != null) "item_id": itemId!,
    if (quantity != null) "quantity": quantity!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (item != null) "item": item!.toJson(),
  };
}

/// The fulfillment provider's details.
class BaseFulfillmentProvider {
  BaseFulfillmentProvider({
    this.id,
    this.isEnabled,
  });

  /// The provider's ID.
  final String? id;
  /// Whether the provider is enabled.
  final bool? isEnabled;

  factory BaseFulfillmentProvider.fromJson(Map<String, dynamic> json) => BaseFulfillmentProvider(
    id: json["id"] as String?,
    isEnabled: json["is_enabled"] as bool?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (isEnabled != null) "is_enabled": isEnabled!,
  };
}

/// The adjustment's details.
class BaseLineItemAdjustment {
  BaseLineItemAdjustment({
    this.item,
    this.itemId,
    this.id,
    this.code,
    this.amount,
    this.cartId,
    this.description,
    this.promotionId,
    this.providerId,
    this.createdAt,
    this.updatedAt,
  });

  final Map<String, dynamic>? item;
  /// The ID of the item this adjustment applies on.
  final String? itemId;
  /// The adjustment's ID.
  final String? id;
  /// The adjustment's code.
  final String? code;
  /// The adjustment's amount.
  final num? amount;
  /// The ID of the cart this adjustment belongs to.
  final String? cartId;
  /// The adjustment's description.
  final String? description;
  /// The ID of the promotion applied by this adjustment.
  final String? promotionId;
  /// The adjustment's provider ID.
  final String? providerId;
  /// The date the adjustment was created.
  final DateTime? createdAt;
  /// The date the adjustment was updated.
  final DateTime? updatedAt;

  factory BaseLineItemAdjustment.fromJson(Map<String, dynamic> json) => BaseLineItemAdjustment(
    item: json["item"] as Map<String, dynamic>?,
    itemId: json["item_id"] as String?,
    id: json["id"] as String?,
    code: json["code"] as String?,
    amount: json["amount"] as num?,
    cartId: json["cart_id"] as String?,
    description: json["description"] as String?,
    promotionId: json["promotion_id"] as String?,
    providerId: json["provider_id"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (item != null) "item": item!,
    if (itemId != null) "item_id": itemId!,
    if (id != null) "id": id!,
    if (code != null) "code": code!,
    if (amount != null) "amount": amount!,
    if (cartId != null) "cart_id": cartId!,
    if (description != null) "description": description!,
    if (promotionId != null) "promotion_id": promotionId!,
    if (providerId != null) "provider_id": providerId!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The tax line's details.
class BaseLineItemTaxLine {
  BaseLineItemTaxLine({
    this.item,
    this.itemId,
    this.total,
    this.subtotal,
    this.id,
    this.description,
    this.taxRateId,
    this.code,
    this.rate,
    this.providerId,
    this.createdAt,
    this.updatedAt,
  });

  final BaseCartLineItem? item;
  /// The ID of the line item this tax line belongs to.
  final String? itemId;
  /// The item's total including taxes and promotions.
  final num? total;
  /// The item's total excluding taxes, including promotions.
  final num? subtotal;
  /// The tax line's ID.
  final String? id;
  /// The tax line's description.
  final String? description;
  /// The ID of the applied tax rate.
  final String? taxRateId;
  /// The code that the tax rate is identified by.
  final String? code;
  /// The charged rate.
  final num? rate;
  /// The ID of the tax provider used to calculate the tax line.
  final String? providerId;
  /// The date the tax line was created.
  final DateTime? createdAt;
  /// The date the tax line was updated.
  final DateTime? updatedAt;

  factory BaseLineItemTaxLine.fromJson(Map<String, dynamic> json) => BaseLineItemTaxLine(
    item: json["item"] == null ? null : BaseCartLineItem.fromJson(json["item"] as Map<String, dynamic>),
    itemId: json["item_id"] as String?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    id: json["id"] as String?,
    description: json["description"] as String?,
    taxRateId: json["tax_rate_id"] as String?,
    code: json["code"] as String?,
    rate: json["rate"] as num?,
    providerId: json["provider_id"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (item != null) "item": item!.toJson(),
    if (itemId != null) "item_id": itemId!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (id != null) "id": id!,
    if (description != null) "description": description!,
    if (taxRateId != null) "tax_rate_id": taxRateId!,
    if (code != null) "code": code!,
    if (rate != null) "rate": rate!,
    if (providerId != null) "provider_id": providerId!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The order's details.
class BaseOrder {
  BaseOrder({
    this.paymentCollections,
    this.fulfillments,
    this.shippingAddress,
    this.billingAddress,
    this.id,
    this.version,
    this.regionId,
    this.customerId,
    this.salesChannelId,
    this.email,
    this.currencyCode,
    this.displayId,
    this.items,
    this.shippingMethods,
    this.paymentStatus,
    this.fulfillmentStatus,
    this.transactions,
    this.summary,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.originalItemTotal,
    this.originalItemSubtotal,
    this.originalItemTaxTotal,
    this.itemTotal,
    this.itemSubtotal,
    this.itemTaxTotal,
    this.originalTotal,
    this.originalSubtotal,
    this.originalTaxTotal,
    this.total,
    this.subtotal,
    this.taxTotal,
    this.discountTotal,
    this.discountTaxTotal,
    this.giftCardTotal,
    this.giftCardTaxTotal,
    this.shippingTotal,
    this.shippingSubtotal,
    this.shippingTaxTotal,
    this.originalShippingTotal,
    this.originalShippingSubtotal,
    this.originalShippingTaxTotal,
    this.status,
    this.creditLineTotal,
    this.itemDiscountTotal,
    this.shippingDiscountTotal,
    this.customDisplayId,
  });

  /// The order's payment collections.
  final List<BasePaymentCollection>? paymentCollections;
  /// The order's fulfillments.
  final List<BaseOrderFulfillment>? fulfillments;
  final BaseOrderAddress? shippingAddress;
  final BaseOrderAddress? billingAddress;
  /// The order's ID.
  final String? id;
  /// The order's version.
  final num? version;
  /// The ID of the region associated with the order.
  final String? regionId;
  /// The ID of the customer that placed the order.
  final String? customerId;
  /// The ID of the sales channel the order is placed in.
  final String? salesChannelId;
  /// The email of the customer that placed the order.
  final String? email;
  /// The order's currency code.
  final String? currencyCode;
  /// The order's display ID.
  final num? displayId;
  /// The order's items.
  final List<BaseOrderLineItem>? items;
  /// The order's shipping methods.
  final List<BaseOrderShippingMethod>? shippingMethods;
  /// The order's payment status.
  final String? paymentStatus;
  /// The order's fulfillment status.
  final String? fulfillmentStatus;
  /// The order's transactions.
  final List<BaseOrderTransaction>? transactions;
  final BaseOrderSummary? summary;
  /// The order's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the order was created.
  final DateTime? createdAt;
  /// The date the order was updated.
  final DateTime? updatedAt;
  /// The sum of all line items' original totals before discounts, including taxes.
  final num? originalItemTotal;
  /// The sum of all line items' original subtotals before discounts, excluding taxes.
  final num? originalItemSubtotal;
  /// The sum of all line items' original tax totals before discounts.
  final num? originalItemTaxTotal;
  /// The sum of all line items' totals after discounts, including taxes.
  final num? itemTotal;
  /// The sum of all line items' subtotals before discounts, excluding taxes.
  final num? itemSubtotal;
  /// The sum of all line items' tax totals after discounts.
  final num? itemTaxTotal;
  /// The order's total before discounts, including taxes. Calculated as the sum of `original_item_total` and `original_shipping_total`.
  final num? originalTotal;
  /// The order's subtotal before discounts, excluding taxes. Calculated as the sum of `original_item_subtotal` and `original_shipping_subtotal`.
  final num? originalSubtotal;
  /// The order's tax total before discounts. Calculated as the sum of `original_item_tax_total` and `original_shipping_tax_total`.
  final num? originalTaxTotal;
  /// The order's final total after discounts and credit lines, including taxes.
  final num? total;
  /// The order's subtotal before discounts, excluding taxes. Calculated as the sum of `item_subtotal` and `shipping_subtotal`.
  final num? subtotal;
  /// The order's tax total after discounts. Calculated as the sum of `item_tax_total` and `shipping_tax_total`.
  final num? taxTotal;
  /// The total amount of discounts applied to the order, including the tax portion of discounts.
  final num? discountTotal;
  /// The total amount of discounts applied to the order's tax. Represents the tax portion of discounts.
  final num? discountTaxTotal;
  /// The order's gift card total.
  final num? giftCardTotal;
  /// The tax total of the order's gift card.
  final num? giftCardTaxTotal;
  /// The sum of all shipping methods' totals after discounts, including taxes.
  final num? shippingTotal;
  /// The sum of all shipping methods' subtotals before discounts, excluding taxes.
  final num? shippingSubtotal;
  /// The sum of all shipping methods' tax totals after discounts.
  final num? shippingTaxTotal;
  /// The sum of all shipping methods' original totals before discounts, including taxes.
  final num? originalShippingTotal;
  /// The sum of all shipping methods' original subtotals before discounts, excluding taxes.
  final num? originalShippingSubtotal;
  /// The sum of all shipping methods' original tax totals before discounts.
  final num? originalShippingTaxTotal;
  /// The order's status.
  final String? status;
  /// The total amount of credit lines applied to the order, including taxes. Subtracted from the final total.
  final num? creditLineTotal;
  /// The sum of all discounts applied to line items, including the tax portion of discounts.
  final num? itemDiscountTotal;
  /// The sum of all discounts applied to shipping methods, including the tax portion of discounts.
  final num? shippingDiscountTotal;
  /// The custom display ID of the order.
  final String? customDisplayId;

  factory BaseOrder.fromJson(Map<String, dynamic> json) => BaseOrder(
    paymentCollections: (json["payment_collections"] as List?)?.map((e) => e == null ? null : BasePaymentCollection.fromJson(e as Map<String, dynamic>)).toList().cast<BasePaymentCollection>(),
    fulfillments: (json["fulfillments"] as List?)?.map((e) => e == null ? null : BaseOrderFulfillment.fromJson(e as Map<String, dynamic>)).toList().cast<BaseOrderFulfillment>(),
    shippingAddress: json["shipping_address"] == null ? null : BaseOrderAddress.fromJson(json["shipping_address"] as Map<String, dynamic>),
    billingAddress: json["billing_address"] == null ? null : BaseOrderAddress.fromJson(json["billing_address"] as Map<String, dynamic>),
    id: json["id"] as String?,
    version: json["version"] as num?,
    regionId: json["region_id"] as String?,
    customerId: json["customer_id"] as String?,
    salesChannelId: json["sales_channel_id"] as String?,
    email: json["email"] as String?,
    currencyCode: json["currency_code"] as String?,
    displayId: json["display_id"] as num?,
    items: (json["items"] as List?)?.map((e) => e == null ? null : BaseOrderLineItem.fromJson(e as Map<String, dynamic>)).toList().cast<BaseOrderLineItem>(),
    shippingMethods: (json["shipping_methods"] as List?)?.map((e) => e == null ? null : BaseOrderShippingMethod.fromJson(e as Map<String, dynamic>)).toList().cast<BaseOrderShippingMethod>(),
    paymentStatus: json["payment_status"] as String?,
    fulfillmentStatus: json["fulfillment_status"] as String?,
    transactions: (json["transactions"] as List?)?.map((e) => e == null ? null : BaseOrderTransaction.fromJson(e as Map<String, dynamic>)).toList().cast<BaseOrderTransaction>(),
    summary: json["summary"] == null ? null : BaseOrderSummary.fromJson(json["summary"] as Map<String, dynamic>),
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    originalItemTotal: json["original_item_total"] as num?,
    originalItemSubtotal: json["original_item_subtotal"] as num?,
    originalItemTaxTotal: json["original_item_tax_total"] as num?,
    itemTotal: json["item_total"] as num?,
    itemSubtotal: json["item_subtotal"] as num?,
    itemTaxTotal: json["item_tax_total"] as num?,
    originalTotal: json["original_total"] as num?,
    originalSubtotal: json["original_subtotal"] as num?,
    originalTaxTotal: json["original_tax_total"] as num?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    taxTotal: json["tax_total"] as num?,
    discountTotal: json["discount_total"] as num?,
    discountTaxTotal: json["discount_tax_total"] as num?,
    giftCardTotal: json["gift_card_total"] as num?,
    giftCardTaxTotal: json["gift_card_tax_total"] as num?,
    shippingTotal: json["shipping_total"] as num?,
    shippingSubtotal: json["shipping_subtotal"] as num?,
    shippingTaxTotal: json["shipping_tax_total"] as num?,
    originalShippingTotal: json["original_shipping_total"] as num?,
    originalShippingSubtotal: json["original_shipping_subtotal"] as num?,
    originalShippingTaxTotal: json["original_shipping_tax_total"] as num?,
    status: json["status"] as String?,
    creditLineTotal: json["credit_line_total"] as num?,
    itemDiscountTotal: json["item_discount_total"] as num?,
    shippingDiscountTotal: json["shipping_discount_total"] as num?,
    customDisplayId: json["custom_display_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (paymentCollections != null) "payment_collections": paymentCollections!.map((e) => e.toJson()).toList(),
    if (fulfillments != null) "fulfillments": fulfillments!.map((e) => e.toJson()).toList(),
    if (shippingAddress != null) "shipping_address": shippingAddress!.toJson(),
    if (billingAddress != null) "billing_address": billingAddress!.toJson(),
    if (id != null) "id": id!,
    if (version != null) "version": version!,
    if (regionId != null) "region_id": regionId!,
    if (customerId != null) "customer_id": customerId!,
    if (salesChannelId != null) "sales_channel_id": salesChannelId!,
    if (email != null) "email": email!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (displayId != null) "display_id": displayId!,
    if (items != null) "items": items!.map((e) => e.toJson()).toList(),
    if (shippingMethods != null) "shipping_methods": shippingMethods!.map((e) => e.toJson()).toList(),
    if (paymentStatus != null) "payment_status": paymentStatus!,
    if (fulfillmentStatus != null) "fulfillment_status": fulfillmentStatus!,
    if (transactions != null) "transactions": transactions!.map((e) => e.toJson()).toList(),
    if (summary != null) "summary": summary!.toJson(),
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (originalItemTotal != null) "original_item_total": originalItemTotal!,
    if (originalItemSubtotal != null) "original_item_subtotal": originalItemSubtotal!,
    if (originalItemTaxTotal != null) "original_item_tax_total": originalItemTaxTotal!,
    if (itemTotal != null) "item_total": itemTotal!,
    if (itemSubtotal != null) "item_subtotal": itemSubtotal!,
    if (itemTaxTotal != null) "item_tax_total": itemTaxTotal!,
    if (originalTotal != null) "original_total": originalTotal!,
    if (originalSubtotal != null) "original_subtotal": originalSubtotal!,
    if (originalTaxTotal != null) "original_tax_total": originalTaxTotal!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (taxTotal != null) "tax_total": taxTotal!,
    if (discountTotal != null) "discount_total": discountTotal!,
    if (discountTaxTotal != null) "discount_tax_total": discountTaxTotal!,
    if (giftCardTotal != null) "gift_card_total": giftCardTotal!,
    if (giftCardTaxTotal != null) "gift_card_tax_total": giftCardTaxTotal!,
    if (shippingTotal != null) "shipping_total": shippingTotal!,
    if (shippingSubtotal != null) "shipping_subtotal": shippingSubtotal!,
    if (shippingTaxTotal != null) "shipping_tax_total": shippingTaxTotal!,
    if (originalShippingTotal != null) "original_shipping_total": originalShippingTotal!,
    if (originalShippingSubtotal != null) "original_shipping_subtotal": originalShippingSubtotal!,
    if (originalShippingTaxTotal != null) "original_shipping_tax_total": originalShippingTaxTotal!,
    if (status != null) "status": status!,
    if (creditLineTotal != null) "credit_line_total": creditLineTotal!,
    if (itemDiscountTotal != null) "item_discount_total": itemDiscountTotal!,
    if (shippingDiscountTotal != null) "shipping_discount_total": shippingDiscountTotal!,
    if (customDisplayId != null) "custom_display_id": customDisplayId!,
  };
}

/// An order address.
class BaseOrderAddress {
  BaseOrderAddress({
    this.id,
    this.customerId,
    this.firstName,
    this.lastName,
    this.phone,
    this.company,
    this.address1,
    this.address2,
    this.city,
    this.countryCode,
    this.province,
    this.postalCode,
    this.metadata,
    this.createdAt,
    this.updatedAt,
  });

  /// The address's ID.
  final String? id;
  /// The ID of the customer this address belongs to.
  final String? customerId;
  /// The address's first name.
  final String? firstName;
  /// The address's last name.
  final String? lastName;
  /// The address's phone.
  final String? phone;
  /// The address's company.
  final String? company;
  /// The address's first line.
  final String? address1;
  /// The address's second line.
  final String? address2;
  /// The address's city.
  final String? city;
  /// The address's country code.
  final String? countryCode;
  /// The address's lower-case ISO 3166-2 province code.
  final String? province;
  /// The address's postal code.
  final String? postalCode;
  /// The address's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the address was created.
  final DateTime? createdAt;
  /// The date the address was updated.
  final DateTime? updatedAt;

  factory BaseOrderAddress.fromJson(Map<String, dynamic> json) => BaseOrderAddress(
    id: json["id"] as String?,
    customerId: json["customer_id"] as String?,
    firstName: json["first_name"] as String?,
    lastName: json["last_name"] as String?,
    phone: json["phone"] as String?,
    company: json["company"] as String?,
    address1: json["address_1"] as String?,
    address2: json["address_2"] as String?,
    city: json["city"] as String?,
    countryCode: json["country_code"] as String?,
    province: json["province"] as String?,
    postalCode: json["postal_code"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (customerId != null) "customer_id": customerId!,
    if (firstName != null) "first_name": firstName!,
    if (lastName != null) "last_name": lastName!,
    if (phone != null) "phone": phone!,
    if (company != null) "company": company!,
    if (address1 != null) "address_1": address1!,
    if (address2 != null) "address_2": address2!,
    if (city != null) "city": city!,
    if (countryCode != null) "country_code": countryCode!,
    if (province != null) "province": province!,
    if (postalCode != null) "postal_code": postalCode!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The action's details.
class BaseOrderChangeAction {
  BaseOrderChangeAction([this.value]);
  final dynamic value;
  factory BaseOrderChangeAction.fromJson(dynamic json) => BaseOrderChangeAction(json);
  dynamic toJson() => value;
}

/// The fulfillment's details.
class BaseOrderFulfillment {
  BaseOrderFulfillment({
    this.id,
    this.locationId,
    this.packedAt,
    this.shippedAt,
    this.deliveredAt,
    this.canceledAt,
    this.data,
    this.providerId,
    this.shippingOptionId,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.requiresShipping,
    this.createdBy,
    this.markedShippedBy,
  });

  /// The fulfillment's ID.
  final String? id;
  /// The ID of the location the items are fulfilled from.
  final String? locationId;
  /// The date the items were packed.
  final DateTime? packedAt;
  /// The date the items were shipped.
  final DateTime? shippedAt;
  /// The date the items were delivered.
  final DateTime? deliveredAt;
  /// The date the items were canceled.
  final DateTime? canceledAt;
  /// Data useful for the fulfillment provider handling it. This is taken from the associated shipping option.
  final Map<String, dynamic>? data;
  /// The ID of the fulfillment provider used to handle the fulfillment.
  final String? providerId;
  /// The ID of the associated shipping option.
  final String? shippingOptionId;
  /// The fulfillment's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the fulfillment was created.
  final DateTime? createdAt;
  /// The date the fulfillment was updated.
  final DateTime? updatedAt;
  /// Whether the fulfillment requires shipping.
  final bool? requiresShipping;
  /// The ID of the user that created the fulfillment.
  final String? createdBy;
  /// The ID of the user that marked the fulfillment as shipped.
  final String? markedShippedBy;

  factory BaseOrderFulfillment.fromJson(Map<String, dynamic> json) => BaseOrderFulfillment(
    id: json["id"] as String?,
    locationId: json["location_id"] as String?,
    packedAt: json["packed_at"] == null ? null : DateTime.parse(json["packed_at"] as String),
    shippedAt: json["shipped_at"] == null ? null : DateTime.parse(json["shipped_at"] as String),
    deliveredAt: json["delivered_at"] == null ? null : DateTime.parse(json["delivered_at"] as String),
    canceledAt: json["canceled_at"] == null ? null : DateTime.parse(json["canceled_at"] as String),
    data: json["data"] as Map<String, dynamic>?,
    providerId: json["provider_id"] as String?,
    shippingOptionId: json["shipping_option_id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    requiresShipping: json["requires_shipping"] as bool?,
    createdBy: json["created_by"] as String?,
    markedShippedBy: json["marked_shipped_by"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (locationId != null) "location_id": locationId!,
    if (packedAt != null) "packed_at": packedAt!.toIso8601String(),
    if (shippedAt != null) "shipped_at": shippedAt!.toIso8601String(),
    if (deliveredAt != null) "delivered_at": deliveredAt!.toIso8601String(),
    if (canceledAt != null) "canceled_at": canceledAt!.toIso8601String(),
    if (data != null) "data": data!,
    if (providerId != null) "provider_id": providerId!,
    if (shippingOptionId != null) "shipping_option_id": shippingOptionId!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (requiresShipping != null) "requires_shipping": requiresShipping!,
    if (createdBy != null) "created_by": createdBy!,
    if (markedShippedBy != null) "marked_shipped_by": markedShippedBy!,
  };
}

/// The item's detail.
class BaseOrderItemDetail {
  BaseOrderItemDetail({
    this.id,
    this.itemId,
    this.item,
    this.quantity,
    this.fulfilledQuantity,
    this.deliveredQuantity,
    this.shippedQuantity,
    this.returnRequestedQuantity,
    this.returnReceivedQuantity,
    this.returnDismissedQuantity,
    this.writtenOffQuantity,
    this.metadata,
    this.createdAt,
    this.updatedAt,
  });

  /// the detail's ID.
  final String? id;
  /// the ID of the associated line item.
  final String? itemId;
  final Map<String, dynamic>? item;
  /// The item's quantity.
  final num? quantity;
  /// The item's fulfilled quantity.
  final num? fulfilledQuantity;
  /// The item's delivered quantity.
  final num? deliveredQuantity;
  /// The item's shipped quantity.
  final num? shippedQuantity;
  /// The item's quantity that's requested to be returned.
  final num? returnRequestedQuantity;
  /// The item's quantity that's returned and added to the underlying variant's stocked quantity.
  final num? returnReceivedQuantity;
  /// The item's quantity that's returned but damaged. So, it's not added to the underlying variant's stocked quantity.
  final num? returnDismissedQuantity;
  /// The item's quantity that's removed from the order.
  final num? writtenOffQuantity;
  /// The item's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the detail was created.
  final DateTime? createdAt;
  /// The date the detail was updated.
  final DateTime? updatedAt;

  factory BaseOrderItemDetail.fromJson(Map<String, dynamic> json) => BaseOrderItemDetail(
    id: json["id"] as String?,
    itemId: json["item_id"] as String?,
    item: json["item"] as Map<String, dynamic>?,
    quantity: json["quantity"] as num?,
    fulfilledQuantity: json["fulfilled_quantity"] as num?,
    deliveredQuantity: json["delivered_quantity"] as num?,
    shippedQuantity: json["shipped_quantity"] as num?,
    returnRequestedQuantity: json["return_requested_quantity"] as num?,
    returnReceivedQuantity: json["return_received_quantity"] as num?,
    returnDismissedQuantity: json["return_dismissed_quantity"] as num?,
    writtenOffQuantity: json["written_off_quantity"] as num?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (itemId != null) "item_id": itemId!,
    if (item != null) "item": item!,
    if (quantity != null) "quantity": quantity!,
    if (fulfilledQuantity != null) "fulfilled_quantity": fulfilledQuantity!,
    if (deliveredQuantity != null) "delivered_quantity": deliveredQuantity!,
    if (shippedQuantity != null) "shipped_quantity": shippedQuantity!,
    if (returnRequestedQuantity != null) "return_requested_quantity": returnRequestedQuantity!,
    if (returnReceivedQuantity != null) "return_received_quantity": returnReceivedQuantity!,
    if (returnDismissedQuantity != null) "return_dismissed_quantity": returnDismissedQuantity!,
    if (writtenOffQuantity != null) "written_off_quantity": writtenOffQuantity!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The item's items.
class BaseOrderLineItem {
  BaseOrderLineItem({
    this.id,
    this.title,
    this.subtitle,
    this.thumbnail,
    this.variant,
    this.variantId,
    this.product,
    this.productId,
    this.productTitle,
    this.productDescription,
    this.productSubtitle,
    this.productType,
    this.productCollection,
    this.productHandle,
    this.variantSku,
    this.variantBarcode,
    this.variantTitle,
    this.variantOptionValues,
    this.requiresShipping,
    this.isDiscountable,
    this.isTaxInclusive,
    this.compareAtUnitPrice,
    this.unitPrice,
    this.quantity,
    this.taxLines,
    this.adjustments,
    this.detail,
    this.createdAt,
    this.updatedAt,
    this.metadata,
    this.originalTotal,
    this.originalSubtotal,
    this.originalTaxTotal,
    this.itemTotal,
    this.itemSubtotal,
    this.itemTaxTotal,
    this.total,
    this.subtotal,
    this.taxTotal,
    this.discountTotal,
    this.discountTaxTotal,
    this.refundableTotal,
    this.refundableTotalPerUnit,
    this.productTypeId,
  });

  /// The item's ID.
  final String? id;
  /// The item's title.
  final String? title;
  /// The item's subtitle.
  final String? subtitle;
  /// The URL of the item's thumbnail.
  final String? thumbnail;
  final BaseProductVariant? variant;
  /// The ID of the associated variant.
  final String? variantId;
  final BaseProduct? product;
  /// The ID of the associated product.
  final String? productId;
  /// The item's product title.
  final String? productTitle;
  /// The item's product description.
  final String? productDescription;
  /// The item's product subtitle.
  final String? productSubtitle;
  /// The item's product type.
  final String? productType;
  /// The ID of the collection the item's product belongs to.
  final String? productCollection;
  /// The item's product handle.
  final String? productHandle;
  /// The item's variant SKU.
  final String? variantSku;
  /// The item's variant barcode.
  final String? variantBarcode;
  /// The item's variant title.
  final String? variantTitle;
  /// The values of the item variant's options.
  final Map<String, dynamic>? variantOptionValues;
  /// Whether the item requires shipping.
  final bool? requiresShipping;
  /// Whether the item is discountable.
  final bool? isDiscountable;
  /// Whether the item is tax inclusive.
  final bool? isTaxInclusive;
  /// The original price of the item before a promotion or sale.
  final num? compareAtUnitPrice;
  /// The item's unit price.
  final num? unitPrice;
  /// The item's quantity.
  final num? quantity;
  /// The item's tax lines.
  final List<BaseOrderLineItemTaxLine>? taxLines;
  /// The item's adjustments.
  final List<BaseOrderLineItemAdjustment>? adjustments;
  final BaseOrderItemDetail? detail;
  /// The date the item was created.
  final DateTime? createdAt;
  /// The date the item was updated.
  final DateTime? updatedAt;
  /// The item's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The line item's original total before discounts, including taxes.
  final num? originalTotal;
  /// The line item's original subtotal before discounts, excluding taxes.
  final num? originalSubtotal;
  /// The line item's original tax total before discounts.
  final num? originalTaxTotal;
  /// The line item's total after discounts, including taxes.
  final num? itemTotal;
  /// The line item's subtotal before discounts, excluding taxes.
  final num? itemSubtotal;
  /// The line item's tax total after discounts.
  final num? itemTaxTotal;
  /// The line item's total after discounts, including taxes.
  final num? total;
  /// The line item's subtotal before discounts, excluding taxes.
  final num? subtotal;
  /// The line item's tax total after discounts.
  final num? taxTotal;
  /// The total amount of discounts applied to the line item, including the tax portion of discounts.
  final num? discountTotal;
  /// The total amount of discounts applied to the line item's tax. Represents the tax portion of discounts.
  final num? discountTaxTotal;
  /// The total refundable amount of the item's total.
  final num? refundableTotal;
  /// The total refundable amount of the item's total for a single quantity.
  final num? refundableTotalPerUnit;
  /// The ID of the associated product's type.
  final String? productTypeId;

  factory BaseOrderLineItem.fromJson(Map<String, dynamic> json) => BaseOrderLineItem(
    id: json["id"] as String?,
    title: json["title"] as String?,
    subtitle: json["subtitle"] as String?,
    thumbnail: json["thumbnail"] as String?,
    variant: json["variant"] == null ? null : BaseProductVariant.fromJson(json["variant"] as Map<String, dynamic>),
    variantId: json["variant_id"] as String?,
    product: json["product"] == null ? null : BaseProduct.fromJson(json["product"] as Map<String, dynamic>),
    productId: json["product_id"] as String?,
    productTitle: json["product_title"] as String?,
    productDescription: json["product_description"] as String?,
    productSubtitle: json["product_subtitle"] as String?,
    productType: json["product_type"] as String?,
    productCollection: json["product_collection"] as String?,
    productHandle: json["product_handle"] as String?,
    variantSku: json["variant_sku"] as String?,
    variantBarcode: json["variant_barcode"] as String?,
    variantTitle: json["variant_title"] as String?,
    variantOptionValues: json["variant_option_values"] as Map<String, dynamic>?,
    requiresShipping: json["requires_shipping"] as bool?,
    isDiscountable: json["is_discountable"] as bool?,
    isTaxInclusive: json["is_tax_inclusive"] as bool?,
    compareAtUnitPrice: json["compare_at_unit_price"] as num?,
    unitPrice: json["unit_price"] as num?,
    quantity: json["quantity"] as num?,
    taxLines: (json["tax_lines"] as List?)?.map((e) => e == null ? null : BaseOrderLineItemTaxLine.fromJson(e as Map<String, dynamic>)).toList().cast<BaseOrderLineItemTaxLine>(),
    adjustments: (json["adjustments"] as List?)?.map((e) => e == null ? null : BaseOrderLineItemAdjustment.fromJson(e as Map<String, dynamic>)).toList().cast<BaseOrderLineItemAdjustment>(),
    detail: json["detail"] == null ? null : BaseOrderItemDetail.fromJson(json["detail"] as Map<String, dynamic>),
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    metadata: json["metadata"] as Map<String, dynamic>?,
    originalTotal: json["original_total"] as num?,
    originalSubtotal: json["original_subtotal"] as num?,
    originalTaxTotal: json["original_tax_total"] as num?,
    itemTotal: json["item_total"] as num?,
    itemSubtotal: json["item_subtotal"] as num?,
    itemTaxTotal: json["item_tax_total"] as num?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    taxTotal: json["tax_total"] as num?,
    discountTotal: json["discount_total"] as num?,
    discountTaxTotal: json["discount_tax_total"] as num?,
    refundableTotal: json["refundable_total"] as num?,
    refundableTotalPerUnit: json["refundable_total_per_unit"] as num?,
    productTypeId: json["product_type_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (title != null) "title": title!,
    if (subtitle != null) "subtitle": subtitle!,
    if (thumbnail != null) "thumbnail": thumbnail!,
    if (variant != null) "variant": variant!.toJson(),
    if (variantId != null) "variant_id": variantId!,
    if (product != null) "product": product!.toJson(),
    if (productId != null) "product_id": productId!,
    if (productTitle != null) "product_title": productTitle!,
    if (productDescription != null) "product_description": productDescription!,
    if (productSubtitle != null) "product_subtitle": productSubtitle!,
    if (productType != null) "product_type": productType!,
    if (productCollection != null) "product_collection": productCollection!,
    if (productHandle != null) "product_handle": productHandle!,
    if (variantSku != null) "variant_sku": variantSku!,
    if (variantBarcode != null) "variant_barcode": variantBarcode!,
    if (variantTitle != null) "variant_title": variantTitle!,
    if (variantOptionValues != null) "variant_option_values": variantOptionValues!,
    if (requiresShipping != null) "requires_shipping": requiresShipping!,
    if (isDiscountable != null) "is_discountable": isDiscountable!,
    if (isTaxInclusive != null) "is_tax_inclusive": isTaxInclusive!,
    if (compareAtUnitPrice != null) "compare_at_unit_price": compareAtUnitPrice!,
    if (unitPrice != null) "unit_price": unitPrice!,
    if (quantity != null) "quantity": quantity!,
    if (taxLines != null) "tax_lines": taxLines!.map((e) => e.toJson()).toList(),
    if (adjustments != null) "adjustments": adjustments!.map((e) => e.toJson()).toList(),
    if (detail != null) "detail": detail!.toJson(),
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (metadata != null) "metadata": metadata!,
    if (originalTotal != null) "original_total": originalTotal!,
    if (originalSubtotal != null) "original_subtotal": originalSubtotal!,
    if (originalTaxTotal != null) "original_tax_total": originalTaxTotal!,
    if (itemTotal != null) "item_total": itemTotal!,
    if (itemSubtotal != null) "item_subtotal": itemSubtotal!,
    if (itemTaxTotal != null) "item_tax_total": itemTaxTotal!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (taxTotal != null) "tax_total": taxTotal!,
    if (discountTotal != null) "discount_total": discountTotal!,
    if (discountTaxTotal != null) "discount_tax_total": discountTaxTotal!,
    if (refundableTotal != null) "refundable_total": refundableTotal!,
    if (refundableTotalPerUnit != null) "refundable_total_per_unit": refundableTotalPerUnit!,
    if (productTypeId != null) "product_type_id": productTypeId!,
  };
}

/// The adjustment's details.
class BaseOrderLineItemAdjustment {
  BaseOrderLineItemAdjustment({
    this.item,
    this.itemId,
    this.id,
    this.code,
    this.amount,
    this.orderId,
    this.description,
    this.promotionId,
    this.providerId,
    this.createdAt,
    this.updatedAt,
  });

  final Map<String, dynamic>? item;
  /// The ID of the item this adjustment applies to.
  final String? itemId;
  /// The adjustment's ID.
  final String? id;
  /// The adjustment's code.
  final String? code;
  /// The adjustment's amount.
  final num? amount;
  /// The ID of the associated order.
  final String? orderId;
  /// The adjustment's description.
  final String? description;
  /// The ID of the promotion that this applies on the order's item.
  final String? promotionId;
  /// The adjustment's provider ID.
  final String? providerId;
  /// The date the adjustment was created.
  final DateTime? createdAt;
  /// The date the adjustment was updated.
  final DateTime? updatedAt;

  factory BaseOrderLineItemAdjustment.fromJson(Map<String, dynamic> json) => BaseOrderLineItemAdjustment(
    item: json["item"] as Map<String, dynamic>?,
    itemId: json["item_id"] as String?,
    id: json["id"] as String?,
    code: json["code"] as String?,
    amount: json["amount"] as num?,
    orderId: json["order_id"] as String?,
    description: json["description"] as String?,
    promotionId: json["promotion_id"] as String?,
    providerId: json["provider_id"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (item != null) "item": item!,
    if (itemId != null) "item_id": itemId!,
    if (id != null) "id": id!,
    if (code != null) "code": code!,
    if (amount != null) "amount": amount!,
    if (orderId != null) "order_id": orderId!,
    if (description != null) "description": description!,
    if (promotionId != null) "promotion_id": promotionId!,
    if (providerId != null) "provider_id": providerId!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The tax line's tax lines.
class BaseOrderLineItemTaxLine {
  BaseOrderLineItemTaxLine({
    this.item,
    this.itemId,
    this.total,
    this.subtotal,
    this.id,
    this.description,
    this.taxRateId,
    this.code,
    this.rate,
    this.providerId,
    this.createdAt,
    this.updatedAt,
  });

  final Map<String, dynamic>? item;
  /// The ID of the associated line item.
  final String? itemId;
  /// The tax line's total including promotions.
  final num? total;
  /// The tax line's subtotal excluding promotions.
  final num? subtotal;
  /// The tax line's ID.
  final String? id;
  /// The tax line's description.
  final String? description;
  /// The ID of the applied tax rate.
  final String? taxRateId;
  /// The code that the tax rate is identified by.
  final String? code;
  /// The rate to charge.
  final num? rate;
  /// The ID of the tax provider used to calculate the tax line.
  final String? providerId;
  /// The date the tax line was created.
  final DateTime? createdAt;
  /// The date the tax line was updated.
  final DateTime? updatedAt;

  factory BaseOrderLineItemTaxLine.fromJson(Map<String, dynamic> json) => BaseOrderLineItemTaxLine(
    item: json["item"] as Map<String, dynamic>?,
    itemId: json["item_id"] as String?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    id: json["id"] as String?,
    description: json["description"] as String?,
    taxRateId: json["tax_rate_id"] as String?,
    code: json["code"] as String?,
    rate: json["rate"] as num?,
    providerId: json["provider_id"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (item != null) "item": item!,
    if (itemId != null) "item_id": itemId!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (id != null) "id": id!,
    if (description != null) "description": description!,
    if (taxRateId != null) "tax_rate_id": taxRateId!,
    if (code != null) "code": code!,
    if (rate != null) "rate": rate!,
    if (providerId != null) "provider_id": providerId!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// Details of changes to a shipping method.
class BaseOrderShippingDetail {
  BaseOrderShippingDetail({
    this.id,
    this.shippingMethodId,
    this.shippingMethod,
    this.claimId,
    this.exchangeId,
    this.returnId,
    this.createdAt,
    this.updatedAt,
  });

  /// The ID of the new changes to the shipping method.
  final String? id;
  /// The ID of the shipping method.
  final String? shippingMethodId;
  final Map<String, dynamic>? shippingMethod;
  /// The ID of the associated claim.
  final String? claimId;
  /// The ID of the associated exchange.
  final String? exchangeId;
  /// The ID of the associated return.
  final String? returnId;
  /// The date the shipping method change was created.
  final DateTime? createdAt;
  /// The date the shipping method change was updated.
  final DateTime? updatedAt;

  factory BaseOrderShippingDetail.fromJson(Map<String, dynamic> json) => BaseOrderShippingDetail(
    id: json["id"] as String?,
    shippingMethodId: json["shipping_method_id"] as String?,
    shippingMethod: json["shipping_method"] as Map<String, dynamic>?,
    claimId: json["claim_id"] as String?,
    exchangeId: json["exchange_id"] as String?,
    returnId: json["return_id"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (shippingMethodId != null) "shipping_method_id": shippingMethodId!,
    if (shippingMethod != null) "shipping_method": shippingMethod!,
    if (claimId != null) "claim_id": claimId!,
    if (exchangeId != null) "exchange_id": exchangeId!,
    if (returnId != null) "return_id": returnId!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The shipping method's details.
class BaseOrderShippingMethod {
  BaseOrderShippingMethod({
    this.id,
    this.orderId,
    this.name,
    this.description,
    this.amount,
    this.isTaxInclusive,
    this.shippingOptionId,
    this.data,
    this.metadata,
    this.taxLines,
    this.adjustments,
    this.originalTotal,
    this.originalSubtotal,
    this.originalTaxTotal,
    this.total,
    this.subtotal,
    this.taxTotal,
    this.discountTotal,
    this.discountTaxTotal,
    this.createdAt,
    this.updatedAt,
    this.detail,
  });

  /// The shipping method's ID.
  final String? id;
  /// The ID of the order the shipping method belongs to.
  final String? orderId;
  /// The shipping method's name.
  final String? name;
  /// The shipping method's description.
  final String? description;
  /// The shipping method's amount.
  final num? amount;
  /// Whether the shipping method's amount includes applied taxes.
  final bool? isTaxInclusive;
  /// The ID of the shipping option this method was created from.
  final String? shippingOptionId;
  /// The shipping method's data, useful for fulfillment provider handling its fulfillment.
  final Map<String, dynamic>? data;
  /// The shipping method's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The shipping method's tax lines.
  final List<BaseOrderShippingMethodTaxLine>? taxLines;
  /// The shipping method's adjustments.
  final List<BaseOrderShippingMethodAdjustment>? adjustments;
  /// The shipping method's total including taxes, excluding promotions.
  final num? originalTotal;
  /// The shipping method's total excluding taxes, including promotions.
  final num? originalSubtotal;
  /// The shipping method's total taxes excluding promotions.
  final num? originalTaxTotal;
  /// The shipping method's total including taxes and promotions.
  final num? total;
  /// The shipping method's total excluding taxes, including promotions.
  final num? subtotal;
  /// The shipping method's tax total including promotions.
  final num? taxTotal;
  /// The total discounts applied on the shipping method.
  final num? discountTotal;
  /// The taxes applied on the discount amount.
  final num? discountTaxTotal;
  /// The date the shipping method was created.
  final DateTime? createdAt;
  /// The date the shipping method was updated.
  final DateTime? updatedAt;
  final BaseOrderShippingDetail? detail;

  factory BaseOrderShippingMethod.fromJson(Map<String, dynamic> json) => BaseOrderShippingMethod(
    id: json["id"] as String?,
    orderId: json["order_id"] as String?,
    name: json["name"] as String?,
    description: json["description"] as String?,
    amount: json["amount"] as num?,
    isTaxInclusive: json["is_tax_inclusive"] as bool?,
    shippingOptionId: json["shipping_option_id"] as String?,
    data: json["data"] as Map<String, dynamic>?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    taxLines: (json["tax_lines"] as List?)?.map((e) => e == null ? null : BaseOrderShippingMethodTaxLine.fromJson(e as Map<String, dynamic>)).toList().cast<BaseOrderShippingMethodTaxLine>(),
    adjustments: (json["adjustments"] as List?)?.map((e) => e == null ? null : BaseOrderShippingMethodAdjustment.fromJson(e as Map<String, dynamic>)).toList().cast<BaseOrderShippingMethodAdjustment>(),
    originalTotal: json["original_total"] as num?,
    originalSubtotal: json["original_subtotal"] as num?,
    originalTaxTotal: json["original_tax_total"] as num?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    taxTotal: json["tax_total"] as num?,
    discountTotal: json["discount_total"] as num?,
    discountTaxTotal: json["discount_tax_total"] as num?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    detail: json["detail"] == null ? null : BaseOrderShippingDetail.fromJson(json["detail"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (orderId != null) "order_id": orderId!,
    if (name != null) "name": name!,
    if (description != null) "description": description!,
    if (amount != null) "amount": amount!,
    if (isTaxInclusive != null) "is_tax_inclusive": isTaxInclusive!,
    if (shippingOptionId != null) "shipping_option_id": shippingOptionId!,
    if (data != null) "data": data!,
    if (metadata != null) "metadata": metadata!,
    if (taxLines != null) "tax_lines": taxLines!.map((e) => e.toJson()).toList(),
    if (adjustments != null) "adjustments": adjustments!.map((e) => e.toJson()).toList(),
    if (originalTotal != null) "original_total": originalTotal!,
    if (originalSubtotal != null) "original_subtotal": originalSubtotal!,
    if (originalTaxTotal != null) "original_tax_total": originalTaxTotal!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (taxTotal != null) "tax_total": taxTotal!,
    if (discountTotal != null) "discount_total": discountTotal!,
    if (discountTaxTotal != null) "discount_tax_total": discountTaxTotal!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (detail != null) "detail": detail!.toJson(),
  };
}

/// The adjustment's adjustments.
class BaseOrderShippingMethodAdjustment {
  BaseOrderShippingMethodAdjustment({
    this.shippingMethod,
    this.shippingMethodId,
    this.id,
    this.code,
    this.amount,
    this.orderId,
    this.description,
    this.promotionId,
    this.providerId,
    this.createdAt,
    this.updatedAt,
  });

  final Map<String, dynamic>? shippingMethod;
  /// The ID of the shipping method this adjustment applies on.
  final String? shippingMethodId;
  /// The adjustment's ID.
  final String? id;
  /// The adjustment's code.
  final String? code;
  /// The adjustment's amount.
  final num? amount;
  /// The ID of the associated order.
  final String? orderId;
  /// The adjustment's description.
  final String? description;
  /// The ID of the promotion applied by this adjustment.
  final String? promotionId;
  /// The adjustment's provider ID.
  final String? providerId;
  /// The date the adjustment was created.
  final DateTime? createdAt;
  /// The date the adjustment was updated.
  final DateTime? updatedAt;

  factory BaseOrderShippingMethodAdjustment.fromJson(Map<String, dynamic> json) => BaseOrderShippingMethodAdjustment(
    shippingMethod: json["shipping_method"] as Map<String, dynamic>?,
    shippingMethodId: json["shipping_method_id"] as String?,
    id: json["id"] as String?,
    code: json["code"] as String?,
    amount: json["amount"] as num?,
    orderId: json["order_id"] as String?,
    description: json["description"] as String?,
    promotionId: json["promotion_id"] as String?,
    providerId: json["provider_id"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (shippingMethod != null) "shipping_method": shippingMethod!,
    if (shippingMethodId != null) "shipping_method_id": shippingMethodId!,
    if (id != null) "id": id!,
    if (code != null) "code": code!,
    if (amount != null) "amount": amount!,
    if (orderId != null) "order_id": orderId!,
    if (description != null) "description": description!,
    if (promotionId != null) "promotion_id": promotionId!,
    if (providerId != null) "provider_id": providerId!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The tax line's details.
class BaseOrderShippingMethodTaxLine {
  BaseOrderShippingMethodTaxLine({
    this.shippingMethod,
    this.shippingMethodId,
    this.total,
    this.subtotal,
    this.id,
    this.description,
    this.taxRateId,
    this.code,
    this.rate,
    this.providerId,
    this.createdAt,
    this.updatedAt,
  });

  final Map<String, dynamic>? shippingMethod;
  /// The ID of the shipping method this tax line belongs to.
  final String? shippingMethodId;
  /// The shipping method's total including taxes and promotions.
  final num? total;
  /// The shipping method's total excluding taxes, including promotions.
  final num? subtotal;
  /// The tax line's ID.
  final String? id;
  /// The tax line's description.
  final String? description;
  /// The ID of the applied tax rate.
  final String? taxRateId;
  /// The code that the tax rate is identified by.
  final String? code;
  /// The rate to charge.
  final num? rate;
  /// The ID of the tax provider handling the tax calculation.
  final String? providerId;
  /// The date the tax line was created.
  final DateTime? createdAt;
  /// The date the tax line was updated.
  final DateTime? updatedAt;

  factory BaseOrderShippingMethodTaxLine.fromJson(Map<String, dynamic> json) => BaseOrderShippingMethodTaxLine(
    shippingMethod: json["shipping_method"] as Map<String, dynamic>?,
    shippingMethodId: json["shipping_method_id"] as String?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    id: json["id"] as String?,
    description: json["description"] as String?,
    taxRateId: json["tax_rate_id"] as String?,
    code: json["code"] as String?,
    rate: json["rate"] as num?,
    providerId: json["provider_id"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (shippingMethod != null) "shipping_method": shippingMethod!,
    if (shippingMethodId != null) "shipping_method_id": shippingMethodId!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (id != null) "id": id!,
    if (description != null) "description": description!,
    if (taxRateId != null) "tax_rate_id": taxRateId!,
    if (code != null) "code": code!,
    if (rate != null) "rate": rate!,
    if (providerId != null) "provider_id": providerId!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The order's summary details.
class BaseOrderSummary {
  BaseOrderSummary({
    this.paidTotal,
    this.refundedTotal,
    this.pendingDifference,
    this.currentOrderTotal,
    this.originalOrderTotal,
    this.transactionTotal,
    this.accountingTotal,
  });

  /// The total amount paid.
  final num? paidTotal;
  /// The total amount refunded.
  final num? refundedTotal;
  /// The difference pending to be processed. If negative, the customer needs a refund. Otherwise, additional payment is required from the customer.
  final num? pendingDifference;
  /// The order's current total, could be the total after a change in the order.
  final num? currentOrderTotal;
  /// The order's original total.
  final num? originalOrderTotal;
  /// The total of the transactions made on the order.
  final num? transactionTotal;
  /// The order's total without the credit-line total.
  final num? accountingTotal;

  factory BaseOrderSummary.fromJson(Map<String, dynamic> json) => BaseOrderSummary(
    paidTotal: json["paid_total"] as num?,
    refundedTotal: json["refunded_total"] as num?,
    pendingDifference: json["pending_difference"] as num?,
    currentOrderTotal: json["current_order_total"] as num?,
    originalOrderTotal: json["original_order_total"] as num?,
    transactionTotal: json["transaction_total"] as num?,
    accountingTotal: json["accounting_total"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (paidTotal != null) "paid_total": paidTotal!,
    if (refundedTotal != null) "refunded_total": refundedTotal!,
    if (pendingDifference != null) "pending_difference": pendingDifference!,
    if (currentOrderTotal != null) "current_order_total": currentOrderTotal!,
    if (originalOrderTotal != null) "original_order_total": originalOrderTotal!,
    if (transactionTotal != null) "transaction_total": transactionTotal!,
    if (accountingTotal != null) "accounting_total": accountingTotal!,
  };
}

/// An order transaction's details.
class BaseOrderTransaction {
  BaseOrderTransaction({
    this.id,
    this.orderId,
    this.amount,
    this.currencyCode,
    this.reference,
    this.referenceId,
    this.metadata,
    this.createdAt,
    this.updatedAt,
  });

  /// The transaction's ID.
  final String? id;
  /// The ID of the order this transaction belongs to.
  final String? orderId;
  /// The transaction's amount.
  final num? amount;
  /// The transaction's currency code.
  final String? currencyCode;
  /// The name of a table that this transaction references. If this transaction is for captured payment, its value is `capture`. If this transaction is for refunded p
  final String? reference;
  /// The ID of the referenced record in the referenced table.
  final String? referenceId;
  /// The transaction's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date that the transaction was created.
  final DateTime? createdAt;
  /// The date that the transaction was updated.
  final DateTime? updatedAt;

  factory BaseOrderTransaction.fromJson(Map<String, dynamic> json) => BaseOrderTransaction(
    id: json["id"] as String?,
    orderId: json["order_id"] as String?,
    amount: json["amount"] as num?,
    currencyCode: json["currency_code"] as String?,
    reference: json["reference"] as String?,
    referenceId: json["reference_id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (orderId != null) "order_id": orderId!,
    if (amount != null) "amount": amount!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (reference != null) "reference": reference!,
    if (referenceId != null) "reference_id": referenceId!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The payment's details.
class BasePayment {
  BasePayment({
    this.id,
    this.amount,
    this.authorizedAmount,
    this.currencyCode,
    this.providerId,
    this.data,
    this.createdAt,
    this.updatedAt,
    this.capturedAt,
    this.canceledAt,
    this.capturedAmount,
    this.refundedAmount,
    this.captures,
    this.refunds,
    this.paymentCollection,
    this.paymentSession,
  });

  /// The payment's ID.
  final String? id;
  /// The payment's amount.
  final num? amount;
  /// The amount authorized of the payment.
  final num? authorizedAmount;
  /// The payment's currency code.
  final String? currencyCode;
  /// The ID of the payment provider used to process this payment.
  final String? providerId;
  /// The payment's data, useful for processing by the payment provider.
  final Map<String, dynamic>? data;
  /// The date the payment was created.
  final DateTime? createdAt;
  /// The date the payment was updated.
  final DateTime? updatedAt;
  /// The date the payment was captured.
  final DateTime? capturedAt;
  /// The date the payment was canceled.
  final DateTime? canceledAt;
  /// The captured amount of the payment.
  final num? capturedAmount;
  /// The refunded amount of the payment.
  final num? refundedAmount;
  /// The details of payment captures.
  final List<BaseCapture>? captures;
  /// The details of payment refunds.
  final List<BaseRefund>? refunds;
  final Map<String, dynamic>? paymentCollection;
  final Map<String, dynamic>? paymentSession;

  factory BasePayment.fromJson(Map<String, dynamic> json) => BasePayment(
    id: json["id"] as String?,
    amount: json["amount"] as num?,
    authorizedAmount: json["authorized_amount"] as num?,
    currencyCode: json["currency_code"] as String?,
    providerId: json["provider_id"] as String?,
    data: json["data"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    capturedAt: json["captured_at"] == null ? null : DateTime.parse(json["captured_at"] as String),
    canceledAt: json["canceled_at"] == null ? null : DateTime.parse(json["canceled_at"] as String),
    capturedAmount: json["captured_amount"] as num?,
    refundedAmount: json["refunded_amount"] as num?,
    captures: (json["captures"] as List?)?.map((e) => e == null ? null : BaseCapture.fromJson(e as Map<String, dynamic>)).toList().cast<BaseCapture>(),
    refunds: (json["refunds"] as List?)?.map((e) => e == null ? null : BaseRefund.fromJson(e as Map<String, dynamic>)).toList().cast<BaseRefund>(),
    paymentCollection: json["payment_collection"] as Map<String, dynamic>?,
    paymentSession: json["payment_session"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (amount != null) "amount": amount!,
    if (authorizedAmount != null) "authorized_amount": authorizedAmount!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (providerId != null) "provider_id": providerId!,
    if (data != null) "data": data!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (capturedAt != null) "captured_at": capturedAt!.toIso8601String(),
    if (canceledAt != null) "canceled_at": canceledAt!.toIso8601String(),
    if (capturedAmount != null) "captured_amount": capturedAmount!,
    if (refundedAmount != null) "refunded_amount": refundedAmount!,
    if (captures != null) "captures": captures!.map((e) => e.toJson()).toList(),
    if (refunds != null) "refunds": refunds!.map((e) => e.toJson()).toList(),
    if (paymentCollection != null) "payment_collection": paymentCollection!,
    if (paymentSession != null) "payment_session": paymentSession!,
  };
}

/// The payment collection's details.
class BasePaymentCollection {
  BasePaymentCollection({
    this.id,
    this.currencyCode,
    this.amount,
    this.authorizedAmount,
    this.capturedAmount,
    this.refundedAmount,
    this.completedAt,
    this.createdAt,
    this.updatedAt,
    this.metadata,
    this.status,
    this.paymentProviders,
    this.paymentSessions,
    this.payments,
  });

  /// The payment collection's ID.
  final String? id;
  /// The payment collection's currency code.
  final String? currencyCode;
  /// The total amount to be paid.
  final num? amount;
  /// The total authorized amount of the collection's payments.
  final num? authorizedAmount;
  /// The total captured amount of the collection's payments.
  final num? capturedAmount;
  /// The total refunded amount of the collection's payments.
  final num? refundedAmount;
  /// The date the payment collection was completed.
  final DateTime? completedAt;
  /// The date the payment collection was created.
  final DateTime? createdAt;
  /// The date the payment collection was updated.
  final DateTime? updatedAt;
  /// The payment collection's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The payment collection's status.
  final String? status;
  /// The payment provider used to process the collection's payments and sessions.
  final List<BasePaymentProvider>? paymentProviders;
  /// The payment collection's payment sessions.
  final List<BasePaymentSession>? paymentSessions;
  /// The payment collection's payments.
  final List<BasePayment>? payments;

  factory BasePaymentCollection.fromJson(Map<String, dynamic> json) => BasePaymentCollection(
    id: json["id"] as String?,
    currencyCode: json["currency_code"] as String?,
    amount: json["amount"] as num?,
    authorizedAmount: json["authorized_amount"] as num?,
    capturedAmount: json["captured_amount"] as num?,
    refundedAmount: json["refunded_amount"] as num?,
    completedAt: json["completed_at"] == null ? null : DateTime.parse(json["completed_at"] as String),
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    metadata: json["metadata"] as Map<String, dynamic>?,
    status: json["status"] as String?,
    paymentProviders: (json["payment_providers"] as List?)?.map((e) => e == null ? null : BasePaymentProvider.fromJson(e as Map<String, dynamic>)).toList().cast<BasePaymentProvider>(),
    paymentSessions: (json["payment_sessions"] as List?)?.map((e) => e == null ? null : BasePaymentSession.fromJson(e as Map<String, dynamic>)).toList().cast<BasePaymentSession>(),
    payments: (json["payments"] as List?)?.map((e) => e == null ? null : BasePayment.fromJson(e as Map<String, dynamic>)).toList().cast<BasePayment>(),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (amount != null) "amount": amount!,
    if (authorizedAmount != null) "authorized_amount": authorizedAmount!,
    if (capturedAmount != null) "captured_amount": capturedAmount!,
    if (refundedAmount != null) "refunded_amount": refundedAmount!,
    if (completedAt != null) "completed_at": completedAt!.toIso8601String(),
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (metadata != null) "metadata": metadata!,
    if (status != null) "status": status!,
    if (paymentProviders != null) "payment_providers": paymentProviders!.map((e) => e.toJson()).toList(),
    if (paymentSessions != null) "payment_sessions": paymentSessions!.map((e) => e.toJson()).toList(),
    if (payments != null) "payments": payments!.map((e) => e.toJson()).toList(),
  };
}

/// The payment provider's details.
class BasePaymentProvider {
  BasePaymentProvider({
    this.id,
  });

  /// The payment provider's ID.
  final String? id;

  factory BasePaymentProvider.fromJson(Map<String, dynamic> json) => BasePaymentProvider(
    id: json["id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
  };
}

/// The payment session's details.
class BasePaymentSession {
  BasePaymentSession({
    this.id,
    this.amount,
    this.currencyCode,
    this.providerId,
    this.data,
    this.context,
    this.status,
    this.authorizedAt,
    this.paymentCollection,
    this.payment,
  });

  /// The payment session's ID.
  final String? id;
  /// The payment session's amount.
  final num? amount;
  /// The payment session's currency code.
  final String? currencyCode;
  /// The ID of the payment provider processing this session.
  final String? providerId;
  /// The payment session's data, useful for the payment provider processing the payment.
  final Map<String, dynamic>? data;
  /// The context around the payment, such as the customer's details.
  final Map<String, dynamic>? context;
  /// The payment session's status.
  final String? status;
  /// The date the payment session was authorized.
  final DateTime? authorizedAt;
  final Map<String, dynamic>? paymentCollection;
  final BasePayment? payment;

  factory BasePaymentSession.fromJson(Map<String, dynamic> json) => BasePaymentSession(
    id: json["id"] as String?,
    amount: json["amount"] as num?,
    currencyCode: json["currency_code"] as String?,
    providerId: json["provider_id"] as String?,
    data: json["data"] as Map<String, dynamic>?,
    context: json["context"] as Map<String, dynamic>?,
    status: json["status"] as String?,
    authorizedAt: json["authorized_at"] == null ? null : DateTime.parse(json["authorized_at"] as String),
    paymentCollection: json["payment_collection"] as Map<String, dynamic>?,
    payment: json["payment"] == null ? null : BasePayment.fromJson(json["payment"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (amount != null) "amount": amount!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (providerId != null) "provider_id": providerId!,
    if (data != null) "data": data!,
    if (context != null) "context": context!,
    if (status != null) "status": status!,
    if (authorizedAt != null) "authorized_at": authorizedAt!.toIso8601String(),
    if (paymentCollection != null) "payment_collection": paymentCollection!,
    if (payment != null) "payment": payment!.toJson(),
  };
}

/// The product's details.
class BaseProduct {
  BaseProduct({
    this.collection,
    this.categories,
    this.variants,
    this.type,
    this.tags,
    this.length,
    this.title,
    this.status,
    this.options,
    this.description,
    this.id,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.handle,
    this.subtitle,
    this.isGiftcard,
    this.thumbnail,
    this.width,
    this.weight,
    this.height,
    this.originCountry,
    this.hsCode,
    this.midCode,
    this.material,
    this.collectionId,
    this.typeId,
    this.images,
    this.discountable,
    this.externalId,
    this.deletedAt,
  });

  final BaseCollection? collection;
  /// The product's categories.
  final List<BaseProductCategory>? categories;
  /// The product's variants.
  final List<Map<String, dynamic>>? variants;
  final BaseProductType? type;
  /// The product's tags.
  final List<BaseProductTag>? tags;
  /// The product's length.
  final num? length;
  /// The product's title.
  final String? title;
  /// The product's status.
  final String? status;
  /// The product's options.
  final List<BaseProductOption>? options;
  /// The product's description.
  final String? description;
  /// The product's ID.
  final String? id;
  /// The product's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the product was created.
  final DateTime? createdAt;
  /// The date the product was updated.
  final DateTime? updatedAt;
  /// The product's unique handle.
  final String? handle;
  /// The product's subtitle.
  final String? subtitle;
  /// Whether the product is a gift card.
  final bool? isGiftcard;
  /// The product's thumbnail.
  final String? thumbnail;
  /// The product's width.
  final num? width;
  /// The product's weight.
  final num? weight;
  /// The product's height.
  final num? height;
  /// The product's origin country.
  final String? originCountry;
  /// The product's HS code.
  final String? hsCode;
  /// The product's MID code.
  final String? midCode;
  /// The product's material.
  final String? material;
  /// The ID of the collection that the product belongs to.
  final String? collectionId;
  /// The ID of the product's type.
  final String? typeId;
  /// The product's images.
  final List<BaseProductImage>? images;
  /// Whether discounts can be applied on the product.
  final bool? discountable;
  /// The ID of a product in an external system, such as an ERP or CMS.
  final String? externalId;
  /// The date the product was deleted.
  final DateTime? deletedAt;

  factory BaseProduct.fromJson(Map<String, dynamic> json) => BaseProduct(
    collection: json["collection"] == null ? null : BaseCollection.fromJson(json["collection"] as Map<String, dynamic>),
    categories: (json["categories"] as List?)?.map((e) => e == null ? null : BaseProductCategory.fromJson(e as Map<String, dynamic>)).toList().cast<BaseProductCategory>(),
    variants: (json["variants"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    type: json["type"] == null ? null : BaseProductType.fromJson(json["type"] as Map<String, dynamic>),
    tags: (json["tags"] as List?)?.map((e) => e == null ? null : BaseProductTag.fromJson(e as Map<String, dynamic>)).toList().cast<BaseProductTag>(),
    length: json["length"] as num?,
    title: json["title"] as String?,
    status: json["status"] as String?,
    options: (json["options"] as List?)?.map((e) => e == null ? null : BaseProductOption.fromJson(e as Map<String, dynamic>)).toList().cast<BaseProductOption>(),
    description: json["description"] as String?,
    id: json["id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    handle: json["handle"] as String?,
    subtitle: json["subtitle"] as String?,
    isGiftcard: json["is_giftcard"] as bool?,
    thumbnail: json["thumbnail"] as String?,
    width: json["width"] as num?,
    weight: json["weight"] as num?,
    height: json["height"] as num?,
    originCountry: json["origin_country"] as String?,
    hsCode: json["hs_code"] as String?,
    midCode: json["mid_code"] as String?,
    material: json["material"] as String?,
    collectionId: json["collection_id"] as String?,
    typeId: json["type_id"] as String?,
    images: (json["images"] as List?)?.map((e) => e == null ? null : BaseProductImage.fromJson(e as Map<String, dynamic>)).toList().cast<BaseProductImage>(),
    discountable: json["discountable"] as bool?,
    externalId: json["external_id"] as String?,
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (collection != null) "collection": collection!.toJson(),
    if (categories != null) "categories": categories!.map((e) => e.toJson()).toList(),
    if (variants != null) "variants": variants!.map((e) => e).toList(),
    if (type != null) "type": type!.toJson(),
    if (tags != null) "tags": tags!.map((e) => e.toJson()).toList(),
    if (length != null) "length": length!,
    if (title != null) "title": title!,
    if (status != null) "status": status!,
    if (options != null) "options": options!.map((e) => e.toJson()).toList(),
    if (description != null) "description": description!,
    if (id != null) "id": id!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (handle != null) "handle": handle!,
    if (subtitle != null) "subtitle": subtitle!,
    if (isGiftcard != null) "is_giftcard": isGiftcard!,
    if (thumbnail != null) "thumbnail": thumbnail!,
    if (width != null) "width": width!,
    if (weight != null) "weight": weight!,
    if (height != null) "height": height!,
    if (originCountry != null) "origin_country": originCountry!,
    if (hsCode != null) "hs_code": hsCode!,
    if (midCode != null) "mid_code": midCode!,
    if (material != null) "material": material!,
    if (collectionId != null) "collection_id": collectionId!,
    if (typeId != null) "type_id": typeId!,
    if (images != null) "images": images!.map((e) => e.toJson()).toList(),
    if (discountable != null) "discountable": discountable!,
    if (externalId != null) "external_id": externalId!,
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
  };
}

/// The product category's details.
class BaseProductCategory {
  BaseProductCategory({
    this.categoryChildren,
    this.parentCategory,
    this.products,
    this.name,
    this.description,
    this.id,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.handle,
    this.deletedAt,
    this.isActive,
    this.isInternal,
    this.rank,
    this.parentCategoryId,
  });

  /// The category's children.
  final List<AdminProductCategory>? categoryChildren;
  final AdminProductCategory? parentCategory;
  /// The category's products.
  final List<Map<String, dynamic>>? products;
  /// The category's name.
  final String? name;
  /// The category's description.
  final String? description;
  /// The category's ID.
  final String? id;
  /// The category's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the category was created.
  final DateTime? createdAt;
  /// The date the category was updated.
  final DateTime? updatedAt;
  /// The category's unique handle.
  final String? handle;
  /// The date the category was deleted.
  final DateTime? deletedAt;
  /// Whether the category is active. If disabled, the category isn't shown in the storefront.
  final bool? isActive;
  /// Whether the category is internal. If enabled, the category is only seen by admin users.
  final bool? isInternal;
  /// The category's rank among sibling categories.
  final num? rank;
  /// The ID of the category's parent.
  final String? parentCategoryId;

  factory BaseProductCategory.fromJson(Map<String, dynamic> json) => BaseProductCategory(
    categoryChildren: (json["category_children"] as List?)?.map((e) => e == null ? null : AdminProductCategory.fromJson(e as Map<String, dynamic>)).toList().cast<AdminProductCategory>(),
    parentCategory: json["parent_category"] == null ? null : AdminProductCategory.fromJson(json["parent_category"] as Map<String, dynamic>),
    products: (json["products"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    name: json["name"] as String?,
    description: json["description"] as String?,
    id: json["id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    handle: json["handle"] as String?,
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    isActive: json["is_active"] as bool?,
    isInternal: json["is_internal"] as bool?,
    rank: json["rank"] as num?,
    parentCategoryId: json["parent_category_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (categoryChildren != null) "category_children": categoryChildren!.map((e) => e.toJson()).toList(),
    if (parentCategory != null) "parent_category": parentCategory!.toJson(),
    if (products != null) "products": products!.map((e) => e).toList(),
    if (name != null) "name": name!,
    if (description != null) "description": description!,
    if (id != null) "id": id!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (handle != null) "handle": handle!,
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (isActive != null) "is_active": isActive!,
    if (isInternal != null) "is_internal": isInternal!,
    if (rank != null) "rank": rank!,
    if (parentCategoryId != null) "parent_category_id": parentCategoryId!,
  };
}

/// The image's details.
class BaseProductImage {
  BaseProductImage({
    this.id,
    this.url,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.metadata,
    this.rank,
  });

  /// The image's ID.
  final String? id;
  /// The image's URL.
  final String? url;
  /// The date the image was created.
  final DateTime? createdAt;
  /// The date the image was updated.
  final DateTime? updatedAt;
  /// The date the image was deleted.
  final DateTime? deletedAt;
  /// The image's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The image's rank among its sibling images.
  final num? rank;

  factory BaseProductImage.fromJson(Map<String, dynamic> json) => BaseProductImage(
    id: json["id"] as String?,
    url: json["url"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    metadata: json["metadata"] as Map<String, dynamic>?,
    rank: json["rank"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (url != null) "url": url!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (metadata != null) "metadata": metadata!,
    if (rank != null) "rank": rank!,
  };
}

/// The product option's details.
class BaseProductOption {
  BaseProductOption({
    this.id,
    this.title,
    this.values,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.isExclusive,
    this.products,
  });

  /// The product option's ID.
  final String? id;
  /// The product option's title.
  final String? title;
  /// The product option's values.
  final List<BaseProductOptionValue>? values;
  /// The product option's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the product option was created.
  final DateTime? createdAt;
  /// The date the product option was updated.
  final DateTime? updatedAt;
  /// The date the product option was deleted.
  final DateTime? deletedAt;
  /// Whether the option is exclusive to a product.
  final bool? isExclusive;
  /// The products that use the option.
  final List<Map<String, dynamic>>? products;

  factory BaseProductOption.fromJson(Map<String, dynamic> json) => BaseProductOption(
    id: json["id"] as String?,
    title: json["title"] as String?,
    values: (json["values"] as List?)?.map((e) => e == null ? null : BaseProductOptionValue.fromJson(e as Map<String, dynamic>)).toList().cast<BaseProductOptionValue>(),
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    isExclusive: json["is_exclusive"] as bool?,
    products: (json["products"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (title != null) "title": title!,
    if (values != null) "values": values!.map((e) => e.toJson()).toList(),
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (isExclusive != null) "is_exclusive": isExclusive!,
    if (products != null) "products": products!.map((e) => e).toList(),
  };
}

/// The product option value's details.
class BaseProductOptionValue {
  BaseProductOptionValue({
    this.id,
    this.value,
    this.option,
    this.optionId,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.rank,
  });

  /// The value's ID.
  final String? id;
  /// The value.
  final String? value;
  final Map<String, dynamic>? option;
  /// The ID of the option this value belongs to.
  final String? optionId;
  /// The value's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the value was created.
  final DateTime? createdAt;
  /// The date the value was updated.
  final DateTime? updatedAt;
  /// The date the value was deleted.
  final DateTime? deletedAt;
  /// The rank of the value among other option values.
  final num? rank;

  factory BaseProductOptionValue.fromJson(Map<String, dynamic> json) => BaseProductOptionValue(
    id: json["id"] as String?,
    value: json["value"] as String?,
    option: json["option"] as Map<String, dynamic>?,
    optionId: json["option_id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    rank: json["rank"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (value != null) "value": value!,
    if (option != null) "option": option!,
    if (optionId != null) "option_id": optionId!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (rank != null) "rank": rank!,
  };
}

/// The product tag's details.
class BaseProductTag {
  BaseProductTag({
    this.id,
    this.value,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.metadata,
    this.externalId,
  });

  /// The tag's ID.
  final String? id;
  /// The tag's value.
  final String? value;
  /// The date the tag was created.
  final DateTime? createdAt;
  /// The date the tag was updated.
  final DateTime? updatedAt;
  /// The date the tag was deleted.
  final DateTime? deletedAt;
  /// The tag's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The tag's external ID used to link it with external systems.
  final String? externalId;

  factory BaseProductTag.fromJson(Map<String, dynamic> json) => BaseProductTag(
    id: json["id"] as String?,
    value: json["value"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    metadata: json["metadata"] as Map<String, dynamic>?,
    externalId: json["external_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (value != null) "value": value!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (metadata != null) "metadata": metadata!,
    if (externalId != null) "external_id": externalId!,
  };
}

/// The product type's details.
class BaseProductType {
  BaseProductType({
    this.id,
    this.value,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.metadata,
    this.externalId,
  });

  /// The type's ID.
  final String? id;
  /// The type's value.
  final String? value;
  /// The date the type was created.
  final DateTime? createdAt;
  /// The date the type was updated.
  final DateTime? updatedAt;
  /// The date the type was deleted.
  final DateTime? deletedAt;
  /// The type's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The type's external ID used to link it with external systems.
  final String? externalId;

  factory BaseProductType.fromJson(Map<String, dynamic> json) => BaseProductType(
    id: json["id"] as String?,
    value: json["value"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    metadata: json["metadata"] as Map<String, dynamic>?,
    externalId: json["external_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (value != null) "value": value!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (metadata != null) "metadata": metadata!,
    if (externalId != null) "external_id": externalId!,
  };
}

/// The product variant's details.
class BaseProductVariant {
  BaseProductVariant({
    this.id,
    this.title,
    this.sku,
    this.barcode,
    this.ean,
    this.upc,
    this.allowBackorder,
    this.manageInventory,
    this.inventoryQuantity,
    this.hsCode,
    this.originCountry,
    this.midCode,
    this.material,
    this.weight,
    this.length,
    this.height,
    this.width,
    this.variantRank,
    this.options,
    this.product,
    this.productId,
    this.calculatedPrice,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.metadata,
    this.thumbnail,
    this.images,
  });

  /// The variant's ID.
  final String? id;
  /// The variant's title.
  final String? title;
  /// The variant's SKU.
  final String? sku;
  /// The variant's barcode.
  final String? barcode;
  /// The variant's EAN code.
  final String? ean;
  /// The variant's UPC.
  final String? upc;
  /// Whether the variant can be ordered even if it's out of stock.
  final bool? allowBackorder;
  /// Whether the Medusa application manages the variant's inventory quantity and availablility. If disabled, the variant is always considered in stock.
  final bool? manageInventory;
  /// The variant's inventory quantity. This is only included if you pass in the `fields` query parameter a `+variants.inventory_quantity` parameter.
  final num? inventoryQuantity;
  /// The variant's HS code.
  final String? hsCode;
  /// The variant's origin country.
  final String? originCountry;
  /// The variant's MID code.
  final String? midCode;
  /// The variant's material.
  final String? material;
  /// The variant's weight.
  final num? weight;
  /// The variant's length.
  final num? length;
  /// The variant's height.
  final num? height;
  /// The variant's width.
  final num? width;
  /// The variant's rank among its sibling variants.
  final num? variantRank;
  /// The variant's option values.
  final List<BaseProductOptionValue>? options;
  final BaseProduct? product;
  /// The ID of the product that the variant belongs to.
  final String? productId;
  final BaseCalculatedPriceSet? calculatedPrice;
  /// The date the variant was created.
  final DateTime? createdAt;
  /// The date the variant was updated.
  final DateTime? updatedAt;
  /// The date the variant was deleted.
  final DateTime? deletedAt;
  /// The variant's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The variant's thumbnail.
  final String? thumbnail;
  /// The variant's images.
  final List<BaseProductImage>? images;

  factory BaseProductVariant.fromJson(Map<String, dynamic> json) => BaseProductVariant(
    id: json["id"] as String?,
    title: json["title"] as String?,
    sku: json["sku"] as String?,
    barcode: json["barcode"] as String?,
    ean: json["ean"] as String?,
    upc: json["upc"] as String?,
    allowBackorder: json["allow_backorder"] as bool?,
    manageInventory: json["manage_inventory"] as bool?,
    inventoryQuantity: json["inventory_quantity"] as num?,
    hsCode: json["hs_code"] as String?,
    originCountry: json["origin_country"] as String?,
    midCode: json["mid_code"] as String?,
    material: json["material"] as String?,
    weight: json["weight"] as num?,
    length: json["length"] as num?,
    height: json["height"] as num?,
    width: json["width"] as num?,
    variantRank: json["variant_rank"] as num?,
    options: (json["options"] as List?)?.map((e) => e == null ? null : BaseProductOptionValue.fromJson(e as Map<String, dynamic>)).toList().cast<BaseProductOptionValue>(),
    product: json["product"] == null ? null : BaseProduct.fromJson(json["product"] as Map<String, dynamic>),
    productId: json["product_id"] as String?,
    calculatedPrice: json["calculated_price"] == null ? null : BaseCalculatedPriceSet.fromJson(json["calculated_price"] as Map<String, dynamic>),
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    metadata: json["metadata"] as Map<String, dynamic>?,
    thumbnail: json["thumbnail"] as String?,
    images: (json["images"] as List?)?.map((e) => e == null ? null : BaseProductImage.fromJson(e as Map<String, dynamic>)).toList().cast<BaseProductImage>(),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (title != null) "title": title!,
    if (sku != null) "sku": sku!,
    if (barcode != null) "barcode": barcode!,
    if (ean != null) "ean": ean!,
    if (upc != null) "upc": upc!,
    if (allowBackorder != null) "allow_backorder": allowBackorder!,
    if (manageInventory != null) "manage_inventory": manageInventory!,
    if (inventoryQuantity != null) "inventory_quantity": inventoryQuantity!,
    if (hsCode != null) "hs_code": hsCode!,
    if (originCountry != null) "origin_country": originCountry!,
    if (midCode != null) "mid_code": midCode!,
    if (material != null) "material": material!,
    if (weight != null) "weight": weight!,
    if (length != null) "length": length!,
    if (height != null) "height": height!,
    if (width != null) "width": width!,
    if (variantRank != null) "variant_rank": variantRank!,
    if (options != null) "options": options!.map((e) => e.toJson()).toList(),
    if (product != null) "product": product!.toJson(),
    if (productId != null) "product_id": productId!,
    if (calculatedPrice != null) "calculated_price": calculatedPrice!.toJson(),
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (metadata != null) "metadata": metadata!,
    if (thumbnail != null) "thumbnail": thumbnail!,
    if (images != null) "images": images!.map((e) => e.toJson()).toList(),
  };
}

/// The rule value's details.
class BasePromotionRuleValue {
  BasePromotionRuleValue({
    this.id,
    this.value,
  });

  /// The value's ID.
  final String? id;
  /// The value.
  final String? value;

  factory BasePromotionRuleValue.fromJson(Map<String, dynamic> json) => BasePromotionRuleValue(
    id: json["id"] as String?,
    value: json["value"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (value != null) "value": value!,
  };
}

/// The refund's details.
class BaseRefund {
  BaseRefund({
    this.id,
    this.amount,
    this.refundReasonId,
    this.note,
    this.createdAt,
    this.createdBy,
    this.payment,
    this.refundReason,
  });

  /// The refund's ID.
  final String? id;
  /// The refund's amount.
  final num? amount;
  /// The ID of the refund reason.
  final String? refundReasonId;
  /// More details about the refund.
  final String? note;
  /// The date the refund was created.
  final DateTime? createdAt;
  /// The ID of the user that created the refund.
  final String? createdBy;
  final Map<String, dynamic>? payment;
  final BaseRefundReason? refundReason;

  factory BaseRefund.fromJson(Map<String, dynamic> json) => BaseRefund(
    id: json["id"] as String?,
    amount: json["amount"] as num?,
    refundReasonId: json["refund_reason_id"] as String?,
    note: json["note"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    createdBy: json["created_by"] as String?,
    payment: json["payment"] as Map<String, dynamic>?,
    refundReason: json["refund_reason"] == null ? null : BaseRefundReason.fromJson(json["refund_reason"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (amount != null) "amount": amount!,
    if (refundReasonId != null) "refund_reason_id": refundReasonId!,
    if (note != null) "note": note!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (createdBy != null) "created_by": createdBy!,
    if (payment != null) "payment": payment!,
    if (refundReason != null) "refund_reason": refundReason!.toJson(),
  };
}

/// The refund reason's details.
class BaseRefundReason {
  BaseRefundReason({
    this.id,
    this.label,
    this.description,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.code,
  });

  /// The refund reason's ID.
  final String? id;
  /// The refund reason's label.
  final String? label;
  /// The refund reason's description.
  final String? description;
  /// The refund reason's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the refund reason was created.
  final DateTime? createdAt;
  /// The date the refund reason was updated.
  final DateTime? updatedAt;
  /// The refund reason's code.
  final String? code;

  factory BaseRefundReason.fromJson(Map<String, dynamic> json) => BaseRefundReason(
    id: json["id"] as String?,
    label: json["label"] as String?,
    description: json["description"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    code: json["code"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (label != null) "label": label!,
    if (description != null) "description": description!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (code != null) "code": code!,
  };
}

/// The region's details.
class BaseRegion {
  BaseRegion({
    this.id,
    this.name,
    this.currencyCode,
    this.automaticTaxes,
    this.countries,
    this.paymentProviders,
    this.metadata,
    this.createdAt,
    this.updatedAt,
  });

  /// The region's ID.
  final String? id;
  /// The region's name.
  final String? name;
  /// The region's currency code.
  final String? currencyCode;
  /// The region's automatic taxes.
  final bool? automaticTaxes;
  /// The region's countries.
  final List<BaseRegionCountry>? countries;
  /// The region's payment providers.
  final List<AdminPaymentProvider>? paymentProviders;
  /// The region's metadata.
  final Map<String, dynamic>? metadata;
  /// The region's created at.
  final DateTime? createdAt;
  /// The region's updated at.
  final DateTime? updatedAt;

  factory BaseRegion.fromJson(Map<String, dynamic> json) => BaseRegion(
    id: json["id"] as String?,
    name: json["name"] as String?,
    currencyCode: json["currency_code"] as String?,
    automaticTaxes: json["automatic_taxes"] as bool?,
    countries: (json["countries"] as List?)?.map((e) => e == null ? null : BaseRegionCountry.fromJson(e as Map<String, dynamic>)).toList().cast<BaseRegionCountry>(),
    paymentProviders: (json["payment_providers"] as List?)?.map((e) => e == null ? null : AdminPaymentProvider.fromJson(e as Map<String, dynamic>)).toList().cast<AdminPaymentProvider>(),
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (name != null) "name": name!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (automaticTaxes != null) "automatic_taxes": automaticTaxes!,
    if (countries != null) "countries": countries!.map((e) => e.toJson()).toList(),
    if (paymentProviders != null) "payment_providers": paymentProviders!.map((e) => e.toJson()).toList(),
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The country's details.
class BaseRegionCountry {
  BaseRegionCountry({
    this.id,
    this.iso2,
    this.iso3,
    this.numCode,
    this.name,
    this.displayName,
  });

  /// The country's ID.
  final String? id;
  /// The country's iso 2.
  final String? iso2;
  /// The country's iso 3.
  final String? iso3;
  /// The country's num code.
  final String? numCode;
  /// The country's name.
  final String? name;
  /// The country's display name.
  final String? displayName;

  factory BaseRegionCountry.fromJson(Map<String, dynamic> json) => BaseRegionCountry(
    id: json["id"] as String?,
    iso2: json["iso_2"] as String?,
    iso3: json["iso_3"] as String?,
    numCode: json["num_code"] as String?,
    name: json["name"] as String?,
    displayName: json["display_name"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (iso2 != null) "iso_2": iso2!,
    if (iso3 != null) "iso_3": iso3!,
    if (numCode != null) "num_code": numCode!,
    if (name != null) "name": name!,
    if (displayName != null) "display_name": displayName!,
  };
}

/// The operator's details.
class BaseRuleOperatorOptions {
  BaseRuleOperatorOptions({
    this.id,
    this.value,
    this.label,
  });

  /// The operator's ID.
  final String? id;
  /// The operator's value.
  final String? value;
  /// The operator's label.
  final String? label;

  factory BaseRuleOperatorOptions.fromJson(Map<String, dynamic> json) => BaseRuleOperatorOptions(
    id: json["id"] as String?,
    value: json["value"] as String?,
    label: json["label"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (value != null) "value": value!,
    if (label != null) "label": label!,
  };
}

/// The adjustment's details.
class BaseShippingMethodAdjustment {
  BaseShippingMethodAdjustment({
    this.shippingMethod,
    this.shippingMethodId,
    this.id,
    this.code,
    this.amount,
    this.cartId,
    this.description,
    this.promotionId,
    this.providerId,
    this.createdAt,
    this.updatedAt,
  });

  final Map<String, dynamic>? shippingMethod;
  /// The ID of the shipping method this adjustment applies on.
  final String? shippingMethodId;
  /// The adjustment's ID.
  final String? id;
  /// The adjustment's code.
  final String? code;
  /// The adjustment's amount.
  final num? amount;
  /// The ID of the associated cart.
  final String? cartId;
  /// The adjustment's description.
  final String? description;
  /// The ID of the applied promotion.
  final String? promotionId;
  /// The adjustment's provider ID.
  final String? providerId;
  /// The date the adjustment was created.
  final DateTime? createdAt;
  /// The date the adjustment was updated.
  final DateTime? updatedAt;

  factory BaseShippingMethodAdjustment.fromJson(Map<String, dynamic> json) => BaseShippingMethodAdjustment(
    shippingMethod: json["shipping_method"] as Map<String, dynamic>?,
    shippingMethodId: json["shipping_method_id"] as String?,
    id: json["id"] as String?,
    code: json["code"] as String?,
    amount: json["amount"] as num?,
    cartId: json["cart_id"] as String?,
    description: json["description"] as String?,
    promotionId: json["promotion_id"] as String?,
    providerId: json["provider_id"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (shippingMethod != null) "shipping_method": shippingMethod!,
    if (shippingMethodId != null) "shipping_method_id": shippingMethodId!,
    if (id != null) "id": id!,
    if (code != null) "code": code!,
    if (amount != null) "amount": amount!,
    if (cartId != null) "cart_id": cartId!,
    if (description != null) "description": description!,
    if (promotionId != null) "promotion_id": promotionId!,
    if (providerId != null) "provider_id": providerId!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The tax line's details
class BaseShippingMethodTaxLine {
  BaseShippingMethodTaxLine({
    this.shippingMethod,
    this.shippingMethodId,
    this.total,
    this.subtotal,
    this.id,
    this.description,
    this.taxRateId,
    this.code,
    this.rate,
    this.providerId,
    this.createdAt,
    this.updatedAt,
  });

  final Map<String, dynamic>? shippingMethod;
  /// The ID of the shipping method this tax line belongs to.
  final String? shippingMethodId;
  /// The method's total including taxes and promotions.
  final num? total;
  /// The method's total excluding taxes, including promotions.
  final num? subtotal;
  /// The tax line's ID.
  final String? id;
  /// The tax line's description.
  final String? description;
  /// The ID of the applied tax rate.
  final String? taxRateId;
  /// The code that the tax rate is identified by.
  final String? code;
  /// The charged rate.
  final num? rate;
  /// The ID of the tax provider used to calculate the tax line.
  final String? providerId;
  /// The date the tax line was created.
  final DateTime? createdAt;
  /// The date the tax line was updated.
  final DateTime? updatedAt;

  factory BaseShippingMethodTaxLine.fromJson(Map<String, dynamic> json) => BaseShippingMethodTaxLine(
    shippingMethod: json["shipping_method"] as Map<String, dynamic>?,
    shippingMethodId: json["shipping_method_id"] as String?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    id: json["id"] as String?,
    description: json["description"] as String?,
    taxRateId: json["tax_rate_id"] as String?,
    code: json["code"] as String?,
    rate: json["rate"] as num?,
    providerId: json["provider_id"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (shippingMethod != null) "shipping_method": shippingMethod!,
    if (shippingMethodId != null) "shipping_method_id": shippingMethodId!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (id != null) "id": id!,
    if (description != null) "description": description!,
    if (taxRateId != null) "tax_rate_id": taxRateId!,
    if (code != null) "code": code!,
    if (rate != null) "rate": rate!,
    if (providerId != null) "provider_id": providerId!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The campaign's details.
class CampaignResponse {
  CampaignResponse({
    this.id,
    this.name,
    this.description,
    this.currency,
    this.campaignIdentifier,
    this.startsAt,
    this.endsAt,
    this.budget,
  });

  /// The campaign's ID.
  final String? id;
  /// The campaign's name.
  final String? name;
  /// The campaign's description.
  final String? description;
  /// The campaign's currency.
  final String? currency;
  /// The campaign's campaign identifier.
  final String? campaignIdentifier;
  /// The campaign's starts at.
  final String? startsAt;
  /// The campaign's ends at.
  final String? endsAt;
  /// The campaign's budget.
  final Map<String, dynamic>? budget;

  factory CampaignResponse.fromJson(Map<String, dynamic> json) => CampaignResponse(
    id: json["id"] as String?,
    name: json["name"] as String?,
    description: json["description"] as String?,
    currency: json["currency"] as String?,
    campaignIdentifier: json["campaign_identifier"] as String?,
    startsAt: json["starts_at"] as String?,
    endsAt: json["ends_at"] as String?,
    budget: json["budget"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (name != null) "name": name!,
    if (description != null) "description": description!,
    if (currency != null) "currency": currency!,
    if (campaignIdentifier != null) "campaign_identifier": campaignIdentifier!,
    if (startsAt != null) "starts_at": startsAt!,
    if (endsAt != null) "ends_at": endsAt!,
    if (budget != null) "budget": budget!,
  };
}

/// The address to create.
class CreateAddress {
  CreateAddress({
    this.customerId,
    this.company,
    this.firstName,
    this.lastName,
    this.address1,
    this.address2,
    this.city,
    this.countryCode,
    this.province,
    this.postalCode,
    this.phone,
    this.metadata,
  });

  /// The ID of the customer this address belongs to.
  final String? customerId;
  /// The address's company.
  final String? company;
  /// The address's first name.
  final String? firstName;
  /// The address's last name.
  final String? lastName;
  /// The address's first line.
  final String? address1;
  /// The address's second line.
  final String? address2;
  /// The address's city.
  final String? city;
  /// The address's country code.
  final String? countryCode;
  /// The address's ISO 3166-2 province code. Must be lower-case.
  final String? province;
  /// The address's postal code.
  final String? postalCode;
  /// The address's phone.
  final String? phone;
  /// The address's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory CreateAddress.fromJson(Map<String, dynamic> json) => CreateAddress(
    customerId: json["customer_id"] as String?,
    company: json["company"] as String?,
    firstName: json["first_name"] as String?,
    lastName: json["last_name"] as String?,
    address1: json["address_1"] as String?,
    address2: json["address_2"] as String?,
    city: json["city"] as String?,
    countryCode: json["country_code"] as String?,
    province: json["province"] as String?,
    postalCode: json["postal_code"] as String?,
    phone: json["phone"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (customerId != null) "customer_id": customerId!,
    if (company != null) "company": company!,
    if (firstName != null) "first_name": firstName!,
    if (lastName != null) "last_name": lastName!,
    if (address1 != null) "address_1": address1!,
    if (address2 != null) "address_2": address2!,
    if (city != null) "city": city!,
    if (countryCode != null) "country_code": countryCode!,
    if (province != null) "province": province!,
    if (postalCode != null) "postal_code": postalCode!,
    if (phone != null) "phone": phone!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// Filter by customer groups to get their associated customers.
class CustomerGroupInCustomerFilters {
  CustomerGroupInCustomerFilters({
    this.id,
    this.name,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
  });

  final dynamic id;
  final dynamic name;
  /// Filter by the group's creation date.
  final Map<String, dynamic>? createdAt;
  /// Filter by the group's update date.
  final Map<String, dynamic>? updatedAt;
  /// Filter by the group's deletion date.
  final Map<String, dynamic>? deletedAt;

  factory CustomerGroupInCustomerFilters.fromJson(Map<String, dynamic> json) => CustomerGroupInCustomerFilters(
    id: json["id"],
    name: json["name"],
    createdAt: json["created_at"] as Map<String, dynamic>?,
    updatedAt: json["updated_at"] as Map<String, dynamic>?,
    deletedAt: json["deleted_at"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (name != null) "name": name!,
    if (createdAt != null) "created_at": createdAt!,
    if (updatedAt != null) "updated_at": updatedAt!,
    if (deletedAt != null) "deleted_at": deletedAt!,
  };
}

class Error {
  Error({
    this.code,
    this.message,
    this.type,
  });

  /// A slug code to indicate the type of the error.
  final String? code;
  /// Description of the error that occurred.
  final String? message;
  /// A slug indicating the type of the error.
  final String? type;

  factory Error.fromJson(Map<String, dynamic> json) => Error(
    code: json["code"] as String?,
    message: json["message"] as String?,
    type: json["type"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (code != null) "code": code!,
    if (message != null) "message": message!,
    if (type != null) "type": type!,
  };
}
