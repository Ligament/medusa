// GENERATED by tool/generate_models.mjs — do not edit.
part of '../models.dart';

/// The product's details.
class AdminUpdateProduct {
  AdminUpdateProduct({
    this.title,
    this.subtitle,
    this.description,
    this.isGiftcard,
    this.discountable,
    this.images,
    this.thumbnail,
    this.handle,
    this.status,
    this.typeId,
    this.collectionId,
    this.categories,
    this.tags,
    this.options,
    this.variants,
    this.salesChannels,
    this.weight,
    this.length,
    this.height,
    this.width,
    this.hsCode,
    this.midCode,
    this.originCountry,
    this.material,
    this.metadata,
    this.externalId,
  });

  /// The product's title.
  final String? title;
  /// The product's subtitle.
  final String? subtitle;
  /// The product's description.
  final String? description;
  /// Whether the product is a gift card.
  final bool? isGiftcard;
  /// Whether discounts can be applied on the product.
  final bool? discountable;
  /// The product's images.
  final List<Map<String, dynamic>>? images;
  /// The product's thumbnail URL.
  final String? thumbnail;
  /// The product's unique handle.
  final String? handle;
  /// The product's status.
  final String? status;
  /// The ID of the product's type.
  final String? typeId;
  /// The ID of the product's collection.
  final String? collectionId;
  /// The product's categories.
  final List<Map<String, dynamic>>? categories;
  /// The product's tags.
  final List<Map<String, dynamic>>? tags;
  /// The product's options.
  final List<AdminUpdateProductOption>? options;
  /// The product's variants. You can add new variants or update existing ones, passing their IDs in the object.
  final List<dynamic>? variants;
  /// The sales channels that the product is available in.
  final List<Map<String, dynamic>>? salesChannels;
  /// The product's weight.
  final num? weight;
  /// The product's length.
  final num? length;
  /// The product's height.
  final num? height;
  /// The product's width.
  final num? width;
  /// The product's HS code.
  final String? hsCode;
  /// The product's MID code.
  final String? midCode;
  /// The product's origin country.
  final String? originCountry;
  /// The product's material.
  final String? material;
  /// The product's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The ID of the product in an external or third-party system.
  final String? externalId;

  factory AdminUpdateProduct.fromJson(Map<String, dynamic> json) => AdminUpdateProduct(
    title: json["title"] as String?,
    subtitle: json["subtitle"] as String?,
    description: json["description"] as String?,
    isGiftcard: json["is_giftcard"] as bool?,
    discountable: json["discountable"] as bool?,
    images: (json["images"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    thumbnail: json["thumbnail"] as String?,
    handle: json["handle"] as String?,
    status: json["status"] as String?,
    typeId: json["type_id"] as String?,
    collectionId: json["collection_id"] as String?,
    categories: (json["categories"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    tags: (json["tags"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    options: (json["options"] as List?)?.map((e) => e == null ? null : AdminUpdateProductOption.fromJson(e as Map<String, dynamic>)).toList().cast<AdminUpdateProductOption>(),
    variants: (json["variants"] as List?)?.map((e) => e).toList().cast<dynamic>(),
    salesChannels: (json["sales_channels"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    weight: json["weight"] as num?,
    length: json["length"] as num?,
    height: json["height"] as num?,
    width: json["width"] as num?,
    hsCode: json["hs_code"] as String?,
    midCode: json["mid_code"] as String?,
    originCountry: json["origin_country"] as String?,
    material: json["material"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    externalId: json["external_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (title != null) "title": title!,
    if (subtitle != null) "subtitle": subtitle!,
    if (description != null) "description": description!,
    if (isGiftcard != null) "is_giftcard": isGiftcard!,
    if (discountable != null) "discountable": discountable!,
    if (images != null) "images": images!.map((e) => e).toList(),
    if (thumbnail != null) "thumbnail": thumbnail!,
    if (handle != null) "handle": handle!,
    if (status != null) "status": status!,
    if (typeId != null) "type_id": typeId!,
    if (collectionId != null) "collection_id": collectionId!,
    if (categories != null) "categories": categories!.map((e) => e).toList(),
    if (tags != null) "tags": tags!.map((e) => e).toList(),
    if (options != null) "options": options!.map((e) => e.toJson()).toList(),
    if (variants != null) "variants": variants!.map((e) => e).toList(),
    if (salesChannels != null) "sales_channels": salesChannels!.map((e) => e).toList(),
    if (weight != null) "weight": weight!,
    if (length != null) "length": length!,
    if (height != null) "height": height!,
    if (width != null) "width": width!,
    if (hsCode != null) "hs_code": hsCode!,
    if (midCode != null) "mid_code": midCode!,
    if (originCountry != null) "origin_country": originCountry!,
    if (material != null) "material": material!,
    if (metadata != null) "metadata": metadata!,
    if (externalId != null) "external_id": externalId!,
  };
}

/// The properties to update in the product category.
class AdminUpdateProductCategory {
  AdminUpdateProductCategory({
    this.name,
    this.description,
    this.handle,
    this.isInternal,
    this.isActive,
    this.parentCategoryId,
    this.metadata,
    this.rank,
    this.externalId,
  });

  /// The product category's name.
  final String? name;
  /// The product category's description.
  final String? description;
  /// The product category's handle. Must be a unique value.
  final String? handle;
  /// Whether the product category is only used for internal purposes and shouldn't be shown the customer.
  final bool? isInternal;
  /// Whether the product category is active.
  final bool? isActive;
  /// The ID of a parent category.
  final String? parentCategoryId;
  /// The product category's metadata. Can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The product category's rank among other categories.
  final num? rank;
  /// The product category's external ID used to link it with external systems.
  final String? externalId;

  factory AdminUpdateProductCategory.fromJson(Map<String, dynamic> json) => AdminUpdateProductCategory(
    name: json["name"] as String?,
    description: json["description"] as String?,
    handle: json["handle"] as String?,
    isInternal: json["is_internal"] as bool?,
    isActive: json["is_active"] as bool?,
    parentCategoryId: json["parent_category_id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    rank: json["rank"] as num?,
    externalId: json["external_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (name != null) "name": name!,
    if (description != null) "description": description!,
    if (handle != null) "handle": handle!,
    if (isInternal != null) "is_internal": isInternal!,
    if (isActive != null) "is_active": isActive!,
    if (parentCategoryId != null) "parent_category_id": parentCategoryId!,
    if (metadata != null) "metadata": metadata!,
    if (rank != null) "rank": rank!,
    if (externalId != null) "external_id": externalId!,
  };
}

/// The details to update in a product option.
class AdminUpdateProductOption {
  AdminUpdateProductOption({
    this.title,
    this.values,
    this.id,
  });

  /// The option's title.
  final String? title;
  /// The option's values.
  final List<String>? values;
  /// The option's ID.
  final String? id;

  factory AdminUpdateProductOption.fromJson(Map<String, dynamic> json) => AdminUpdateProductOption(
    title: json["title"] as String?,
    values: (json["values"] as List?)?.map((e) => e as String?).toList().cast<String>(),
    id: json["id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (title != null) "title": title!,
    if (values != null) "values": values!.map((e) => e).toList(),
    if (id != null) "id": id!,
  };
}

/// The properties to update in the product tag.
class AdminUpdateProductTag {
  AdminUpdateProductTag({
    this.value,
    this.metadata,
    this.externalId,
  });

  /// The product tag's value.
  final String? value;
  /// The product tag's metadata. Can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The product tag's external ID used to link it with external systems.
  final String? externalId;

  factory AdminUpdateProductTag.fromJson(Map<String, dynamic> json) => AdminUpdateProductTag(
    value: json["value"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    externalId: json["external_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (value != null) "value": value!,
    if (metadata != null) "metadata": metadata!,
    if (externalId != null) "external_id": externalId!,
  };
}

/// The properties to update in the product type.
class AdminUpdateProductType {
  AdminUpdateProductType({
    this.value,
    this.metadata,
    this.externalId,
  });

  /// The product type's value.
  final String? value;
  /// The product type's metadata. Can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The product type's external ID used to link it with external systems.
  final String? externalId;

  factory AdminUpdateProductType.fromJson(Map<String, dynamic> json) => AdminUpdateProductType(
    value: json["value"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    externalId: json["external_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (value != null) "value": value!,
    if (metadata != null) "metadata": metadata!,
    if (externalId != null) "external_id": externalId!,
  };
}

/// The properties to update of a product variant.
class AdminUpdateProductVariant {
  AdminUpdateProductVariant({
    this.title,
    this.sku,
    this.ean,
    this.upc,
    this.barcode,
    this.hsCode,
    this.midCode,
    this.allowBackorder,
    this.manageInventory,
    this.variantRank,
    this.weight,
    this.length,
    this.height,
    this.width,
    this.originCountry,
    this.material,
    this.metadata,
    this.prices,
    this.options,
  });

  /// The product variant's title.
  final String? title;
  /// The product variant's SKU.
  final String? sku;
  /// The product variant's EAN.
  final String? ean;
  /// The product variant's UPC.
  final String? upc;
  /// The product variant's barcode.
  final String? barcode;
  /// The product variant's HS code.
  final String? hsCode;
  /// The product variant's MID code.
  final String? midCode;
  /// Whether customers can order the variant even if it's not in stock.
  final bool? allowBackorder;
  /// Whether the Medusa application manages the variant's inventory. If disabled, the product variant is always considered in stock.
  final bool? manageInventory;
  /// The product variant's rank among its siblings.
  final num? variantRank;
  /// The product variant's weight.
  final num? weight;
  /// The product variant's length.
  final num? length;
  /// The product variant's height.
  final num? height;
  /// The product variant's width.
  final num? width;
  /// The product variant's origin country.
  final String? originCountry;
  /// The product variant's material.
  final String? material;
  /// The product variant's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The product variant's prices.
  final List<AdminCreateProductVariantPrice>? prices;
  /// The product variant's options.
  final Map<String, dynamic>? options;

  factory AdminUpdateProductVariant.fromJson(Map<String, dynamic> json) => AdminUpdateProductVariant(
    title: json["title"] as String?,
    sku: json["sku"] as String?,
    ean: json["ean"] as String?,
    upc: json["upc"] as String?,
    barcode: json["barcode"] as String?,
    hsCode: json["hs_code"] as String?,
    midCode: json["mid_code"] as String?,
    allowBackorder: json["allow_backorder"] as bool?,
    manageInventory: json["manage_inventory"] as bool?,
    variantRank: json["variant_rank"] as num?,
    weight: json["weight"] as num?,
    length: json["length"] as num?,
    height: json["height"] as num?,
    width: json["width"] as num?,
    originCountry: json["origin_country"] as String?,
    material: json["material"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    prices: (json["prices"] as List?)?.map((e) => e == null ? null : AdminCreateProductVariantPrice.fromJson(e as Map<String, dynamic>)).toList().cast<AdminCreateProductVariantPrice>(),
    options: json["options"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (title != null) "title": title!,
    if (sku != null) "sku": sku!,
    if (ean != null) "ean": ean!,
    if (upc != null) "upc": upc!,
    if (barcode != null) "barcode": barcode!,
    if (hsCode != null) "hs_code": hsCode!,
    if (midCode != null) "mid_code": midCode!,
    if (allowBackorder != null) "allow_backorder": allowBackorder!,
    if (manageInventory != null) "manage_inventory": manageInventory!,
    if (variantRank != null) "variant_rank": variantRank!,
    if (weight != null) "weight": weight!,
    if (length != null) "length": length!,
    if (height != null) "height": height!,
    if (width != null) "width": width!,
    if (originCountry != null) "origin_country": originCountry!,
    if (material != null) "material": material!,
    if (metadata != null) "metadata": metadata!,
    if (prices != null) "prices": prices!.map((e) => e.toJson()).toList(),
    if (options != null) "options": options!,
  };
}

/// The details of a variant's price.
class AdminUpdateProductVariantPrice {
  AdminUpdateProductVariantPrice({
    this.currencyCode,
    this.amount,
    this.minQuantity,
    this.maxQuantity,
    this.rules,
    this.id,
  });

  /// The price's currency code.
  final String? currencyCode;
  /// The price's amount.
  final num? amount;
  /// The minimum quantity that must be available in the cart for this price to apply.
  final num? minQuantity;
  /// The maximum quantity that must not be surpassed in the cart for this price to apply.
  final num? maxQuantity;
  /// The price's rules.
  final Map<String, dynamic>? rules;
  /// The price's ID.
  final String? id;

  factory AdminUpdateProductVariantPrice.fromJson(Map<String, dynamic> json) => AdminUpdateProductVariantPrice(
    currencyCode: json["currency_code"] as String?,
    amount: json["amount"] as num?,
    minQuantity: json["min_quantity"] as num?,
    maxQuantity: json["max_quantity"] as num?,
    rules: json["rules"] as Map<String, dynamic>?,
    id: json["id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (currencyCode != null) "currency_code": currencyCode!,
    if (amount != null) "amount": amount!,
    if (minQuantity != null) "min_quantity": minQuantity!,
    if (maxQuantity != null) "max_quantity": maxQuantity!,
    if (rules != null) "rules": rules!,
    if (id != null) "id": id!,
  };
}

/// The properties to update in a buy rule.
class AdminUpdatePromotionRule {
  AdminUpdatePromotionRule({
    this.id,
    this.operator,
    this.description,
    this.attribute,
    this.values,
  });

  /// The buy rule's ID.
  final String? id;
  /// The operator used to check whether the buy rule applies on a cart. For example, `eq` means that the cart's value for the specified attribute must match the spec
  final String? operator;
  /// The buy rule's description.
  final String? description;
  /// The attribute to compare against when checking whether a promotion can be applied on a cart.
  final String? attribute;
  final dynamic values;

  factory AdminUpdatePromotionRule.fromJson(Map<String, dynamic> json) => AdminUpdatePromotionRule(
    id: json["id"] as String?,
    operator: json["operator"] as String?,
    description: json["description"] as String?,
    attribute: json["attribute"] as String?,
    values: json["values"],
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (operator != null) "operator": operator!,
    if (description != null) "description": description!,
    if (attribute != null) "attribute": attribute!,
    if (values != null) "values": values!,
  };
}

/// The details to update in a property label.
class AdminUpdatePropertyLabel {
  AdminUpdatePropertyLabel({
    this.label,
    this.description,
  });

  /// The label of the property.
  final String? label;
  /// The property label's description.
  final String? description;

  factory AdminUpdatePropertyLabel.fromJson(Map<String, dynamic> json) => AdminUpdatePropertyLabel(
    label: json["label"] as String?,
    description: json["description"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (label != null) "label": label!,
    if (description != null) "description": description!,
  };
}

/// The properties to update in the refund reason.
class AdminUpdateRefundReason {
  AdminUpdateRefundReason({
    this.label,
    this.description,
    this.code,
  });

  /// The refund reason's label.
  final String? label;
  /// The refund reason's description.
  final String? description;
  /// The refund reason's code.
  final String? code;

  factory AdminUpdateRefundReason.fromJson(Map<String, dynamic> json) => AdminUpdateRefundReason(
    label: json["label"] as String?,
    description: json["description"] as String?,
    code: json["code"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (label != null) "label": label!,
    if (description != null) "description": description!,
    if (code != null) "code": code!,
  };
}

/// The propeties to update in the region.
class AdminUpdateRegion {
  AdminUpdateRegion({
    this.name,
    this.currencyCode,
    this.countries,
    this.automaticTaxes,
    this.paymentProviders,
    this.metadata,
    this.isTaxInclusive,
  });

  /// The region's name.
  final String? name;
  /// The region's currency code.
  final String? currencyCode;
  /// The region's countries.
  final List<String>? countries;
  /// Whether taxes are calculated automatically for carts in the region.
  final bool? automaticTaxes;
  /// The payment providers enabled in the region.
  final List<String>? paymentProviders;
  /// The region's metadata. Can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// Whether the prices in the region are tax inclusive.
  final bool? isTaxInclusive;

  factory AdminUpdateRegion.fromJson(Map<String, dynamic> json) => AdminUpdateRegion(
    name: json["name"] as String?,
    currencyCode: json["currency_code"] as String?,
    countries: (json["countries"] as List?)?.map((e) => e as String?).toList().cast<String>(),
    automaticTaxes: json["automatic_taxes"] as bool?,
    paymentProviders: (json["payment_providers"] as List?)?.map((e) => e as String?).toList().cast<String>(),
    metadata: json["metadata"] as Map<String, dynamic>?,
    isTaxInclusive: json["is_tax_inclusive"] as bool?,
  );

  Map<String, dynamic> toJson() => {
    if (name != null) "name": name!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (countries != null) "countries": countries!.map((e) => e).toList(),
    if (automaticTaxes != null) "automatic_taxes": automaticTaxes!,
    if (paymentProviders != null) "payment_providers": paymentProviders!.map((e) => e).toList(),
    if (metadata != null) "metadata": metadata!,
    if (isTaxInclusive != null) "is_tax_inclusive": isTaxInclusive!,
  };
}

/// The properties to update in the reservation.
class AdminUpdateReservation {
  AdminUpdateReservation({
    this.locationId,
    this.quantity,
    this.description,
    this.metadata,
  });

  /// The ID of the associated location.
  final String? locationId;
  /// The reserved quantity.
  final num? quantity;
  /// The reservation's description.
  final String? description;
  /// The reservation's metadata. Can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory AdminUpdateReservation.fromJson(Map<String, dynamic> json) => AdminUpdateReservation(
    locationId: json["location_id"] as String?,
    quantity: json["quantity"] as num?,
    description: json["description"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (locationId != null) "location_id": locationId!,
    if (quantity != null) "quantity": quantity!,
    if (description != null) "description": description!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The details to update in the item.
class AdminUpdateReturnItems {
  AdminUpdateReturnItems({
    this.quantity,
    this.internalNote,
    this.reasonId,
    this.metadata,
  });

  /// The item's quantity.
  final num? quantity;
  /// A note viewed only by admin users.
  final String? internalNote;
  /// The ID of the associated return reason.
  final String? reasonId;
  /// The claim's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory AdminUpdateReturnItems.fromJson(Map<String, dynamic> json) => AdminUpdateReturnItems(
    quantity: json["quantity"] as num?,
    internalNote: json["internal_note"] as String?,
    reasonId: json["reason_id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (quantity != null) "quantity": quantity!,
    if (internalNote != null) "internal_note": internalNote!,
    if (reasonId != null) "reason_id": reasonId!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The details to update in a return reason.
class AdminUpdateReturnReason {
  AdminUpdateReturnReason({
    this.label,
    this.value,
    this.description,
    this.metadata,
  });

  /// The return reason's label.
  final String? label;
  /// The return reason's value.
  final String? value;
  /// The return reason's description.
  final String? description;
  /// The return reason's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory AdminUpdateReturnReason.fromJson(Map<String, dynamic> json) => AdminUpdateReturnReason(
    label: json["label"] as String?,
    value: json["value"] as String?,
    description: json["description"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (label != null) "label": label!,
    if (value != null) "value": value!,
    if (description != null) "description": description!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The return's details.
class AdminUpdateReturnRequest {
  AdminUpdateReturnRequest({
    this.locationId,
    this.noNotification,
    this.metadata,
  });

  /// The ID of the location to return items to.
  final String? locationId;
  /// Whether the customer should receive notifications about updates to the return.
  final bool? noNotification;
  /// The return's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory AdminUpdateReturnRequest.fromJson(Map<String, dynamic> json) => AdminUpdateReturnRequest(
    locationId: json["location_id"] as String?,
    noNotification: json["no_notification"] as bool?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (locationId != null) "location_id": locationId!,
    if (noNotification != null) "no_notification": noNotification!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The shipping method's details.
class AdminUpdateReturnShipping {
  AdminUpdateReturnShipping({
    this.customAmount,
    this.internalNote,
    this.metadata,
  });

  /// Set a custom price for the shipping method.
  final num? customAmount;
  /// A note viewed only by admin users.
  final String? internalNote;
  /// The return's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory AdminUpdateReturnShipping.fromJson(Map<String, dynamic> json) => AdminUpdateReturnShipping(
    customAmount: json["custom_amount"] as num?,
    internalNote: json["internal_note"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (customAmount != null) "custom_amount": customAmount!,
    if (internalNote != null) "internal_note": internalNote!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The properties to update in the sales channel.
class AdminUpdateSalesChannel {
  AdminUpdateSalesChannel({
    this.name,
    this.description,
    this.isDisabled,
    this.metadata,
  });

  /// The sales channel's name.
  final String? name;
  /// The sales channel's description.
  final String? description;
  /// Whether the sales channel is disabled.
  final bool? isDisabled;
  /// The sales channel's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory AdminUpdateSalesChannel.fromJson(Map<String, dynamic> json) => AdminUpdateSalesChannel(
    name: json["name"] as String?,
    description: json["description"] as String?,
    isDisabled: json["is_disabled"] as bool?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (name != null) "name": name!,
    if (description != null) "description": description!,
    if (isDisabled != null) "is_disabled": isDisabled!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The properties to update in the shipping option type.
class AdminUpdateShippingOption {
  AdminUpdateShippingOption({
    this.name,
    this.data,
    this.priceType,
    this.providerId,
    this.shippingProfileId,
    this.type,
    this.typeId,
    this.prices,
    this.rules,
    this.metadata,
  });

  /// The shipping option's name.
  final String? name;
  /// The shipping option's data.
  final Map<String, dynamic>? data;
  /// The shipping option's price type.
  final String? priceType;
  /// The shipping option's provider id.
  final String? providerId;
  /// The shipping option's shipping profile id.
  final String? shippingProfileId;
  final AdminCreateShippingOptionType? type;
  /// The shipping option's type id.
  final String? typeId;
  /// The shipping option's prices.
  final List<dynamic>? prices;
  /// The shipping option's rules.
  final List<dynamic>? rules;
  /// The shipping option's metadata.
  final Map<String, dynamic>? metadata;

  factory AdminUpdateShippingOption.fromJson(Map<String, dynamic> json) => AdminUpdateShippingOption(
    name: json["name"] as String?,
    data: json["data"] as Map<String, dynamic>?,
    priceType: json["price_type"] as String?,
    providerId: json["provider_id"] as String?,
    shippingProfileId: json["shipping_profile_id"] as String?,
    type: json["type"] == null ? null : AdminCreateShippingOptionType.fromJson(json["type"] as Map<String, dynamic>),
    typeId: json["type_id"] as String?,
    prices: (json["prices"] as List?)?.map((e) => e).toList().cast<dynamic>(),
    rules: (json["rules"] as List?)?.map((e) => e).toList().cast<dynamic>(),
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (name != null) "name": name!,
    if (data != null) "data": data!,
    if (priceType != null) "price_type": priceType!,
    if (providerId != null) "provider_id": providerId!,
    if (shippingProfileId != null) "shipping_profile_id": shippingProfileId!,
    if (type != null) "type": type!.toJson(),
    if (typeId != null) "type_id": typeId!,
    if (prices != null) "prices": prices!.map((e) => e).toList(),
    if (rules != null) "rules": rules!.map((e) => e).toList(),
    if (metadata != null) "metadata": metadata!,
  };
}

/// The properties to update in a shipping option price with a currency.
class AdminUpdateShippingOptionPriceWithCurrency {
  AdminUpdateShippingOptionPriceWithCurrency({
    this.id,
    this.currencyCode,
    this.amount,
    this.rules,
  });

  /// The price's ID.
  final String? id;
  /// The price's currency code.
  final String? currencyCode;
  /// The price's amount.
  final num? amount;
  /// The price's rules.
  final List<PriceRule>? rules;

  factory AdminUpdateShippingOptionPriceWithCurrency.fromJson(Map<String, dynamic> json) => AdminUpdateShippingOptionPriceWithCurrency(
    id: json["id"] as String?,
    currencyCode: json["currency_code"] as String?,
    amount: json["amount"] as num?,
    rules: (json["rules"] as List?)?.map((e) => e == null ? null : PriceRule.fromJson(e as Map<String, dynamic>)).toList().cast<PriceRule>(),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (amount != null) "amount": amount!,
    if (rules != null) "rules": rules!.map((e) => e.toJson()).toList(),
  };
}

/// The properties to update in a shipping option price with a region.
class AdminUpdateShippingOptionPriceWithRegion {
  AdminUpdateShippingOptionPriceWithRegion({
    this.id,
    this.regionId,
    this.amount,
    this.rules,
  });

  /// The price's ID.
  final String? id;
  /// The ID of the region the price is associated with.
  final String? regionId;
  /// The price's amount.
  final num? amount;
  /// The price's rules.
  final List<PriceRule>? rules;

  factory AdminUpdateShippingOptionPriceWithRegion.fromJson(Map<String, dynamic> json) => AdminUpdateShippingOptionPriceWithRegion(
    id: json["id"] as String?,
    regionId: json["region_id"] as String?,
    amount: json["amount"] as num?,
    rules: (json["rules"] as List?)?.map((e) => e == null ? null : PriceRule.fromJson(e as Map<String, dynamic>)).toList().cast<PriceRule>(),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (regionId != null) "region_id": regionId!,
    if (amount != null) "amount": amount!,
    if (rules != null) "rules": rules!.map((e) => e.toJson()).toList(),
  };
}

/// The properties to update in the shipping option rule.
class AdminUpdateShippingOptionRule {
  AdminUpdateShippingOptionRule({
    this.id,
    this.operator,
    this.attribute,
    this.value,
  });

  /// The rule's ID.
  final String? id;
  /// The operator used to check whether a rule applies.
  final String? operator;
  /// The name of a property or table that the rule applies to.
  final String? attribute;
  final dynamic value;

  factory AdminUpdateShippingOptionRule.fromJson(Map<String, dynamic> json) => AdminUpdateShippingOptionRule(
    id: json["id"] as String?,
    operator: json["operator"] as String?,
    attribute: json["attribute"] as String?,
    value: json["value"],
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (operator != null) "operator": operator!,
    if (attribute != null) "attribute": attribute!,
    if (value != null) "value": value!,
  };
}

/// The properties to update in the shipping option type.
class AdminUpdateShippingOptionType {
  AdminUpdateShippingOptionType({
    this.code,
    this.description,
    this.label,
  });

  /// The shipping option type's code.
  final String? code;
  /// The shipping option type's description.
  final String? description;
  /// The shipping option type's label.
  final String? label;

  factory AdminUpdateShippingOptionType.fromJson(Map<String, dynamic> json) => AdminUpdateShippingOptionType(
    code: json["code"] as String?,
    description: json["description"] as String?,
    label: json["label"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (code != null) "code": code!,
    if (description != null) "description": description!,
    if (label != null) "label": label!,
  };
}

/// The properties to update in the shipping profile.
class AdminUpdateShippingProfile {
  AdminUpdateShippingProfile({
    this.name,
    this.type,
    this.metadata,
  });

  /// The shipping profile's name.
  final String? name;
  /// The shipping profile's type.
  final String? type;
  /// The shipping profile's metadata.
  final Map<String, dynamic>? metadata;

  factory AdminUpdateShippingProfile.fromJson(Map<String, dynamic> json) => AdminUpdateShippingProfile(
    name: json["name"] as String?,
    type: json["type"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (name != null) "name": name!,
    if (type != null) "type": type!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The properties to update in a stock location.
class AdminUpdateStockLocation {
  AdminUpdateStockLocation({
    this.name,
    this.address,
    this.addressId,
    this.metadata,
  });

  /// The stock location's name.
  final String? name;
  final AdminUpsertStockLocationAddress? address;
  /// The ID of an existing stock location address to associate the stock location with.
  final String? addressId;
  /// The stock location's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory AdminUpdateStockLocation.fromJson(Map<String, dynamic> json) => AdminUpdateStockLocation(
    name: json["name"] as String?,
    address: json["address"] == null ? null : AdminUpsertStockLocationAddress.fromJson(json["address"] as Map<String, dynamic>),
    addressId: json["address_id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (name != null) "name": name!,
    if (address != null) "address": address!.toJson(),
    if (addressId != null) "address_id": addressId!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The properties to update in a store.
class AdminUpdateStore {
  AdminUpdateStore({
    this.name,
    this.supportedCurrencies,
    this.defaultSalesChannelId,
    this.defaultRegionId,
    this.defaultLocationId,
    this.metadata,
    this.supportedLocales,
  });

  /// The store's name.
  final String? name;
  /// The store's supported currencies.
  final List<AdminUpdateStoreSupportedCurrency>? supportedCurrencies;
  /// The ID of the default sales channel in the store.
  final String? defaultSalesChannelId;
  /// The ID of the default region in the store.
  final String? defaultRegionId;
  /// The ID of the default stock location in the store.
  final String? defaultLocationId;
  /// The store's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The store's supported locales.
  final List<AdminUpdateStoreSupportedLocale>? supportedLocales;

  factory AdminUpdateStore.fromJson(Map<String, dynamic> json) => AdminUpdateStore(
    name: json["name"] as String?,
    supportedCurrencies: (json["supported_currencies"] as List?)?.map((e) => e == null ? null : AdminUpdateStoreSupportedCurrency.fromJson(e as Map<String, dynamic>)).toList().cast<AdminUpdateStoreSupportedCurrency>(),
    defaultSalesChannelId: json["default_sales_channel_id"] as String?,
    defaultRegionId: json["default_region_id"] as String?,
    defaultLocationId: json["default_location_id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    supportedLocales: (json["supported_locales"] as List?)?.map((e) => e == null ? null : AdminUpdateStoreSupportedLocale.fromJson(e as Map<String, dynamic>)).toList().cast<AdminUpdateStoreSupportedLocale>(),
  );

  Map<String, dynamic> toJson() => {
    if (name != null) "name": name!,
    if (supportedCurrencies != null) "supported_currencies": supportedCurrencies!.map((e) => e.toJson()).toList(),
    if (defaultSalesChannelId != null) "default_sales_channel_id": defaultSalesChannelId!,
    if (defaultRegionId != null) "default_region_id": defaultRegionId!,
    if (defaultLocationId != null) "default_location_id": defaultLocationId!,
    if (metadata != null) "metadata": metadata!,
    if (supportedLocales != null) "supported_locales": supportedLocales!.map((e) => e.toJson()).toList(),
  };
}

/// The details to update in a supported currency of the store.
class AdminUpdateStoreSupportedCurrency {
  AdminUpdateStoreSupportedCurrency({
    this.currencyCode,
    this.isDefault,
    this.isTaxInclusive,
  });

  /// The currency's code.
  final String? currencyCode;
  /// Whether the currency is the default in the store.
  final bool? isDefault;
  /// Whether prices using this currency are tax inclusive.
  final bool? isTaxInclusive;

  factory AdminUpdateStoreSupportedCurrency.fromJson(Map<String, dynamic> json) => AdminUpdateStoreSupportedCurrency(
    currencyCode: json["currency_code"] as String?,
    isDefault: json["is_default"] as bool?,
    isTaxInclusive: json["is_tax_inclusive"] as bool?,
  );

  Map<String, dynamic> toJson() => {
    if (currencyCode != null) "currency_code": currencyCode!,
    if (isDefault != null) "is_default": isDefault!,
    if (isTaxInclusive != null) "is_tax_inclusive": isTaxInclusive!,
  };
}

/// The payload to update a store's supported locale.
class AdminUpdateStoreSupportedLocale {
  AdminUpdateStoreSupportedLocale({
    this.localeCode,
  });

  /// The locale's code in [BCP 47 format](https://gist.github.com/typpo/b2b828a35e683b9bf8db91b5404f1bd1).
  final String? localeCode;

  factory AdminUpdateStoreSupportedLocale.fromJson(Map<String, dynamic> json) => AdminUpdateStoreSupportedLocale(
    localeCode: json["locale_code"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (localeCode != null) "locale_code": localeCode!,
  };
}

/// The properties to update in the tax rate.
class AdminUpdateTaxRate {
  AdminUpdateTaxRate({
    this.rate,
    this.code,
    this.rules,
    this.name,
    this.isDefault,
    this.isCombinable,
    this.metadata,
  });

  /// The rate to charge.
  final num? rate;
  /// The code that the tax rate is identified by.
  final String? code;
  /// The tax rate's rules.
  final List<AdminCreateTaxRateRule>? rules;
  /// The tax rate's name.
  final String? name;
  /// Whether the tax rate is the default in the store.
  final bool? isDefault;
  /// Whether the tax rate should be combined with parent rates.
  final bool? isCombinable;
  /// The tax rate's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory AdminUpdateTaxRate.fromJson(Map<String, dynamic> json) => AdminUpdateTaxRate(
    rate: json["rate"] as num?,
    code: json["code"] as String?,
    rules: (json["rules"] as List?)?.map((e) => e == null ? null : AdminCreateTaxRateRule.fromJson(e as Map<String, dynamic>)).toList().cast<AdminCreateTaxRateRule>(),
    name: json["name"] as String?,
    isDefault: json["is_default"] as bool?,
    isCombinable: json["is_combinable"] as bool?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (rate != null) "rate": rate!,
    if (code != null) "code": code!,
    if (rules != null) "rules": rules!.map((e) => e.toJson()).toList(),
    if (name != null) "name": name!,
    if (isDefault != null) "is_default": isDefault!,
    if (isCombinable != null) "is_combinable": isCombinable!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The details to update in a tax region.
class AdminUpdateTaxRegion {
  AdminUpdateTaxRegion({
    this.provinceCode,
    this.metadata,
    this.providerId,
  });

  /// The tax region's ISO 3166-2 province code. Must be lower-case.
  final String? provinceCode;
  /// The tax region's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The ID of the tax provider to use when calculating taxes for this tax region.
  final String? providerId;

  factory AdminUpdateTaxRegion.fromJson(Map<String, dynamic> json) => AdminUpdateTaxRegion(
    provinceCode: json["province_code"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    providerId: json["provider_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (provinceCode != null) "province_code": provinceCode!,
    if (metadata != null) "metadata": metadata!,
    if (providerId != null) "provider_id": providerId!,
  };
}

/// The data for updating a translation.
class AdminUpdateTranslation {
  AdminUpdateTranslation({
    this.id,
    this.reference,
    this.referenceId,
    this.localeCode,
    this.translations,
  });

  /// The ID of the translation to update.
  final String? id;
  /// The resource that the translation belongs to.
  final String? reference;
  /// The ID of the resource that the translation belongs to. For example, the ID of a product.
  final String? referenceId;
  /// The translation's locale code in BCP 47 format.
  final String? localeCode;
  /// The translation key-value pairs. Each key is a field in the resource, and the value is the translated text.
  final Map<String, dynamic>? translations;

  factory AdminUpdateTranslation.fromJson(Map<String, dynamic> json) => AdminUpdateTranslation(
    id: json["id"] as String?,
    reference: json["reference"] as String?,
    referenceId: json["reference_id"] as String?,
    localeCode: json["locale_code"] as String?,
    translations: json["translations"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (reference != null) "reference": reference!,
    if (referenceId != null) "reference_id": referenceId!,
    if (localeCode != null) "locale_code": localeCode!,
    if (translations != null) "translations": translations!,
  };
}

/// The translation settings to update.
class AdminUpdateTranslationSettings {
  AdminUpdateTranslationSettings({
    this.id,
    this.entityType,
    this.fields,
    this.isActive,
  });

  /// The ID of the translation settings to update.
  final String? id;
  /// The entity type to update the translation settings for.
  final String? entityType;
  /// The fields to update translations for.
  final List<String>? fields;
  /// Whether translations are active for the given entity type.
  final bool? isActive;

  factory AdminUpdateTranslationSettings.fromJson(Map<String, dynamic> json) => AdminUpdateTranslationSettings(
    id: json["id"] as String?,
    entityType: json["entity_type"] as String?,
    fields: (json["fields"] as List?)?.map((e) => e as String?).toList().cast<String>(),
    isActive: json["is_active"] as bool?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (entityType != null) "entity_type": entityType!,
    if (fields != null) "fields": fields!.map((e) => e).toList(),
    if (isActive != null) "is_active": isActive!,
  };
}

/// The properties to update in a user.
class AdminUpdateUser {
  AdminUpdateUser({
    this.firstName,
    this.lastName,
    this.avatarUrl,
    this.metadata,
  });

  /// The user's first name.
  final String? firstName;
  /// The user's last name.
  final String? lastName;
  /// The URL of the user's avatar.
  final String? avatarUrl;
  /// The user's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory AdminUpdateUser.fromJson(Map<String, dynamic> json) => AdminUpdateUser(
    firstName: json["first_name"] as String?,
    lastName: json["last_name"] as String?,
    avatarUrl: json["avatar_url"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (firstName != null) "first_name": firstName!,
    if (lastName != null) "last_name": lastName!,
    if (avatarUrl != null) "avatar_url": avatarUrl!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The properties to update of the variant's inventory item association.
class AdminUpdateVariantInventoryItem {
  AdminUpdateVariantInventoryItem({
    this.requiredQuantity,
  });

  /// The number of units a single quantity is equivalent to. For example, if a customer orders one quantity of the variant, Medusa checks the availability of the qua
  final num? requiredQuantity;

  factory AdminUpdateVariantInventoryItem.fromJson(Map<String, dynamic> json) => AdminUpdateVariantInventoryItem(
    requiredQuantity: json["required_quantity"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (requiredQuantity != null) "required_quantity": requiredQuantity!,
  };
}

/// The details to update in a view configuration.
class AdminUpdateViewConfiguration {
  AdminUpdateViewConfiguration({
    this.setActive,
    this.name,
    this.isSystemDefault,
    this.configuration,
  });

  /// Whether the view is set as active.
  final bool? setActive;
  /// The view's name.
  final String? name;
  /// Whether the view is the system default.
  final bool? isSystemDefault;
  /// The view's configuration.
  final Map<String, dynamic>? configuration;

  factory AdminUpdateViewConfiguration.fromJson(Map<String, dynamic> json) => AdminUpdateViewConfiguration(
    setActive: json["set_active"] as bool?,
    name: json["name"] as String?,
    isSystemDefault: json["is_system_default"] as bool?,
    configuration: json["configuration"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (setActive != null) "set_active": setActive!,
    if (name != null) "name": name!,
    if (isSystemDefault != null) "is_system_default": isSystemDefault!,
    if (configuration != null) "configuration": configuration!,
  };
}

/// The details of the file to upload.
class AdminUploadPreSignedUrl {
  AdminUploadPreSignedUrl({
    this.originalname,
    this.size,
    this.mimeType,
    this.access,
  });

  /// The file's original name.
  final String? originalname;
  /// The file's size in bytes.
  final num? size;
  /// The file's mime type.
  final String? mimeType;
  /// The access level of the file. If `private`, the file will not be publicly accessible. The default value depends on the configured File Module Provider.
  final String? access;

  factory AdminUploadPreSignedUrl.fromJson(Map<String, dynamic> json) => AdminUploadPreSignedUrl(
    originalname: json["originalname"] as String?,
    size: json["size"] as num?,
    mimeType: json["mime_type"] as String?,
    access: json["access"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (originalname != null) "originalname": originalname!,
    if (size != null) "size": size!,
    if (mimeType != null) "mime_type": mimeType!,
    if (access != null) "access": access!,
  };
}

/// The result of the pre-signed URL upload request.
class AdminUploadPreSignedUrlResponse {
  AdminUploadPreSignedUrlResponse({
    this.url,
    this.filename,
    this.originalname,
    this.mimeType,
    this.extension,
    this.size,
  });

  /// The file's pre-signed upload URL.
  final String? url;
  /// The file's filename.
  final String? filename;
  /// The file's orignal name.
  final String? originalname;
  /// The file's mime type.
  final String? mimeType;
  /// The file's extension.
  final String? extension;
  /// The file's size in bytes.
  final num? size;

  factory AdminUploadPreSignedUrlResponse.fromJson(Map<String, dynamic> json) => AdminUploadPreSignedUrlResponse(
    url: json["url"] as String?,
    filename: json["filename"] as String?,
    originalname: json["originalname"] as String?,
    mimeType: json["mime_type"] as String?,
    extension: json["extension"] as String?,
    size: json["size"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (url != null) "url": url!,
    if (filename != null) "filename": filename!,
    if (originalname != null) "originalname": originalname!,
    if (mimeType != null) "mime_type": mimeType!,
    if (extension != null) "extension": extension!,
    if (size != null) "size": size!,
  };
}

/// The details of the stock location address to create.
class AdminUpsertStockLocationAddress {
  AdminUpsertStockLocationAddress({
    this.address1,
    this.address2,
    this.company,
    this.countryCode,
    this.city,
    this.phone,
    this.postalCode,
    this.province,
    this.metadata,
  });

  /// The address's first line.
  final String? address1;
  /// The address's second line.
  final String? address2;
  /// The address's company.
  final String? company;
  /// The address's country code.
  final String? countryCode;
  /// The address's city.
  final String? city;
  /// The address's phone.
  final String? phone;
  /// The address's postal code.
  final String? postalCode;
  /// The address's ISO 3166-2 province code. Must be lower-case.
  final String? province;
  /// The address's metadata.
  final Map<String, dynamic>? metadata;

  factory AdminUpsertStockLocationAddress.fromJson(Map<String, dynamic> json) => AdminUpsertStockLocationAddress(
    address1: json["address_1"] as String?,
    address2: json["address_2"] as String?,
    company: json["company"] as String?,
    countryCode: json["country_code"] as String?,
    city: json["city"] as String?,
    phone: json["phone"] as String?,
    postalCode: json["postal_code"] as String?,
    province: json["province"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (address1 != null) "address_1": address1!,
    if (address2 != null) "address_2": address2!,
    if (company != null) "company": company!,
    if (countryCode != null) "country_code": countryCode!,
    if (city != null) "city": city!,
    if (phone != null) "phone": phone!,
    if (postalCode != null) "postal_code": postalCode!,
    if (province != null) "province": province!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The user's details.
class AdminUser {
  AdminUser({
    this.id,
    this.email,
    this.firstName,
    this.lastName,
    this.avatarUrl,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
  });

  /// The user's ID.
  final String? id;
  /// The user's email.
  final String? email;
  /// The user's first name.
  final String? firstName;
  /// The user's last name.
  final String? lastName;
  /// The URL of the user's avatar.
  final String? avatarUrl;
  /// The user's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the user was created.
  final DateTime? createdAt;
  /// The date the user was updated.
  final DateTime? updatedAt;
  /// The date the user was deleted.
  final DateTime? deletedAt;

  factory AdminUser.fromJson(Map<String, dynamic> json) => AdminUser(
    id: json["id"] as String?,
    email: json["email"] as String?,
    firstName: json["first_name"] as String?,
    lastName: json["last_name"] as String?,
    avatarUrl: json["avatar_url"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (email != null) "email": email!,
    if (firstName != null) "first_name": firstName!,
    if (lastName != null) "last_name": lastName!,
    if (avatarUrl != null) "avatar_url": avatarUrl!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
  };
}

/// The details of the deleted user.
class AdminUserDeleteResponse {
  AdminUserDeleteResponse({
    this.id,
    this.object,
    this.deleted,
  });

  /// The user's ID.
  final String? id;
  /// The name of the deleted object.
  final String? object;
  /// Whether the user was deleted.
  final bool? deleted;

  factory AdminUserDeleteResponse.fromJson(Map<String, dynamic> json) => AdminUserDeleteResponse(
    id: json["id"] as String?,
    object: json["object"] as String?,
    deleted: json["deleted"] as bool?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (object != null) "object": object!,
    if (deleted != null) "deleted": deleted!,
  };
}

/// The paginated list of users.
class AdminUserListResponse {
  AdminUserListResponse({
    this.limit,
    this.offset,
    this.count,
    this.users,
    this.estimateCount,
  });

  /// The maximum number of items returned.
  final num? limit;
  /// The number of items skipped before retrieving the returned items.
  final num? offset;
  /// The total number of items.
  final num? count;
  /// The list of users.
  final List<AdminUser>? users;
  /// The estimated count retrieved from the PostgreSQL query planner, which may be inaccurate.
  final num? estimateCount;

  factory AdminUserListResponse.fromJson(Map<String, dynamic> json) => AdminUserListResponse(
    limit: json["limit"] as num?,
    offset: json["offset"] as num?,
    count: json["count"] as num?,
    users: (json["users"] as List?)?.map((e) => e == null ? null : AdminUser.fromJson(e as Map<String, dynamic>)).toList().cast<AdminUser>(),
    estimateCount: json["estimate_count"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (limit != null) "limit": limit!,
    if (offset != null) "offset": offset!,
    if (count != null) "count": count!,
    if (users != null) "users": users!.map((e) => e.toJson()).toList(),
    if (estimateCount != null) "estimate_count": estimateCount!,
  };
}

/// The user's details.
class AdminUserResponse {
  AdminUserResponse({
    this.user,
  });

  final AdminUser? user;

  factory AdminUserResponse.fromJson(Map<String, dynamic> json) => AdminUserResponse(
    user: json["user"] == null ? null : AdminUser.fromJson(json["user"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (user != null) "user": user!.toJson(),
  };
}

/// The details of a view configuration.
class AdminViewConfiguration {
  AdminViewConfiguration({
    this.id,
    this.entity,
    this.name,
    this.userId,
    this.isSystemDefault,
    this.configuration,
    this.createdAt,
    this.updatedAt,
  });

  /// The view configuration's ID.
  final String? id;
  /// The entity the view configuration belongs to.
  final String? entity;
  /// The view configuration's name.
  final String? name;
  /// The ID of the user the view configuration belongs to. If `null`, the view configuration is a system default.
  final String? userId;
  /// Whether the view configuration is the system default.
  final bool? isSystemDefault;
  /// The view's configuration.
  final Map<String, dynamic>? configuration;
  /// The date the view configuration was created.
  final DateTime? createdAt;
  /// The date the view configuration was updated.
  final DateTime? updatedAt;

  factory AdminViewConfiguration.fromJson(Map<String, dynamic> json) => AdminViewConfiguration(
    id: json["id"] as String?,
    entity: json["entity"] as String?,
    name: json["name"] as String?,
    userId: json["user_id"] as String?,
    isSystemDefault: json["is_system_default"] as bool?,
    configuration: json["configuration"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (entity != null) "entity": entity!,
    if (name != null) "name": name!,
    if (userId != null) "user_id": userId!,
    if (isSystemDefault != null) "is_system_default": isSystemDefault!,
    if (configuration != null) "configuration": configuration!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The details of a view configuration.
class AdminViewConfigurationResponse {
  AdminViewConfigurationResponse({
    this.viewConfiguration,
  });

  final AdminViewConfiguration? viewConfiguration;

  factory AdminViewConfigurationResponse.fromJson(Map<String, dynamic> json) => AdminViewConfigurationResponse(
    viewConfiguration: json["view_configuration"] == null ? null : AdminViewConfiguration.fromJson(json["view_configuration"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (viewConfiguration != null) "view_configuration": viewConfiguration!.toJson(),
  };
}

/// The view's columns.
class AdminViewsEntityColumnsResponse {
  AdminViewsEntityColumnsResponse({
    this.columns,
  });

  /// The view's columns.
  final List<AdminColumn>? columns;

  factory AdminViewsEntityColumnsResponse.fromJson(Map<String, dynamic> json) => AdminViewsEntityColumnsResponse(
    columns: (json["columns"] as List?)?.map((e) => e == null ? null : AdminColumn.fromJson(e as Map<String, dynamic>)).toList().cast<AdminColumn>(),
  );

  Map<String, dynamic> toJson() => {
    if (columns != null) "columns": columns!.map((e) => e.toJson()).toList(),
  };
}

/// The workflows execution's details.
class AdminWorkflowExecution {
  AdminWorkflowExecution({
    this.id,
    this.workflowId,
    this.transactionId,
    this.execution,
    this.context,
    this.state,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
  });

  /// The workflow execution's ID.
  final String? id;
  /// The ID of the workflow.
  final String? workflowId;
  /// The workflow execution's transaction ID.
  final String? transactionId;
  final AdminWorkflowExecutionExecution? execution;
  final WorkflowExecutionContext? context;
  /// The workflow execution's state.
  final String? state;
  /// The date the workflow execution was created.
  final DateTime? createdAt;
  /// The date the workflow execution was updated.
  final DateTime? updatedAt;
  /// The date the workflow execution was deleted.
  final DateTime? deletedAt;

  factory AdminWorkflowExecution.fromJson(Map<String, dynamic> json) => AdminWorkflowExecution(
    id: json["id"] as String?,
    workflowId: json["workflow_id"] as String?,
    transactionId: json["transaction_id"] as String?,
    execution: json["execution"] == null ? null : AdminWorkflowExecutionExecution.fromJson(json["execution"] as Map<String, dynamic>),
    context: json["context"] == null ? null : WorkflowExecutionContext.fromJson(json["context"] as Map<String, dynamic>),
    state: json["state"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (workflowId != null) "workflow_id": workflowId!,
    if (transactionId != null) "transaction_id": transactionId!,
    if (execution != null) "execution": execution!.toJson(),
    if (context != null) "context": context!.toJson(),
    if (state != null) "state": state!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
  };
}

/// The workflow execution's steps details.
class AdminWorkflowExecutionExecution {
  AdminWorkflowExecutionExecution({
    this.steps,
  });

  /// The execution's steps. Each object key is a step ID, and the value is the object whose properties are shown below.
  final Map<String, dynamic>? steps;

  factory AdminWorkflowExecutionExecution.fromJson(Map<String, dynamic> json) => AdminWorkflowExecutionExecution(
    steps: json["steps"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (steps != null) "steps": steps!,
  };
}

/// The workflow execution's details.
class AdminWorkflowExecutionResponse {
  AdminWorkflowExecutionResponse({
    this.workflowExecution,
  });

  final AdminWorkflowExecution? workflowExecution;

  factory AdminWorkflowExecutionResponse.fromJson(Map<String, dynamic> json) => AdminWorkflowExecutionResponse(
    workflowExecution: json["workflow_execution"] == null ? null : AdminWorkflowExecution.fromJson(json["workflow_execution"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (workflowExecution != null) "workflow_execution": workflowExecution!.toJson(),
  };
}

/// The API key's details.
class ApiKeyResponse {
  ApiKeyResponse({
    this.id,
    this.token,
    this.redacted,
    this.title,
    this.type,
    this.lastUsedAt,
    this.createdBy,
    this.createdAt,
    this.revokedBy,
    this.revokedAt,
  });

  /// The API key's ID.
  final String? id;
  /// The API key's token.
  final String? token;
  /// The redacted form of the API key's token. This is useful when showing portion of the token. For example `sk_...123`.
  final String? redacted;
  /// The API key's title.
  final String? title;
  /// The API key's type. `secret` API keys are used for authenticating admin users, and `publishable` API key are used for storefronts and similar clients to scope r
  final String? type;
  /// The date the API key was last used.
  final DateTime? lastUsedAt;
  /// The ID of the user that created the API key.
  final String? createdBy;
  /// The date the API key was created.
  final DateTime? createdAt;
  /// The ID of the user that revoked the API key.
  final String? revokedBy;
  /// The date the API key was revoked.
  final DateTime? revokedAt;

  factory ApiKeyResponse.fromJson(Map<String, dynamic> json) => ApiKeyResponse(
    id: json["id"] as String?,
    token: json["token"] as String?,
    redacted: json["redacted"] as String?,
    title: json["title"] as String?,
    type: json["type"] as String?,
    lastUsedAt: json["last_used_at"] == null ? null : DateTime.parse(json["last_used_at"] as String),
    createdBy: json["created_by"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    revokedBy: json["revoked_by"] as String?,
    revokedAt: json["revoked_at"] == null ? null : DateTime.parse(json["revoked_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (token != null) "token": token!,
    if (redacted != null) "redacted": redacted!,
    if (title != null) "title": title!,
    if (type != null) "type": type!,
    if (lastUsedAt != null) "last_used_at": lastUsedAt!.toIso8601String(),
    if (createdBy != null) "created_by": createdBy!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (revokedBy != null) "revoked_by": revokedBy!,
    if (revokedAt != null) "revoked_at": revokedAt!.toIso8601String(),
  };
}

/// The authenticated user's details.
class AuthAdminSessionResponse {
  AuthAdminSessionResponse({
    this.user,
  });

  /// The logged-in user.
  final AdminUser? user;

  factory AuthAdminSessionResponse.fromJson(Map<String, dynamic> json) => AuthAdminSessionResponse(
    user: json["user"] == null ? null : AdminUser.fromJson(json["user"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (user != null) "user": user!.toJson(),
  };
}

/// The authentication's details.
class AuthCallbackResponse {
  AuthCallbackResponse({
    this.location,
  });

  /// The location to redirect the user to for further authentication with the third-party provider.
  final String? location;

  factory AuthCallbackResponse.fromJson(Map<String, dynamic> json) => AuthCallbackResponse(
    location: json["location"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (location != null) "location": location!,
  };
}

/// The authentication's details.
class AuthResponse {
  AuthResponse({
    this.token,
  });

  /// The JWT token used for registration or authentication.
  final String? token;

  factory AuthResponse.fromJson(Map<String, dynamic> json) => AuthResponse(
    token: json["token"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (token != null) "token": token!,
  };
}

/// The authenticated customer's details.
class AuthStoreSessionResponse {
  AuthStoreSessionResponse({
    this.user,
  });

  /// The logged-in customer.
  final StoreCustomer? user;

  factory AuthStoreSessionResponse.fromJson(Map<String, dynamic> json) => AuthStoreSessionResponse(
    user: json["user"] == null ? null : StoreCustomer.fromJson(json["user"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (user != null) "user": user!.toJson(),
  };
}

/// The calculated price's details.
class BaseCalculatedPriceSet {
  BaseCalculatedPriceSet({
    this.id,
    this.isCalculatedPricePriceList,
    this.isCalculatedPriceTaxInclusive,
    this.calculatedAmount,
    this.calculatedAmountWithTax,
    this.calculatedAmountWithoutTax,
    this.isOriginalPricePriceList,
    this.isOriginalPriceTaxInclusive,
    this.originalAmount,
    this.currencyCode,
    this.calculatedPrice,
    this.originalPrice,
    this.originalAmountWithTax,
    this.originalAmountWithoutTax,
  });

  /// The calculated price's ID.
  final String? id;
  /// Whether the calculated price belongs to a price list.
  final bool? isCalculatedPricePriceList;
  /// Whether the calculated price is tax inclusive.
  final bool? isCalculatedPriceTaxInclusive;
  /// The amount of the calculated price, or `null` if there isn't a calculated price. This is the amount shown to the customer.
  final num? calculatedAmount;
  /// The calculated price's amount with taxes applied.
  final num? calculatedAmountWithTax;
  /// The calculated price's amount without taxes applied.
  final num? calculatedAmountWithoutTax;
  /// Whether the original price belongs to a price list.
  final bool? isOriginalPricePriceList;
  /// Whether the original price is tax inclusive.
  final bool? isOriginalPriceTaxInclusive;
  /// The amount of the original price, or `null` if there isn't an original price. This amount is useful to compare with the `calculated_amount`, such as to check fo
  final num? originalAmount;
  /// The calculated price's currency code.
  final String? currencyCode;
  /// The calculated price's details.
  final Map<String, dynamic>? calculatedPrice;
  /// The original price's details.
  final Map<String, dynamic>? originalPrice;
  /// The amount of the original price with taxes applied.
  final num? originalAmountWithTax;
  /// The amount of the original price without taxes.
  final num? originalAmountWithoutTax;

  factory BaseCalculatedPriceSet.fromJson(Map<String, dynamic> json) => BaseCalculatedPriceSet(
    id: json["id"] as String?,
    isCalculatedPricePriceList: json["is_calculated_price_price_list"] as bool?,
    isCalculatedPriceTaxInclusive: json["is_calculated_price_tax_inclusive"] as bool?,
    calculatedAmount: json["calculated_amount"] as num?,
    calculatedAmountWithTax: json["calculated_amount_with_tax"] as num?,
    calculatedAmountWithoutTax: json["calculated_amount_without_tax"] as num?,
    isOriginalPricePriceList: json["is_original_price_price_list"] as bool?,
    isOriginalPriceTaxInclusive: json["is_original_price_tax_inclusive"] as bool?,
    originalAmount: json["original_amount"] as num?,
    currencyCode: json["currency_code"] as String?,
    calculatedPrice: json["calculated_price"] as Map<String, dynamic>?,
    originalPrice: json["original_price"] as Map<String, dynamic>?,
    originalAmountWithTax: json["original_amount_with_tax"] as num?,
    originalAmountWithoutTax: json["original_amount_without_tax"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (isCalculatedPricePriceList != null) "is_calculated_price_price_list": isCalculatedPricePriceList!,
    if (isCalculatedPriceTaxInclusive != null) "is_calculated_price_tax_inclusive": isCalculatedPriceTaxInclusive!,
    if (calculatedAmount != null) "calculated_amount": calculatedAmount!,
    if (calculatedAmountWithTax != null) "calculated_amount_with_tax": calculatedAmountWithTax!,
    if (calculatedAmountWithoutTax != null) "calculated_amount_without_tax": calculatedAmountWithoutTax!,
    if (isOriginalPricePriceList != null) "is_original_price_price_list": isOriginalPricePriceList!,
    if (isOriginalPriceTaxInclusive != null) "is_original_price_tax_inclusive": isOriginalPriceTaxInclusive!,
    if (originalAmount != null) "original_amount": originalAmount!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (calculatedPrice != null) "calculated_price": calculatedPrice!,
    if (originalPrice != null) "original_price": originalPrice!,
    if (originalAmountWithTax != null) "original_amount_with_tax": originalAmountWithTax!,
    if (originalAmountWithoutTax != null) "original_amount_without_tax": originalAmountWithoutTax!,
  };
}

/// The details of a captured payment.
class BaseCapture {
  BaseCapture({
    this.id,
    this.amount,
    this.createdAt,
    this.createdBy,
    this.payment,
  });

  /// The payment capture's ID.
  final String? id;
  /// The captured amount.
  final num? amount;
  /// The date the capture was created.
  final DateTime? createdAt;
  /// The ID of the user that captured the payment.
  final String? createdBy;
  final Map<String, dynamic>? payment;

  factory BaseCapture.fromJson(Map<String, dynamic> json) => BaseCapture(
    id: json["id"] as String?,
    amount: json["amount"] as num?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    createdBy: json["created_by"] as String?,
    payment: json["payment"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (amount != null) "amount": amount!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (createdBy != null) "created_by": createdBy!,
    if (payment != null) "payment": payment!,
  };
}

/// The item's cart.
class BaseCart {
  BaseCart({
    this.id,
    this.region,
    this.regionId,
    this.customerId,
    this.salesChannelId,
    this.email,
    this.currencyCode,
    this.shippingAddress,
    this.billingAddress,
    this.items,
    this.shippingMethods,
    this.paymentCollection,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.originalItemTotal,
    this.originalItemSubtotal,
    this.originalItemTaxTotal,
    this.itemTotal,
    this.itemSubtotal,
    this.itemTaxTotal,
    this.originalTotal,
    this.originalSubtotal,
    this.originalTaxTotal,
    this.total,
    this.subtotal,
    this.taxTotal,
    this.discountTotal,
    this.discountTaxTotal,
    this.giftCardTotal,
    this.giftCardTaxTotal,
    this.shippingTotal,
    this.shippingSubtotal,
    this.shippingTaxTotal,
    this.originalShippingTotal,
    this.originalShippingSubtotal,
    this.originalShippingTaxTotal,
  });

  /// The cart's ID.
  final String? id;
  final BaseRegion? region;
  /// The cart's region id.
  final String? regionId;
  /// The cart's customer id.
  final String? customerId;
  /// The cart's sales channel id.
  final String? salesChannelId;
  /// The cart's email.
  final String? email;
  /// The cart's currency code.
  final String? currencyCode;
  final BaseCartAddress? shippingAddress;
  final BaseCartAddress? billingAddress;
  /// The cart's items.
  final List<Map<String, dynamic>>? items;
  /// The cart's shipping methods.
  final List<BaseCartShippingMethod>? shippingMethods;
  final BasePaymentCollection? paymentCollection;
  /// The cart's metadata.
  final Map<String, dynamic>? metadata;
  /// The cart's created at.
  final DateTime? createdAt;
  /// The cart's updated at.
  final DateTime? updatedAt;
  /// The sum of all line items' original totals before discounts, including taxes.
  final num? originalItemTotal;
  /// The sum of all line items' original subtotals before discounts, excluding taxes.
  final num? originalItemSubtotal;
  /// The sum of all line items' original tax totals before discounts.
  final num? originalItemTaxTotal;
  /// The sum of all line items' totals after discounts, including taxes.
  final num? itemTotal;
  /// The sum of all line items' subtotals before discounts, excluding taxes.
  final num? itemSubtotal;
  /// The sum of all line items' tax totals after discounts.
  final num? itemTaxTotal;
  /// The cart's total before discounts, including taxes. Calculated as the sum of `original_item_total` and `original_shipping_total`.
  final num? originalTotal;
  /// The cart's subtotal before discounts, excluding taxes. Calculated as the sum of `original_item_subtotal` and `original_shipping_subtotal`.
  final num? originalSubtotal;
  /// The cart's tax total before discounts. Calculated as the sum of `original_item_tax_total` and `original_shipping_tax_total`.
  final num? originalTaxTotal;
  /// The cart's final total after discounts and credit lines, including taxes.
  final num? total;
  /// The cart's subtotal before discounts, excluding taxes. Calculated as the sum of `item_subtotal` and `shipping_subtotal`.
  final num? subtotal;
  /// The cart's tax total after discounts. Calculated as the sum of `item_tax_total` and `shipping_tax_total`.
  final num? taxTotal;
  /// The total amount of discounts applied to the cart, including the tax portion of discounts.
  final num? discountTotal;
  /// The total amount of discounts applied to the cart's tax. Represents the tax portion of discounts.
  final num? discountTaxTotal;
  /// The cart's gift card total.
  final num? giftCardTotal;
  /// The cart's gift card tax total.
  final num? giftCardTaxTotal;
  /// The sum of all shipping methods' totals after discounts, including taxes.
  final num? shippingTotal;
  /// The sum of all shipping methods' subtotals before discounts, excluding taxes.
  final num? shippingSubtotal;
  /// The sum of all shipping methods' tax totals after discounts.
  final num? shippingTaxTotal;
  /// The sum of all shipping methods' original totals before discounts, including taxes.
  final num? originalShippingTotal;
  /// The sum of all shipping methods' original subtotals before discounts, excluding taxes.
  final num? originalShippingSubtotal;
  /// The sum of all shipping methods' original tax totals before discounts.
  final num? originalShippingTaxTotal;

  factory BaseCart.fromJson(Map<String, dynamic> json) => BaseCart(
    id: json["id"] as String?,
    region: json["region"] == null ? null : BaseRegion.fromJson(json["region"] as Map<String, dynamic>),
    regionId: json["region_id"] as String?,
    customerId: json["customer_id"] as String?,
    salesChannelId: json["sales_channel_id"] as String?,
    email: json["email"] as String?,
    currencyCode: json["currency_code"] as String?,
    shippingAddress: json["shipping_address"] == null ? null : BaseCartAddress.fromJson(json["shipping_address"]),
    billingAddress: json["billing_address"] == null ? null : BaseCartAddress.fromJson(json["billing_address"]),
    items: (json["items"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    shippingMethods: (json["shipping_methods"] as List?)?.map((e) => e == null ? null : BaseCartShippingMethod.fromJson(e as Map<String, dynamic>)).toList().cast<BaseCartShippingMethod>(),
    paymentCollection: json["payment_collection"] == null ? null : BasePaymentCollection.fromJson(json["payment_collection"] as Map<String, dynamic>),
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    originalItemTotal: json["original_item_total"] as num?,
    originalItemSubtotal: json["original_item_subtotal"] as num?,
    originalItemTaxTotal: json["original_item_tax_total"] as num?,
    itemTotal: json["item_total"] as num?,
    itemSubtotal: json["item_subtotal"] as num?,
    itemTaxTotal: json["item_tax_total"] as num?,
    originalTotal: json["original_total"] as num?,
    originalSubtotal: json["original_subtotal"] as num?,
    originalTaxTotal: json["original_tax_total"] as num?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    taxTotal: json["tax_total"] as num?,
    discountTotal: json["discount_total"] as num?,
    discountTaxTotal: json["discount_tax_total"] as num?,
    giftCardTotal: json["gift_card_total"] as num?,
    giftCardTaxTotal: json["gift_card_tax_total"] as num?,
    shippingTotal: json["shipping_total"] as num?,
    shippingSubtotal: json["shipping_subtotal"] as num?,
    shippingTaxTotal: json["shipping_tax_total"] as num?,
    originalShippingTotal: json["original_shipping_total"] as num?,
    originalShippingSubtotal: json["original_shipping_subtotal"] as num?,
    originalShippingTaxTotal: json["original_shipping_tax_total"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (region != null) "region": region!.toJson(),
    if (regionId != null) "region_id": regionId!,
    if (customerId != null) "customer_id": customerId!,
    if (salesChannelId != null) "sales_channel_id": salesChannelId!,
    if (email != null) "email": email!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (shippingAddress != null) "shipping_address": shippingAddress!.toJson(),
    if (billingAddress != null) "billing_address": billingAddress!.toJson(),
    if (items != null) "items": items!.map((e) => e).toList(),
    if (shippingMethods != null) "shipping_methods": shippingMethods!.map((e) => e.toJson()).toList(),
    if (paymentCollection != null) "payment_collection": paymentCollection!.toJson(),
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (originalItemTotal != null) "original_item_total": originalItemTotal!,
    if (originalItemSubtotal != null) "original_item_subtotal": originalItemSubtotal!,
    if (originalItemTaxTotal != null) "original_item_tax_total": originalItemTaxTotal!,
    if (itemTotal != null) "item_total": itemTotal!,
    if (itemSubtotal != null) "item_subtotal": itemSubtotal!,
    if (itemTaxTotal != null) "item_tax_total": itemTaxTotal!,
    if (originalTotal != null) "original_total": originalTotal!,
    if (originalSubtotal != null) "original_subtotal": originalSubtotal!,
    if (originalTaxTotal != null) "original_tax_total": originalTaxTotal!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (taxTotal != null) "tax_total": taxTotal!,
    if (discountTotal != null) "discount_total": discountTotal!,
    if (discountTaxTotal != null) "discount_tax_total": discountTaxTotal!,
    if (giftCardTotal != null) "gift_card_total": giftCardTotal!,
    if (giftCardTaxTotal != null) "gift_card_tax_total": giftCardTaxTotal!,
    if (shippingTotal != null) "shipping_total": shippingTotal!,
    if (shippingSubtotal != null) "shipping_subtotal": shippingSubtotal!,
    if (shippingTaxTotal != null) "shipping_tax_total": shippingTaxTotal!,
    if (originalShippingTotal != null) "original_shipping_total": originalShippingTotal!,
    if (originalShippingSubtotal != null) "original_shipping_subtotal": originalShippingSubtotal!,
    if (originalShippingTaxTotal != null) "original_shipping_tax_total": originalShippingTaxTotal!,
  };
}

/// The cart's shipping address.
class BaseCartAddress {
  BaseCartAddress([this.value]);
  final dynamic value;
  factory BaseCartAddress.fromJson(dynamic json) => BaseCartAddress(json);
  dynamic toJson() => value;
}

/// The tax line's item.
class BaseCartLineItem {
  BaseCartLineItem({
    this.id,
    this.title,
    this.subtitle,
    this.thumbnail,
    this.quantity,
    this.product,
    this.productId,
    this.productTitle,
    this.productDescription,
    this.productSubtitle,
    this.productType,
    this.productCollection,
    this.productHandle,
    this.variant,
    this.variantId,
    this.variantSku,
    this.variantBarcode,
    this.variantTitle,
    this.variantOptionValues,
    this.requiresShipping,
    this.isDiscountable,
    this.isTaxInclusive,
    this.compareAtUnitPrice,
    this.unitPrice,
    this.taxLines,
    this.adjustments,
    this.cart,
    this.cartId,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.originalTotal,
    this.originalSubtotal,
    this.originalTaxTotal,
    this.itemTotal,
    this.itemSubtotal,
    this.itemTaxTotal,
    this.total,
    this.subtotal,
    this.taxTotal,
    this.discountTotal,
    this.discountTaxTotal,
  });

  /// The item's ID.
  final String? id;
  /// The item's title.
  final String? title;
  /// The item's subtitle.
  final String? subtitle;
  /// The item's thumbnail.
  final String? thumbnail;
  /// The item's quantity.
  final num? quantity;
  final BaseProduct? product;
  /// The item's product id.
  final String? productId;
  /// The item's product title.
  final String? productTitle;
  /// The item's product description.
  final String? productDescription;
  /// The item's product subtitle.
  final String? productSubtitle;
  /// The item's product type.
  final String? productType;
  /// The item's product collection.
  final String? productCollection;
  /// The item's product handle.
  final String? productHandle;
  final BaseProductVariant? variant;
  /// The item's variant id.
  final String? variantId;
  /// The item's variant sku.
  final String? variantSku;
  /// The item's variant barcode.
  final String? variantBarcode;
  /// The item's variant title.
  final String? variantTitle;
  /// The item's variant option values.
  final Map<String, dynamic>? variantOptionValues;
  /// The item's requires shipping.
  final bool? requiresShipping;
  /// The item's is discountable.
  final bool? isDiscountable;
  /// The item's is tax inclusive.
  final bool? isTaxInclusive;
  /// The item's compare at unit price.
  final num? compareAtUnitPrice;
  /// The item's unit price.
  final num? unitPrice;
  /// The item's tax lines.
  final List<Map<String, dynamic>>? taxLines;
  /// The item's adjustments.
  final List<BaseLineItemAdjustment>? adjustments;
  final BaseCart? cart;
  /// The item's cart id.
  final String? cartId;
  /// The item's metadata.
  final Map<String, dynamic>? metadata;
  /// The item's created at.
  final DateTime? createdAt;
  /// The item's updated at.
  final DateTime? updatedAt;
  /// The item's deleted at.
  final DateTime? deletedAt;
  /// The line item's original total before discounts, including taxes.
  final num? originalTotal;
  /// The line item's original subtotal before discounts, excluding taxes.
  final num? originalSubtotal;
  /// The line item's original tax total before discounts.
  final num? originalTaxTotal;
  /// The line item's total after discounts, including taxes.
  final num? itemTotal;
  /// The line item's subtotal before discounts, excluding taxes.
  final num? itemSubtotal;
  /// The line item's tax total after discounts.
  final num? itemTaxTotal;
  /// The line item's total after discounts, including taxes.
  final num? total;
  /// The line item's subtotal before discounts, excluding taxes.
  final num? subtotal;
  /// The line item's tax total after discounts.
  final num? taxTotal;
  /// The total amount of discounts applied to the line item, including the tax portion of discounts.
  final num? discountTotal;
  /// The total amount of discounts applied to the line item's tax. Represents the tax portion of discounts.
  final num? discountTaxTotal;

  factory BaseCartLineItem.fromJson(Map<String, dynamic> json) => BaseCartLineItem(
    id: json["id"] as String?,
    title: json["title"] as String?,
    subtitle: json["subtitle"] as String?,
    thumbnail: json["thumbnail"] as String?,
    quantity: json["quantity"] as num?,
    product: json["product"] == null ? null : BaseProduct.fromJson(json["product"] as Map<String, dynamic>),
    productId: json["product_id"] as String?,
    productTitle: json["product_title"] as String?,
    productDescription: json["product_description"] as String?,
    productSubtitle: json["product_subtitle"] as String?,
    productType: json["product_type"] as String?,
    productCollection: json["product_collection"] as String?,
    productHandle: json["product_handle"] as String?,
    variant: json["variant"] == null ? null : BaseProductVariant.fromJson(json["variant"] as Map<String, dynamic>),
    variantId: json["variant_id"] as String?,
    variantSku: json["variant_sku"] as String?,
    variantBarcode: json["variant_barcode"] as String?,
    variantTitle: json["variant_title"] as String?,
    variantOptionValues: json["variant_option_values"] as Map<String, dynamic>?,
    requiresShipping: json["requires_shipping"] as bool?,
    isDiscountable: json["is_discountable"] as bool?,
    isTaxInclusive: json["is_tax_inclusive"] as bool?,
    compareAtUnitPrice: json["compare_at_unit_price"] as num?,
    unitPrice: json["unit_price"] as num?,
    taxLines: (json["tax_lines"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    adjustments: (json["adjustments"] as List?)?.map((e) => e == null ? null : BaseLineItemAdjustment.fromJson(e as Map<String, dynamic>)).toList().cast<BaseLineItemAdjustment>(),
    cart: json["cart"] == null ? null : BaseCart.fromJson(json["cart"] as Map<String, dynamic>),
    cartId: json["cart_id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    originalTotal: json["original_total"] as num?,
    originalSubtotal: json["original_subtotal"] as num?,
    originalTaxTotal: json["original_tax_total"] as num?,
    itemTotal: json["item_total"] as num?,
    itemSubtotal: json["item_subtotal"] as num?,
    itemTaxTotal: json["item_tax_total"] as num?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    taxTotal: json["tax_total"] as num?,
    discountTotal: json["discount_total"] as num?,
    discountTaxTotal: json["discount_tax_total"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (title != null) "title": title!,
    if (subtitle != null) "subtitle": subtitle!,
    if (thumbnail != null) "thumbnail": thumbnail!,
    if (quantity != null) "quantity": quantity!,
    if (product != null) "product": product!.toJson(),
    if (productId != null) "product_id": productId!,
    if (productTitle != null) "product_title": productTitle!,
    if (productDescription != null) "product_description": productDescription!,
    if (productSubtitle != null) "product_subtitle": productSubtitle!,
    if (productType != null) "product_type": productType!,
    if (productCollection != null) "product_collection": productCollection!,
    if (productHandle != null) "product_handle": productHandle!,
    if (variant != null) "variant": variant!.toJson(),
    if (variantId != null) "variant_id": variantId!,
    if (variantSku != null) "variant_sku": variantSku!,
    if (variantBarcode != null) "variant_barcode": variantBarcode!,
    if (variantTitle != null) "variant_title": variantTitle!,
    if (variantOptionValues != null) "variant_option_values": variantOptionValues!,
    if (requiresShipping != null) "requires_shipping": requiresShipping!,
    if (isDiscountable != null) "is_discountable": isDiscountable!,
    if (isTaxInclusive != null) "is_tax_inclusive": isTaxInclusive!,
    if (compareAtUnitPrice != null) "compare_at_unit_price": compareAtUnitPrice!,
    if (unitPrice != null) "unit_price": unitPrice!,
    if (taxLines != null) "tax_lines": taxLines!.map((e) => e).toList(),
    if (adjustments != null) "adjustments": adjustments!.map((e) => e.toJson()).toList(),
    if (cart != null) "cart": cart!.toJson(),
    if (cartId != null) "cart_id": cartId!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (originalTotal != null) "original_total": originalTotal!,
    if (originalSubtotal != null) "original_subtotal": originalSubtotal!,
    if (originalTaxTotal != null) "original_tax_total": originalTaxTotal!,
    if (itemTotal != null) "item_total": itemTotal!,
    if (itemSubtotal != null) "item_subtotal": itemSubtotal!,
    if (itemTaxTotal != null) "item_tax_total": itemTaxTotal!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (taxTotal != null) "tax_total": taxTotal!,
    if (discountTotal != null) "discount_total": discountTotal!,
    if (discountTaxTotal != null) "discount_tax_total": discountTaxTotal!,
  };
}

/// A cart's shipping method.
class BaseCartShippingMethod {
  BaseCartShippingMethod({
    this.id,
    this.cartId,
    this.name,
    this.description,
    this.amount,
    this.isTaxInclusive,
    this.shippingOptionId,
    this.data,
    this.metadata,
    this.taxLines,
    this.adjustments,
    this.createdAt,
    this.updatedAt,
    this.originalTotal,
    this.originalSubtotal,
    this.originalTaxTotal,
    this.total,
    this.subtotal,
    this.taxTotal,
    this.discountTotal,
    this.discountTaxTotal,
  });

  /// The shipping method's ID.
  final String? id;
  /// The ID of the cart this shipping method belongs to.
  final String? cartId;
  /// The shipping method's name.
  final String? name;
  /// The shipping method's description.
  final String? description;
  /// The shipping method's amount.
  final num? amount;
  /// Whether the shipping method's amount is tax inclusive.
  final bool? isTaxInclusive;
  /// The ID of the shipping option this method was created from.
  final String? shippingOptionId;
  /// The shipping method's data, useful for fulfillment handling by third-party services.
  final Map<String, dynamic>? data;
  /// The shipping method's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The shipping method's tax lines.
  final List<BaseShippingMethodTaxLine>? taxLines;
  /// The shipping method's adjustments, such as applied promotions.
  final List<BaseShippingMethodAdjustment>? adjustments;
  /// The date the shipping method was created.
  final DateTime? createdAt;
  /// The date the shipping method was updated.
  final DateTime? updatedAt;
  /// The shipping method's original total before discounts, including taxes.
  final num? originalTotal;
  /// The shipping method's original subtotal before discounts, excluding taxes.
  final num? originalSubtotal;
  /// The shipping method's original tax total before discounts.
  final num? originalTaxTotal;
  /// The shipping method's total after discounts, including taxes.
  final num? total;
  /// The shipping method's subtotal before discounts, excluding taxes.
  final num? subtotal;
  /// The shipping method's tax total after discounts.
  final num? taxTotal;
  /// The total amount of discounts applied to the shipping method, including the tax portion of discounts.
  final num? discountTotal;
  /// The total amount of discounts applied to the shipping method's tax. Represents the tax portion of discounts.
  final num? discountTaxTotal;

  factory BaseCartShippingMethod.fromJson(Map<String, dynamic> json) => BaseCartShippingMethod(
    id: json["id"] as String?,
    cartId: json["cart_id"] as String?,
    name: json["name"] as String?,
    description: json["description"] as String?,
    amount: json["amount"] as num?,
    isTaxInclusive: json["is_tax_inclusive"] as bool?,
    shippingOptionId: json["shipping_option_id"] as String?,
    data: json["data"] as Map<String, dynamic>?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    taxLines: (json["tax_lines"] as List?)?.map((e) => e == null ? null : BaseShippingMethodTaxLine.fromJson(e as Map<String, dynamic>)).toList().cast<BaseShippingMethodTaxLine>(),
    adjustments: (json["adjustments"] as List?)?.map((e) => e == null ? null : BaseShippingMethodAdjustment.fromJson(e as Map<String, dynamic>)).toList().cast<BaseShippingMethodAdjustment>(),
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    originalTotal: json["original_total"] as num?,
    originalSubtotal: json["original_subtotal"] as num?,
    originalTaxTotal: json["original_tax_total"] as num?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    taxTotal: json["tax_total"] as num?,
    discountTotal: json["discount_total"] as num?,
    discountTaxTotal: json["discount_tax_total"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (cartId != null) "cart_id": cartId!,
    if (name != null) "name": name!,
    if (description != null) "description": description!,
    if (amount != null) "amount": amount!,
    if (isTaxInclusive != null) "is_tax_inclusive": isTaxInclusive!,
    if (shippingOptionId != null) "shipping_option_id": shippingOptionId!,
    if (data != null) "data": data!,
    if (metadata != null) "metadata": metadata!,
    if (taxLines != null) "tax_lines": taxLines!.map((e) => e.toJson()).toList(),
    if (adjustments != null) "adjustments": adjustments!.map((e) => e.toJson()).toList(),
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (originalTotal != null) "original_total": originalTotal!,
    if (originalSubtotal != null) "original_subtotal": originalSubtotal!,
    if (originalTaxTotal != null) "original_tax_total": originalTaxTotal!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (taxTotal != null) "tax_total": taxTotal!,
    if (discountTotal != null) "discount_total": discountTotal!,
    if (discountTaxTotal != null) "discount_tax_total": discountTaxTotal!,
  };
}

/// The claim item's details.
class BaseClaimItem {
  BaseClaimItem({
    this.id,
    this.claimId,
    this.orderId,
    this.itemId,
    this.quantity,
    this.reason,
    this.rawQuantity,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.item,
  });

  /// The claim item's ID.
  final String? id;
  /// The ID of the claim this item belongs to.
  final String? claimId;
  /// The ID of the order this item belongs to.
  final String? orderId;
  /// The ID of the item in the order.
  final String? itemId;
  /// The quantity claimed.
  final num? quantity;
  /// The claim's reason.
  final String? reason;
  /// The quantity claimed.
  final Map<String, dynamic>? rawQuantity;
  /// The item's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the claim was created.
  final DateTime? createdAt;
  /// The date the claim was updated.
  final DateTime? updatedAt;
  final AdminOrderLineItem? item;

  factory BaseClaimItem.fromJson(Map<String, dynamic> json) => BaseClaimItem(
    id: json["id"] as String?,
    claimId: json["claim_id"] as String?,
    orderId: json["order_id"] as String?,
    itemId: json["item_id"] as String?,
    quantity: json["quantity"] as num?,
    reason: json["reason"] as String?,
    rawQuantity: json["raw_quantity"] as Map<String, dynamic>?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    item: json["item"] == null ? null : AdminOrderLineItem.fromJson(json["item"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (claimId != null) "claim_id": claimId!,
    if (orderId != null) "order_id": orderId!,
    if (itemId != null) "item_id": itemId!,
    if (quantity != null) "quantity": quantity!,
    if (reason != null) "reason": reason!,
    if (rawQuantity != null) "raw_quantity": rawQuantity!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (item != null) "item": item!.toJson(),
  };
}

/// The product collection's details.
class BaseCollection {
  BaseCollection({
    this.id,
    this.title,
    this.handle,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.products,
    this.metadata,
    this.externalId,
  });

  /// The collection's ID.
  final String? id;
  /// The collection's title.
  final String? title;
  /// The collection's handle.
  final String? handle;
  /// The collection's creation date.
  final DateTime? createdAt;
  /// The collection's update date.
  final DateTime? updatedAt;
  /// The collection's deletion date.
  final DateTime? deletedAt;
  /// The collection's products.
  final List<Map<String, dynamic>>? products;
  /// The collection's metadata, used to store custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The collection's external ID used to link it with external systems.
  final String? externalId;

  factory BaseCollection.fromJson(Map<String, dynamic> json) => BaseCollection(
    id: json["id"] as String?,
    title: json["title"] as String?,
    handle: json["handle"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    products: (json["products"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    metadata: json["metadata"] as Map<String, dynamic>?,
    externalId: json["external_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (title != null) "title": title!,
    if (handle != null) "handle": handle!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (products != null) "products": products!.map((e) => e).toList(),
    if (metadata != null) "metadata": metadata!,
    if (externalId != null) "external_id": externalId!,
  };
}
