// GENERATED by tool/generate_models.mjs — do not edit.
part of '../models.dart';

/// The list of supported locales in the store.
class StoreLocaleListResponse {
  StoreLocaleListResponse({
    this.locales,
  });

  /// The list of supported locales in the store.
  final List<StoreLocale>? locales;

  factory StoreLocaleListResponse.fromJson(Map<String, dynamic> json) => StoreLocaleListResponse(
    locales: (json["locales"] as List?)?.map((e) => e == null ? null : StoreLocale.fromJson(e as Map<String, dynamic>)).toList().cast<StoreLocale>(),
  );

  Map<String, dynamic> toJson() => {
    if (locales != null) "locales": locales!.map((e) => e.toJson()).toList(),
  };
}

/// The order's details.
class StoreOrder {
  StoreOrder({
    this.id,
    this.regionId,
    this.customerId,
    this.salesChannelId,
    this.email,
    this.currencyCode,
    this.displayId,
    this.shippingAddress,
    this.billingAddress,
    this.items,
    this.shippingMethods,
    this.paymentCollections,
    this.paymentStatus,
    this.fulfillments,
    this.fulfillmentStatus,
    this.summary,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.originalItemTotal,
    this.originalItemSubtotal,
    this.originalItemTaxTotal,
    this.itemTotal,
    this.itemSubtotal,
    this.itemTaxTotal,
    this.originalTotal,
    this.originalSubtotal,
    this.originalTaxTotal,
    this.total,
    this.subtotal,
    this.taxTotal,
    this.discountTotal,
    this.discountTaxTotal,
    this.giftCardTotal,
    this.giftCardTaxTotal,
    this.shippingTotal,
    this.shippingSubtotal,
    this.shippingTaxTotal,
    this.originalShippingTotal,
    this.originalShippingSubtotal,
    this.originalShippingTaxTotal,
    this.customer,
    this.transactions,
    this.status,
    this.creditLineTotal,
    this.itemDiscountTotal,
    this.shippingDiscountTotal,
    this.customDisplayId,
  });

  /// The order's ID.
  final String? id;
  /// The ID of the associated region.
  final String? regionId;
  /// The ID of the customer that placed the order.
  final String? customerId;
  /// The ID of the sales channel the order was placed in.
  final String? salesChannelId;
  /// The email of the customer that placed the order.
  final String? email;
  /// The order's currency code.
  final String? currencyCode;
  /// The order's display ID.
  final num? displayId;
  final StoreOrderAddress? shippingAddress;
  final StoreOrderAddress? billingAddress;
  /// The order's items.
  final List<StoreOrderLineItem>? items;
  /// The order's shipping methods.
  final List<StoreOrderShippingMethod>? shippingMethods;
  /// The order's payment collections.
  final List<StorePaymentCollection>? paymentCollections;
  /// The order's payment status.
  final String? paymentStatus;
  /// The order's fulfillments.
  final List<StoreOrderFulfillment>? fulfillments;
  /// The order's fulfillment status.
  final String? fulfillmentStatus;
  final BaseOrderSummary? summary;
  /// The order's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the order was created.
  final DateTime? createdAt;
  /// The date the order was updated.
  final DateTime? updatedAt;
  /// The sum of all line items' original totals before discounts, including taxes.
  final num? originalItemTotal;
  /// The sum of all line items' original subtotals before discounts, excluding taxes.
  final num? originalItemSubtotal;
  /// The sum of all line items' original tax totals before discounts.
  final num? originalItemTaxTotal;
  /// The sum of all line items' totals after discounts, including taxes.
  final num? itemTotal;
  /// The sum of all line items' subtotals before discounts, excluding taxes.
  final num? itemSubtotal;
  /// The sum of all line items' tax totals after discounts.
  final num? itemTaxTotal;
  /// The order's total before discounts, including taxes. Calculated as the sum of `original_item_total` and `original_shipping_total`.
  final num? originalTotal;
  /// The order's subtotal before discounts, excluding taxes. Calculated as the sum of `original_item_subtotal` and `original_shipping_subtotal`.
  final num? originalSubtotal;
  /// The order's tax total before discounts. Calculated as the sum of `original_item_tax_total` and `original_shipping_tax_total`.
  final num? originalTaxTotal;
  /// The order's final total after discounts and credit lines, including taxes.
  final num? total;
  /// The order's subtotal before discounts, excluding taxes. Calculated as the sum of `item_subtotal` and `shipping_subtotal`.
  final num? subtotal;
  /// The order's tax total after discounts. Calculated as the sum of `item_tax_total` and `shipping_tax_total`.
  final num? taxTotal;
  /// The total amount of discounts applied to the order, including the tax portion of discounts.
  final num? discountTotal;
  /// The total amount of discounts applied to the order's tax. Represents the tax portion of discounts.
  final num? discountTaxTotal;
  /// The order's gift card total.
  final num? giftCardTotal;
  /// The total taxes applied on the gift card's amount.
  final num? giftCardTaxTotal;
  /// The sum of all shipping methods' totals after discounts, including taxes.
  final num? shippingTotal;
  /// The sum of all shipping methods' subtotals before discounts, excluding taxes.
  final num? shippingSubtotal;
  /// The sum of all shipping methods' tax totals after discounts.
  final num? shippingTaxTotal;
  /// The sum of all shipping methods' original totals before discounts, including taxes.
  final num? originalShippingTotal;
  /// The sum of all shipping methods' original subtotals before discounts, excluding taxes.
  final num? originalShippingSubtotal;
  /// The sum of all shipping methods' original tax totals before discounts.
  final num? originalShippingTaxTotal;
  final StoreCustomer? customer;
  /// The order's transactions.
  final List<BaseOrderTransaction>? transactions;
  /// The order's status.
  final String? status;
  /// The total amount of credit lines applied to the order, including taxes. Subtracted from the final total.
  final num? creditLineTotal;
  /// The sum of all discounts applied to line items, including the tax portion of discounts.
  final num? itemDiscountTotal;
  /// The sum of all discounts applied to shipping methods, including the tax portion of discounts.
  final num? shippingDiscountTotal;
  /// The custom display ID of the order.
  final String? customDisplayId;

  factory StoreOrder.fromJson(Map<String, dynamic> json) => StoreOrder(
    id: json["id"] as String?,
    regionId: json["region_id"] as String?,
    customerId: json["customer_id"] as String?,
    salesChannelId: json["sales_channel_id"] as String?,
    email: json["email"] as String?,
    currencyCode: json["currency_code"] as String?,
    displayId: json["display_id"] as num?,
    shippingAddress: json["shipping_address"] == null ? null : StoreOrderAddress.fromJson(json["shipping_address"] as Map<String, dynamic>),
    billingAddress: json["billing_address"] == null ? null : StoreOrderAddress.fromJson(json["billing_address"] as Map<String, dynamic>),
    items: (json["items"] as List?)?.map((e) => e == null ? null : StoreOrderLineItem.fromJson(e as Map<String, dynamic>)).toList().cast<StoreOrderLineItem>(),
    shippingMethods: (json["shipping_methods"] as List?)?.map((e) => e == null ? null : StoreOrderShippingMethod.fromJson(e as Map<String, dynamic>)).toList().cast<StoreOrderShippingMethod>(),
    paymentCollections: (json["payment_collections"] as List?)?.map((e) => e == null ? null : StorePaymentCollection.fromJson(e as Map<String, dynamic>)).toList().cast<StorePaymentCollection>(),
    paymentStatus: json["payment_status"] as String?,
    fulfillments: (json["fulfillments"] as List?)?.map((e) => e == null ? null : StoreOrderFulfillment.fromJson(e as Map<String, dynamic>)).toList().cast<StoreOrderFulfillment>(),
    fulfillmentStatus: json["fulfillment_status"] as String?,
    summary: json["summary"] == null ? null : BaseOrderSummary.fromJson(json["summary"] as Map<String, dynamic>),
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    originalItemTotal: json["original_item_total"] as num?,
    originalItemSubtotal: json["original_item_subtotal"] as num?,
    originalItemTaxTotal: json["original_item_tax_total"] as num?,
    itemTotal: json["item_total"] as num?,
    itemSubtotal: json["item_subtotal"] as num?,
    itemTaxTotal: json["item_tax_total"] as num?,
    originalTotal: json["original_total"] as num?,
    originalSubtotal: json["original_subtotal"] as num?,
    originalTaxTotal: json["original_tax_total"] as num?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    taxTotal: json["tax_total"] as num?,
    discountTotal: json["discount_total"] as num?,
    discountTaxTotal: json["discount_tax_total"] as num?,
    giftCardTotal: json["gift_card_total"] as num?,
    giftCardTaxTotal: json["gift_card_tax_total"] as num?,
    shippingTotal: json["shipping_total"] as num?,
    shippingSubtotal: json["shipping_subtotal"] as num?,
    shippingTaxTotal: json["shipping_tax_total"] as num?,
    originalShippingTotal: json["original_shipping_total"] as num?,
    originalShippingSubtotal: json["original_shipping_subtotal"] as num?,
    originalShippingTaxTotal: json["original_shipping_tax_total"] as num?,
    customer: json["customer"] == null ? null : StoreCustomer.fromJson(json["customer"] as Map<String, dynamic>),
    transactions: (json["transactions"] as List?)?.map((e) => e == null ? null : BaseOrderTransaction.fromJson(e as Map<String, dynamic>)).toList().cast<BaseOrderTransaction>(),
    status: json["status"] as String?,
    creditLineTotal: json["credit_line_total"] as num?,
    itemDiscountTotal: json["item_discount_total"] as num?,
    shippingDiscountTotal: json["shipping_discount_total"] as num?,
    customDisplayId: json["custom_display_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (regionId != null) "region_id": regionId!,
    if (customerId != null) "customer_id": customerId!,
    if (salesChannelId != null) "sales_channel_id": salesChannelId!,
    if (email != null) "email": email!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (displayId != null) "display_id": displayId!,
    if (shippingAddress != null) "shipping_address": shippingAddress!.toJson(),
    if (billingAddress != null) "billing_address": billingAddress!.toJson(),
    if (items != null) "items": items!.map((e) => e.toJson()).toList(),
    if (shippingMethods != null) "shipping_methods": shippingMethods!.map((e) => e.toJson()).toList(),
    if (paymentCollections != null) "payment_collections": paymentCollections!.map((e) => e.toJson()).toList(),
    if (paymentStatus != null) "payment_status": paymentStatus!,
    if (fulfillments != null) "fulfillments": fulfillments!.map((e) => e.toJson()).toList(),
    if (fulfillmentStatus != null) "fulfillment_status": fulfillmentStatus!,
    if (summary != null) "summary": summary!.toJson(),
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (originalItemTotal != null) "original_item_total": originalItemTotal!,
    if (originalItemSubtotal != null) "original_item_subtotal": originalItemSubtotal!,
    if (originalItemTaxTotal != null) "original_item_tax_total": originalItemTaxTotal!,
    if (itemTotal != null) "item_total": itemTotal!,
    if (itemSubtotal != null) "item_subtotal": itemSubtotal!,
    if (itemTaxTotal != null) "item_tax_total": itemTaxTotal!,
    if (originalTotal != null) "original_total": originalTotal!,
    if (originalSubtotal != null) "original_subtotal": originalSubtotal!,
    if (originalTaxTotal != null) "original_tax_total": originalTaxTotal!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (taxTotal != null) "tax_total": taxTotal!,
    if (discountTotal != null) "discount_total": discountTotal!,
    if (discountTaxTotal != null) "discount_tax_total": discountTaxTotal!,
    if (giftCardTotal != null) "gift_card_total": giftCardTotal!,
    if (giftCardTaxTotal != null) "gift_card_tax_total": giftCardTaxTotal!,
    if (shippingTotal != null) "shipping_total": shippingTotal!,
    if (shippingSubtotal != null) "shipping_subtotal": shippingSubtotal!,
    if (shippingTaxTotal != null) "shipping_tax_total": shippingTaxTotal!,
    if (originalShippingTotal != null) "original_shipping_total": originalShippingTotal!,
    if (originalShippingSubtotal != null) "original_shipping_subtotal": originalShippingSubtotal!,
    if (originalShippingTaxTotal != null) "original_shipping_tax_total": originalShippingTaxTotal!,
    if (customer != null) "customer": customer!.toJson(),
    if (transactions != null) "transactions": transactions!.map((e) => e.toJson()).toList(),
    if (status != null) "status": status!,
    if (creditLineTotal != null) "credit_line_total": creditLineTotal!,
    if (itemDiscountTotal != null) "item_discount_total": itemDiscountTotal!,
    if (shippingDiscountTotal != null) "shipping_discount_total": shippingDiscountTotal!,
    if (customDisplayId != null) "custom_display_id": customDisplayId!,
  };
}

/// An order address
class StoreOrderAddress {
  StoreOrderAddress({
    this.id,
    this.customerId,
    this.firstName,
    this.lastName,
    this.phone,
    this.company,
    this.address1,
    this.address2,
    this.city,
    this.countryCode,
    this.country,
    this.province,
    this.postalCode,
    this.metadata,
    this.createdAt,
    this.updatedAt,
  });

  /// The address's ID.
  final String? id;
  /// The ID of the customer this address belongs to.
  final String? customerId;
  /// The address's first name.
  final String? firstName;
  /// The address's last name.
  final String? lastName;
  /// The address's phone.
  final String? phone;
  /// The address's company.
  final String? company;
  /// The address's first line.
  final String? address1;
  /// The address's second line.
  final String? address2;
  /// The address's city.
  final String? city;
  /// The address's country code.
  final String? countryCode;
  final StoreRegionCountry? country;
  /// The address's lower-case ISO 3166-2 province code.
  final String? province;
  /// The address's postal code.
  final String? postalCode;
  /// The address's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the address was created.
  final DateTime? createdAt;
  /// The date the address was updated.
  final DateTime? updatedAt;

  factory StoreOrderAddress.fromJson(Map<String, dynamic> json) => StoreOrderAddress(
    id: json["id"] as String?,
    customerId: json["customer_id"] as String?,
    firstName: json["first_name"] as String?,
    lastName: json["last_name"] as String?,
    phone: json["phone"] as String?,
    company: json["company"] as String?,
    address1: json["address_1"] as String?,
    address2: json["address_2"] as String?,
    city: json["city"] as String?,
    countryCode: json["country_code"] as String?,
    country: json["country"] == null ? null : StoreRegionCountry.fromJson(json["country"] as Map<String, dynamic>),
    province: json["province"] as String?,
    postalCode: json["postal_code"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (customerId != null) "customer_id": customerId!,
    if (firstName != null) "first_name": firstName!,
    if (lastName != null) "last_name": lastName!,
    if (phone != null) "phone": phone!,
    if (company != null) "company": company!,
    if (address1 != null) "address_1": address1!,
    if (address2 != null) "address_2": address2!,
    if (city != null) "city": city!,
    if (countryCode != null) "country_code": countryCode!,
    if (country != null) "country": country!.toJson(),
    if (province != null) "province": province!,
    if (postalCode != null) "postal_code": postalCode!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The fulfillment's details.
class StoreOrderFulfillment {
  StoreOrderFulfillment({
    this.id,
    this.locationId,
    this.packedAt,
    this.shippedAt,
    this.deliveredAt,
    this.canceledAt,
    this.data,
    this.providerId,
    this.shippingOptionId,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.requiresShipping,
    this.createdBy,
    this.markedShippedBy,
  });

  /// The fulfillment's ID.
  final String? id;
  /// The ID of the location the items are fulfilled from.
  final String? locationId;
  /// The date the items were packed.
  final DateTime? packedAt;
  /// The date the items were shipped.
  final DateTime? shippedAt;
  /// The date the items were delivered.
  final DateTime? deliveredAt;
  /// The date the items were canceled.
  final DateTime? canceledAt;
  /// Data useful for the fulfillment provider handling it. This is taken from the associated shipping option.
  final Map<String, dynamic>? data;
  /// The ID of the fulfillment provider used to handle the fulfillment.
  final String? providerId;
  /// The ID of the associated shipping option.
  final String? shippingOptionId;
  /// The fulfillment's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the fulfillment was created.
  final DateTime? createdAt;
  /// The date the fulfillment was updated.
  final DateTime? updatedAt;
  /// The fulfillment's requires shipping.
  final bool? requiresShipping;
  /// The ID of the user that created the fulfillment.
  final String? createdBy;
  /// The ID of the user that marked the fulfillment as shipped.
  final String? markedShippedBy;

  factory StoreOrderFulfillment.fromJson(Map<String, dynamic> json) => StoreOrderFulfillment(
    id: json["id"] as String?,
    locationId: json["location_id"] as String?,
    packedAt: json["packed_at"] == null ? null : DateTime.parse(json["packed_at"] as String),
    shippedAt: json["shipped_at"] == null ? null : DateTime.parse(json["shipped_at"] as String),
    deliveredAt: json["delivered_at"] == null ? null : DateTime.parse(json["delivered_at"] as String),
    canceledAt: json["canceled_at"] == null ? null : DateTime.parse(json["canceled_at"] as String),
    data: json["data"] as Map<String, dynamic>?,
    providerId: json["provider_id"] as String?,
    shippingOptionId: json["shipping_option_id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    requiresShipping: json["requires_shipping"] as bool?,
    createdBy: json["created_by"] as String?,
    markedShippedBy: json["marked_shipped_by"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (locationId != null) "location_id": locationId!,
    if (packedAt != null) "packed_at": packedAt!.toIso8601String(),
    if (shippedAt != null) "shipped_at": shippedAt!.toIso8601String(),
    if (deliveredAt != null) "delivered_at": deliveredAt!.toIso8601String(),
    if (canceledAt != null) "canceled_at": canceledAt!.toIso8601String(),
    if (data != null) "data": data!,
    if (providerId != null) "provider_id": providerId!,
    if (shippingOptionId != null) "shipping_option_id": shippingOptionId!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (requiresShipping != null) "requires_shipping": requiresShipping!,
    if (createdBy != null) "created_by": createdBy!,
    if (markedShippedBy != null) "marked_shipped_by": markedShippedBy!,
  };
}

/// The item's details.
class StoreOrderLineItem {
  StoreOrderLineItem({
    this.variant,
    this.product,
    this.taxLines,
    this.adjustments,
    this.detail,
    this.title,
    this.id,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.originalTotal,
    this.originalSubtotal,
    this.originalTaxTotal,
    this.itemTotal,
    this.itemSubtotal,
    this.itemTaxTotal,
    this.total,
    this.subtotal,
    this.taxTotal,
    this.discountTotal,
    this.discountTaxTotal,
    this.refundableTotal,
    this.refundableTotalPerUnit,
    this.subtitle,
    this.thumbnail,
    this.variantId,
    this.productId,
    this.productTitle,
    this.productDescription,
    this.productSubtitle,
    this.productType,
    this.productCollection,
    this.productHandle,
    this.variantSku,
    this.variantBarcode,
    this.variantTitle,
    this.variantOptionValues,
    this.requiresShipping,
    this.isDiscountable,
    this.isTaxInclusive,
    this.compareAtUnitPrice,
    this.unitPrice,
    this.quantity,
    this.productTypeId,
  });

  final StoreProductVariant? variant;
  final StoreProduct? product;
  /// The item's tax lines.
  final List<Map<String, dynamic>>? taxLines;
  /// The item's adjustments.
  final List<Map<String, dynamic>>? adjustments;
  final Map<String, dynamic>? detail;
  /// The item's title.
  final String? title;
  /// The item's ID.
  final String? id;
  /// Custom key-value pairs of the item.
  final Map<String, dynamic>? metadata;
  /// The date the item was created.
  final DateTime? createdAt;
  /// The date the item was updated.
  final DateTime? updatedAt;
  /// The item's total including taxes, excluding promotions.
  final num? originalTotal;
  /// The item's total excluding taxes, including promotions.
  final num? originalSubtotal;
  /// The total taxes of the item, excluding promotions.
  final num? originalTaxTotal;
  /// The total taxes of the item, including promotions.
  final num? itemTotal;
  /// The item's total excluding taxes, including promotions.
  final num? itemSubtotal;
  /// The total taxes of the item, including promotions.
  final num? itemTaxTotal;
  /// The item's total, including taxes and promotions.
  final num? total;
  /// The item's subtotal excluding taxes, including promotions.
  final num? subtotal;
  /// The tax total of the item including promotions.
  final num? taxTotal;
  /// The total discount amount of the item.
  final num? discountTotal;
  /// The total taxes applied on the discounted amount.
  final num? discountTaxTotal;
  /// The total refundable amount of the item's total.
  final num? refundableTotal;
  /// The total refundable amount of the item's total for a single quantity.
  final num? refundableTotalPerUnit;
  /// The item's subtitle.
  final String? subtitle;
  /// The item's thumbnail.
  final String? thumbnail;
  /// The ID of the associated variant.
  final String? variantId;
  /// The ID of the associated product.
  final String? productId;
  /// The title of the associated product.
  final String? productTitle;
  /// The description of the associated product.
  final String? productDescription;
  /// The subtitle of the associated product.
  final String? productSubtitle;
  /// The type of the associated product.
  final String? productType;
  /// The ID of the associated product's collection.
  final String? productCollection;
  /// The handle of the associated product.
  final String? productHandle;
  /// The SKU of the associated variant.
  final String? variantSku;
  /// The barcode of the associated variant.
  final String? variantBarcode;
  /// The title of the associated variant.
  final String? variantTitle;
  /// The option values of the associated variant, where the key is the option name and the value is the option value.
  final Map<String, dynamic>? variantOptionValues;
  /// Whether the associated variant requires shipping.
  final bool? requiresShipping;
  /// Whether the associated variant is discountable.
  final bool? isDiscountable;
  /// Whether the associated variant's price includes taxes.
  final bool? isTaxInclusive;
  /// The item's compare at unit price.
  final num? compareAtUnitPrice;
  /// The price of a single quantity of the item.
  final num? unitPrice;
  /// The item's quantity.
  final num? quantity;
  /// The ID of the associated product's type.
  final String? productTypeId;

  factory StoreOrderLineItem.fromJson(Map<String, dynamic> json) => StoreOrderLineItem(
    variant: json["variant"] == null ? null : StoreProductVariant.fromJson(json["variant"] as Map<String, dynamic>),
    product: json["product"] == null ? null : StoreProduct.fromJson(json["product"] as Map<String, dynamic>),
    taxLines: (json["tax_lines"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    adjustments: (json["adjustments"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    detail: json["detail"] as Map<String, dynamic>?,
    title: json["title"] as String?,
    id: json["id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    originalTotal: json["original_total"] as num?,
    originalSubtotal: json["original_subtotal"] as num?,
    originalTaxTotal: json["original_tax_total"] as num?,
    itemTotal: json["item_total"] as num?,
    itemSubtotal: json["item_subtotal"] as num?,
    itemTaxTotal: json["item_tax_total"] as num?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    taxTotal: json["tax_total"] as num?,
    discountTotal: json["discount_total"] as num?,
    discountTaxTotal: json["discount_tax_total"] as num?,
    refundableTotal: json["refundable_total"] as num?,
    refundableTotalPerUnit: json["refundable_total_per_unit"] as num?,
    subtitle: json["subtitle"] as String?,
    thumbnail: json["thumbnail"] as String?,
    variantId: json["variant_id"] as String?,
    productId: json["product_id"] as String?,
    productTitle: json["product_title"] as String?,
    productDescription: json["product_description"] as String?,
    productSubtitle: json["product_subtitle"] as String?,
    productType: json["product_type"] as String?,
    productCollection: json["product_collection"] as String?,
    productHandle: json["product_handle"] as String?,
    variantSku: json["variant_sku"] as String?,
    variantBarcode: json["variant_barcode"] as String?,
    variantTitle: json["variant_title"] as String?,
    variantOptionValues: json["variant_option_values"] as Map<String, dynamic>?,
    requiresShipping: json["requires_shipping"] as bool?,
    isDiscountable: json["is_discountable"] as bool?,
    isTaxInclusive: json["is_tax_inclusive"] as bool?,
    compareAtUnitPrice: json["compare_at_unit_price"] as num?,
    unitPrice: json["unit_price"] as num?,
    quantity: json["quantity"] as num?,
    productTypeId: json["product_type_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (variant != null) "variant": variant!.toJson(),
    if (product != null) "product": product!.toJson(),
    if (taxLines != null) "tax_lines": taxLines!.map((e) => e).toList(),
    if (adjustments != null) "adjustments": adjustments!.map((e) => e).toList(),
    if (detail != null) "detail": detail!,
    if (title != null) "title": title!,
    if (id != null) "id": id!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (originalTotal != null) "original_total": originalTotal!,
    if (originalSubtotal != null) "original_subtotal": originalSubtotal!,
    if (originalTaxTotal != null) "original_tax_total": originalTaxTotal!,
    if (itemTotal != null) "item_total": itemTotal!,
    if (itemSubtotal != null) "item_subtotal": itemSubtotal!,
    if (itemTaxTotal != null) "item_tax_total": itemTaxTotal!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (taxTotal != null) "tax_total": taxTotal!,
    if (discountTotal != null) "discount_total": discountTotal!,
    if (discountTaxTotal != null) "discount_tax_total": discountTaxTotal!,
    if (refundableTotal != null) "refundable_total": refundableTotal!,
    if (refundableTotalPerUnit != null) "refundable_total_per_unit": refundableTotalPerUnit!,
    if (subtitle != null) "subtitle": subtitle!,
    if (thumbnail != null) "thumbnail": thumbnail!,
    if (variantId != null) "variant_id": variantId!,
    if (productId != null) "product_id": productId!,
    if (productTitle != null) "product_title": productTitle!,
    if (productDescription != null) "product_description": productDescription!,
    if (productSubtitle != null) "product_subtitle": productSubtitle!,
    if (productType != null) "product_type": productType!,
    if (productCollection != null) "product_collection": productCollection!,
    if (productHandle != null) "product_handle": productHandle!,
    if (variantSku != null) "variant_sku": variantSku!,
    if (variantBarcode != null) "variant_barcode": variantBarcode!,
    if (variantTitle != null) "variant_title": variantTitle!,
    if (variantOptionValues != null) "variant_option_values": variantOptionValues!,
    if (requiresShipping != null) "requires_shipping": requiresShipping!,
    if (isDiscountable != null) "is_discountable": isDiscountable!,
    if (isTaxInclusive != null) "is_tax_inclusive": isTaxInclusive!,
    if (compareAtUnitPrice != null) "compare_at_unit_price": compareAtUnitPrice!,
    if (unitPrice != null) "unit_price": unitPrice!,
    if (quantity != null) "quantity": quantity!,
    if (productTypeId != null) "product_type_id": productTypeId!,
  };
}

/// The order's details.
class StoreOrderResponse {
  StoreOrderResponse({
    this.order,
  });

  final StoreOrder? order;

  factory StoreOrderResponse.fromJson(Map<String, dynamic> json) => StoreOrderResponse(
    order: json["order"] == null ? null : StoreOrder.fromJson(json["order"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (order != null) "order": order!.toJson(),
  };
}

/// The shipping method's details.
class StoreOrderShippingMethod {
  StoreOrderShippingMethod({
    this.taxLines,
    this.adjustments,
    this.detail,
    this.id,
    this.orderId,
    this.name,
    this.description,
    this.amount,
    this.isTaxInclusive,
    this.shippingOptionId,
    this.data,
    this.metadata,
    this.originalTotal,
    this.originalSubtotal,
    this.originalTaxTotal,
    this.total,
    this.subtotal,
    this.taxTotal,
    this.discountTotal,
    this.discountTaxTotal,
    this.createdAt,
    this.updatedAt,
  });

  /// The shipping method's tax lines.
  final List<Map<String, dynamic>>? taxLines;
  /// The shipping method's adjustments.
  final List<Map<String, dynamic>>? adjustments;
  final Map<String, dynamic>? detail;
  /// The shipping method's ID.
  final String? id;
  /// The ID of the order the shipping method belongs to.
  final String? orderId;
  /// The shipping method's name.
  final String? name;
  /// The shipping method's description.
  final String? description;
  /// The shipping method's amount.
  final num? amount;
  /// Whether the shipping method's amount includes applied taxes.
  final bool? isTaxInclusive;
  /// The ID of the shipping option this method was created from.
  final String? shippingOptionId;
  /// The shipping method's data, useful for fulfillment provider handling its fulfillment.
  final Map<String, dynamic>? data;
  /// The shipping method's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The shipping method's total including taxes, excluding promotions.
  final num? originalTotal;
  /// The shipping method's total excluding taxes, including promotions.
  final num? originalSubtotal;
  /// The shipping method's total taxes excluding promotions.
  final num? originalTaxTotal;
  /// The shipping method's total including taxes and promotions.
  final num? total;
  /// The shipping method's total excluding taxes, including promotions.
  final num? subtotal;
  /// The shipping method's tax total including promotions.
  final num? taxTotal;
  /// The total discounts applied on the shipping method.
  final num? discountTotal;
  /// The taxes applied on the discount amount.
  final num? discountTaxTotal;
  /// The date the shipping method was created.
  final DateTime? createdAt;
  /// The date the shipping method was updated.
  final DateTime? updatedAt;

  factory StoreOrderShippingMethod.fromJson(Map<String, dynamic> json) => StoreOrderShippingMethod(
    taxLines: (json["tax_lines"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    adjustments: (json["adjustments"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    detail: json["detail"] as Map<String, dynamic>?,
    id: json["id"] as String?,
    orderId: json["order_id"] as String?,
    name: json["name"] as String?,
    description: json["description"] as String?,
    amount: json["amount"] as num?,
    isTaxInclusive: json["is_tax_inclusive"] as bool?,
    shippingOptionId: json["shipping_option_id"] as String?,
    data: json["data"] as Map<String, dynamic>?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    originalTotal: json["original_total"] as num?,
    originalSubtotal: json["original_subtotal"] as num?,
    originalTaxTotal: json["original_tax_total"] as num?,
    total: json["total"] as num?,
    subtotal: json["subtotal"] as num?,
    taxTotal: json["tax_total"] as num?,
    discountTotal: json["discount_total"] as num?,
    discountTaxTotal: json["discount_tax_total"] as num?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (taxLines != null) "tax_lines": taxLines!.map((e) => e).toList(),
    if (adjustments != null) "adjustments": adjustments!.map((e) => e).toList(),
    if (detail != null) "detail": detail!,
    if (id != null) "id": id!,
    if (orderId != null) "order_id": orderId!,
    if (name != null) "name": name!,
    if (description != null) "description": description!,
    if (amount != null) "amount": amount!,
    if (isTaxInclusive != null) "is_tax_inclusive": isTaxInclusive!,
    if (shippingOptionId != null) "shipping_option_id": shippingOptionId!,
    if (data != null) "data": data!,
    if (metadata != null) "metadata": metadata!,
    if (originalTotal != null) "original_total": originalTotal!,
    if (originalSubtotal != null) "original_subtotal": originalSubtotal!,
    if (originalTaxTotal != null) "original_tax_total": originalTaxTotal!,
    if (total != null) "total": total!,
    if (subtotal != null) "subtotal": subtotal!,
    if (taxTotal != null) "tax_total": taxTotal!,
    if (discountTotal != null) "discount_total": discountTotal!,
    if (discountTaxTotal != null) "discount_tax_total": discountTaxTotal!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The payment collection's details.
class StorePaymentCollection {
  StorePaymentCollection({
    this.id,
    this.currencyCode,
    this.amount,
    this.authorizedAmount,
    this.capturedAmount,
    this.refundedAmount,
    this.completedAt,
    this.createdAt,
    this.updatedAt,
    this.metadata,
    this.status,
    this.paymentProviders,
    this.paymentSessions,
    this.payments,
  });

  /// The payment collection's ID.
  final String? id;
  /// The payment collection's currency code.
  final String? currencyCode;
  /// The total amount to be paid.
  final num? amount;
  /// The total authorized amount of the collection's payments.
  final num? authorizedAmount;
  /// The total captured amount of the collection's payments.
  final num? capturedAmount;
  /// The total refunded amount of the collection's payments.
  final num? refundedAmount;
  /// The date the payment collection was completed.
  final DateTime? completedAt;
  /// The date the payment collection was created.
  final DateTime? createdAt;
  /// The date the payment collection was updated.
  final DateTime? updatedAt;
  /// The payment collection's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The payment collection's status.
  final String? status;
  /// The payment provider used to process the collection's payments and sessions.
  final List<StorePaymentProvider>? paymentProviders;
  /// The payment collection's payment sessions.
  final List<StorePaymentSession>? paymentSessions;
  /// The payment collection's payments.
  final List<BasePayment>? payments;

  factory StorePaymentCollection.fromJson(Map<String, dynamic> json) => StorePaymentCollection(
    id: json["id"] as String?,
    currencyCode: json["currency_code"] as String?,
    amount: json["amount"] as num?,
    authorizedAmount: json["authorized_amount"] as num?,
    capturedAmount: json["captured_amount"] as num?,
    refundedAmount: json["refunded_amount"] as num?,
    completedAt: json["completed_at"] == null ? null : DateTime.parse(json["completed_at"] as String),
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    metadata: json["metadata"] as Map<String, dynamic>?,
    status: json["status"] as String?,
    paymentProviders: (json["payment_providers"] as List?)?.map((e) => e == null ? null : StorePaymentProvider.fromJson(e as Map<String, dynamic>)).toList().cast<StorePaymentProvider>(),
    paymentSessions: (json["payment_sessions"] as List?)?.map((e) => e == null ? null : StorePaymentSession.fromJson(e as Map<String, dynamic>)).toList().cast<StorePaymentSession>(),
    payments: (json["payments"] as List?)?.map((e) => e == null ? null : BasePayment.fromJson(e as Map<String, dynamic>)).toList().cast<BasePayment>(),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (amount != null) "amount": amount!,
    if (authorizedAmount != null) "authorized_amount": authorizedAmount!,
    if (capturedAmount != null) "captured_amount": capturedAmount!,
    if (refundedAmount != null) "refunded_amount": refundedAmount!,
    if (completedAt != null) "completed_at": completedAt!.toIso8601String(),
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (metadata != null) "metadata": metadata!,
    if (status != null) "status": status!,
    if (paymentProviders != null) "payment_providers": paymentProviders!.map((e) => e.toJson()).toList(),
    if (paymentSessions != null) "payment_sessions": paymentSessions!.map((e) => e.toJson()).toList(),
    if (payments != null) "payments": payments!.map((e) => e.toJson()).toList(),
  };
}

/// The payment collection's details.
class StorePaymentCollectionResponse {
  StorePaymentCollectionResponse({
    this.paymentCollection,
  });

  final StorePaymentCollection? paymentCollection;

  factory StorePaymentCollectionResponse.fromJson(Map<String, dynamic> json) => StorePaymentCollectionResponse(
    paymentCollection: json["payment_collection"] == null ? null : StorePaymentCollection.fromJson(json["payment_collection"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (paymentCollection != null) "payment_collection": paymentCollection!.toJson(),
  };
}

/// The payment provider's details
class StorePaymentProvider {
  StorePaymentProvider({
    this.id,
  });

  /// The payment provider's ID.
  final String? id;

  factory StorePaymentProvider.fromJson(Map<String, dynamic> json) => StorePaymentProvider(
    id: json["id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
  };
}

/// The payment session's details.
class StorePaymentSession {
  StorePaymentSession({
    this.id,
    this.amount,
    this.currencyCode,
    this.providerId,
    this.data,
    this.context,
    this.status,
    this.authorizedAt,
    this.paymentCollection,
    this.payment,
  });

  /// The payment session's ID.
  final String? id;
  /// The payment session's amount.
  final num? amount;
  /// The payment session's currency code.
  final String? currencyCode;
  /// The ID of the payment provider processing this session.
  final String? providerId;
  /// The payment session's data, useful for the payment provider processing the payment.
  final Map<String, dynamic>? data;
  /// The context around the payment, such as the customer's details.
  final Map<String, dynamic>? context;
  /// The payment session's status.
  final String? status;
  /// The date the payment session was authorized.
  final DateTime? authorizedAt;
  final Map<String, dynamic>? paymentCollection;
  final BasePayment? payment;

  factory StorePaymentSession.fromJson(Map<String, dynamic> json) => StorePaymentSession(
    id: json["id"] as String?,
    amount: json["amount"] as num?,
    currencyCode: json["currency_code"] as String?,
    providerId: json["provider_id"] as String?,
    data: json["data"] as Map<String, dynamic>?,
    context: json["context"] as Map<String, dynamic>?,
    status: json["status"] as String?,
    authorizedAt: json["authorized_at"] == null ? null : DateTime.parse(json["authorized_at"] as String),
    paymentCollection: json["payment_collection"] as Map<String, dynamic>?,
    payment: json["payment"] == null ? null : BasePayment.fromJson(json["payment"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (amount != null) "amount": amount!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (providerId != null) "provider_id": providerId!,
    if (data != null) "data": data!,
    if (context != null) "context": context!,
    if (status != null) "status": status!,
    if (authorizedAt != null) "authorized_at": authorizedAt!.toIso8601String(),
    if (paymentCollection != null) "payment_collection": paymentCollection!,
    if (payment != null) "payment": payment!.toJson(),
  };
}

/// The price's details.
class StorePrice {
  StorePrice({
    this.id,
    this.currencyCode,
    this.amount,
    this.minQuantity,
    this.maxQuantity,
    this.priceRules,
  });

  /// The price's ID.
  final String? id;
  /// The price's ISO 2 currency code.
  final String? currencyCode;
  /// The price's amount.
  final num? amount;
  /// The minimum quantity required in the cart for the price to apply.
  final num? minQuantity;
  /// The maximum quantity required in the cart for the price to apply.
  final num? maxQuantity;
  /// The price's rules.
  final List<StorePriceRule>? priceRules;

  factory StorePrice.fromJson(Map<String, dynamic> json) => StorePrice(
    id: json["id"] as String?,
    currencyCode: json["currency_code"] as String?,
    amount: json["amount"] as num?,
    minQuantity: json["min_quantity"] as num?,
    maxQuantity: json["max_quantity"] as num?,
    priceRules: (json["price_rules"] as List?)?.map((e) => e == null ? null : StorePriceRule.fromJson(e as Map<String, dynamic>)).toList().cast<StorePriceRule>(),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (amount != null) "amount": amount!,
    if (minQuantity != null) "min_quantity": minQuantity!,
    if (maxQuantity != null) "max_quantity": maxQuantity!,
    if (priceRules != null) "price_rules": priceRules!.map((e) => e.toJson()).toList(),
  };
}

/// The price rule's details.
class StorePriceRule {
  StorePriceRule({
    this.id,
    this.attribute,
    this.operator,
    this.value,
  });

  /// The price rule's ID.
  final String? id;
  /// The price rule's attribute.
  final String? attribute;
  /// The price rule's operator.
  final String? operator;
  /// The price rule's value.
  final String? value;

  factory StorePriceRule.fromJson(Map<String, dynamic> json) => StorePriceRule(
    id: json["id"] as String?,
    attribute: json["attribute"] as String?,
    operator: json["operator"] as String?,
    value: json["value"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (attribute != null) "attribute": attribute!,
    if (operator != null) "operator": operator!,
    if (value != null) "value": value!,
  };
}

/// The product's details.
class StoreProduct {
  StoreProduct({
    this.categories,
    this.type,
    this.length,
    this.title,
    this.status,
    this.options,
    this.description,
    this.id,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.variants,
    this.handle,
    this.subtitle,
    this.isGiftcard,
    this.thumbnail,
    this.width,
    this.weight,
    this.height,
    this.originCountry,
    this.hsCode,
    this.midCode,
    this.material,
    this.collection,
    this.collectionId,
    this.typeId,
    this.tags,
    this.images,
    this.discountable,
    this.externalId,
    this.deletedAt,
  });

  /// The product's categories.
  final List<Map<String, dynamic>>? categories;
  final StoreProductType? type;
  /// The product's length.
  final num? length;
  /// The product's title.
  final String? title;
  /// The product's status.
  final String? status;
  /// The product's options.
  final List<StoreProductOption>? options;
  /// The product's description.
  final String? description;
  /// The product's ID.
  final String? id;
  /// The product's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the product was created.
  final DateTime? createdAt;
  /// The date the product was updated.
  final DateTime? updatedAt;
  /// The product's variants.
  final List<StoreProductVariant>? variants;
  /// The product's handle.
  final String? handle;
  /// The product's subtitle.
  final String? subtitle;
  /// Whether the product is a gift card.
  final bool? isGiftcard;
  /// The product's thumbnail URL.
  final String? thumbnail;
  /// The product's width.
  final num? width;
  /// The product's weight.
  final num? weight;
  /// The product's height.
  final num? height;
  /// The product's origin country.
  final String? originCountry;
  /// The product's HS code.
  final String? hsCode;
  /// The product's MID code.
  final String? midCode;
  /// The product's material.
  final String? material;
  final StoreCollection? collection;
  /// The ID of the collection that the product belongs to.
  final String? collectionId;
  /// The ID of the product's type.
  final String? typeId;
  /// The product's tags.
  final List<StoreProductTag>? tags;
  /// The product's images.
  final List<StoreProductImage>? images;
  /// Whether the product can be discounted.
  final bool? discountable;
  /// The ID of the product in an external service or system.
  final String? externalId;
  /// The date the product was deleted.
  final DateTime? deletedAt;

  factory StoreProduct.fromJson(Map<String, dynamic> json) => StoreProduct(
    categories: (json["categories"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    type: json["type"] == null ? null : StoreProductType.fromJson(json["type"] as Map<String, dynamic>),
    length: json["length"] as num?,
    title: json["title"] as String?,
    status: json["status"] as String?,
    options: (json["options"] as List?)?.map((e) => e == null ? null : StoreProductOption.fromJson(e as Map<String, dynamic>)).toList().cast<StoreProductOption>(),
    description: json["description"] as String?,
    id: json["id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    variants: (json["variants"] as List?)?.map((e) => e == null ? null : StoreProductVariant.fromJson(e as Map<String, dynamic>)).toList().cast<StoreProductVariant>(),
    handle: json["handle"] as String?,
    subtitle: json["subtitle"] as String?,
    isGiftcard: json["is_giftcard"] as bool?,
    thumbnail: json["thumbnail"] as String?,
    width: json["width"] as num?,
    weight: json["weight"] as num?,
    height: json["height"] as num?,
    originCountry: json["origin_country"] as String?,
    hsCode: json["hs_code"] as String?,
    midCode: json["mid_code"] as String?,
    material: json["material"] as String?,
    collection: json["collection"] == null ? null : StoreCollection.fromJson(json["collection"] as Map<String, dynamic>),
    collectionId: json["collection_id"] as String?,
    typeId: json["type_id"] as String?,
    tags: (json["tags"] as List?)?.map((e) => e == null ? null : StoreProductTag.fromJson(e as Map<String, dynamic>)).toList().cast<StoreProductTag>(),
    images: (json["images"] as List?)?.map((e) => e == null ? null : StoreProductImage.fromJson(e as Map<String, dynamic>)).toList().cast<StoreProductImage>(),
    discountable: json["discountable"] as bool?,
    externalId: json["external_id"] as String?,
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (categories != null) "categories": categories!.map((e) => e).toList(),
    if (type != null) "type": type!.toJson(),
    if (length != null) "length": length!,
    if (title != null) "title": title!,
    if (status != null) "status": status!,
    if (options != null) "options": options!.map((e) => e.toJson()).toList(),
    if (description != null) "description": description!,
    if (id != null) "id": id!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (variants != null) "variants": variants!.map((e) => e.toJson()).toList(),
    if (handle != null) "handle": handle!,
    if (subtitle != null) "subtitle": subtitle!,
    if (isGiftcard != null) "is_giftcard": isGiftcard!,
    if (thumbnail != null) "thumbnail": thumbnail!,
    if (width != null) "width": width!,
    if (weight != null) "weight": weight!,
    if (height != null) "height": height!,
    if (originCountry != null) "origin_country": originCountry!,
    if (hsCode != null) "hs_code": hsCode!,
    if (midCode != null) "mid_code": midCode!,
    if (material != null) "material": material!,
    if (collection != null) "collection": collection!.toJson(),
    if (collectionId != null) "collection_id": collectionId!,
    if (typeId != null) "type_id": typeId!,
    if (tags != null) "tags": tags!.map((e) => e.toJson()).toList(),
    if (images != null) "images": images!.map((e) => e.toJson()).toList(),
    if (discountable != null) "discountable": discountable!,
    if (externalId != null) "external_id": externalId!,
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
  };
}

/// The category's details.
class StoreProductCategory {
  StoreProductCategory({
    this.id,
    this.name,
    this.description,
    this.handle,
    this.rank,
    this.parentCategoryId,
    this.parentCategory,
    this.categoryChildren,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.products,
  });

  /// The category's ID.
  final String? id;
  /// The category's name.
  final String? name;
  /// The category's description.
  final String? description;
  /// The category's handle.
  final String? handle;
  /// The category's rank.
  final num? rank;
  /// The ID of the category's parent.
  final String? parentCategoryId;
  final Map<String, dynamic>? parentCategory;
  /// The category's children.
  final List<Map<String, dynamic>>? categoryChildren;
  /// The category's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the category was created.
  final DateTime? createdAt;
  /// The date the category was updated.
  final DateTime? updatedAt;
  /// The date the category was deleted.
  final DateTime? deletedAt;
  /// The category's products.
  final List<StoreProduct>? products;

  factory StoreProductCategory.fromJson(Map<String, dynamic> json) => StoreProductCategory(
    id: json["id"] as String?,
    name: json["name"] as String?,
    description: json["description"] as String?,
    handle: json["handle"] as String?,
    rank: json["rank"] as num?,
    parentCategoryId: json["parent_category_id"] as String?,
    parentCategory: json["parent_category"] as Map<String, dynamic>?,
    categoryChildren: (json["category_children"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    products: (json["products"] as List?)?.map((e) => e == null ? null : StoreProduct.fromJson(e as Map<String, dynamic>)).toList().cast<StoreProduct>(),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (name != null) "name": name!,
    if (description != null) "description": description!,
    if (handle != null) "handle": handle!,
    if (rank != null) "rank": rank!,
    if (parentCategoryId != null) "parent_category_id": parentCategoryId!,
    if (parentCategory != null) "parent_category": parentCategory!,
    if (categoryChildren != null) "category_children": categoryChildren!.map((e) => e).toList(),
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (products != null) "products": products!.map((e) => e.toJson()).toList(),
  };
}

/// The paginated list of product categories.
class StoreProductCategoryListResponse {
  StoreProductCategoryListResponse({
    this.limit,
    this.offset,
    this.count,
    this.productCategories,
    this.estimateCount,
  });

  /// The maximum number of items returned.
  final num? limit;
  /// The number of items skipped before retrieving the returned items.
  final num? offset;
  /// The total number of items.
  final num? count;
  /// The list of product categories.
  final List<StoreProductCategory>? productCategories;
  /// The estimated count retrieved from the PostgreSQL query planner, which may be inaccurate.
  final num? estimateCount;

  factory StoreProductCategoryListResponse.fromJson(Map<String, dynamic> json) => StoreProductCategoryListResponse(
    limit: json["limit"] as num?,
    offset: json["offset"] as num?,
    count: json["count"] as num?,
    productCategories: (json["product_categories"] as List?)?.map((e) => e == null ? null : StoreProductCategory.fromJson(e as Map<String, dynamic>)).toList().cast<StoreProductCategory>(),
    estimateCount: json["estimate_count"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (limit != null) "limit": limit!,
    if (offset != null) "offset": offset!,
    if (count != null) "count": count!,
    if (productCategories != null) "product_categories": productCategories!.map((e) => e.toJson()).toList(),
    if (estimateCount != null) "estimate_count": estimateCount!,
  };
}

/// The product category's details.
class StoreProductCategoryResponse {
  StoreProductCategoryResponse({
    this.productCategory,
  });

  final StoreProductCategory? productCategory;

  factory StoreProductCategoryResponse.fromJson(Map<String, dynamic> json) => StoreProductCategoryResponse(
    productCategory: json["product_category"] == null ? null : StoreProductCategory.fromJson(json["product_category"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (productCategory != null) "product_category": productCategory!.toJson(),
  };
}

/// The image's details.
class StoreProductImage {
  StoreProductImage({
    this.id,
    this.url,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.metadata,
    this.rank,
  });

  /// The image's ID.
  final String? id;
  /// The image's URL.
  final String? url;
  /// The date the image was created.
  final DateTime? createdAt;
  /// The date the image was updated.
  final DateTime? updatedAt;
  /// The date the image was deleted.
  final DateTime? deletedAt;
  /// The image's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The image's rank among its sibling images
  final num? rank;

  factory StoreProductImage.fromJson(Map<String, dynamic> json) => StoreProductImage(
    id: json["id"] as String?,
    url: json["url"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    metadata: json["metadata"] as Map<String, dynamic>?,
    rank: json["rank"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (url != null) "url": url!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (metadata != null) "metadata": metadata!,
    if (rank != null) "rank": rank!,
  };
}

/// The paginated list of products.
class StoreProductListResponse {
  StoreProductListResponse({
    this.limit,
    this.offset,
    this.count,
    this.estimateCount,
    this.products,
  });

  /// The maximum number of items returned.
  final num? limit;
  /// The number of items skipped before retrieving the returned items.
  final num? offset;
  /// The total count of items.
  final num? count;
  /// The estimated count retrieved from the PostgreSQL query planner, which may be inaccurate.
  final num? estimateCount;
  /// The list of products.
  final List<StoreProduct>? products;

  factory StoreProductListResponse.fromJson(Map<String, dynamic> json) => StoreProductListResponse(
    limit: json["limit"] as num?,
    offset: json["offset"] as num?,
    count: json["count"] as num?,
    estimateCount: json["estimate_count"] as num?,
    products: (json["products"] as List?)?.map((e) => e == null ? null : StoreProduct.fromJson(e as Map<String, dynamic>)).toList().cast<StoreProduct>(),
  );

  Map<String, dynamic> toJson() => {
    if (limit != null) "limit": limit!,
    if (offset != null) "offset": offset!,
    if (count != null) "count": count!,
    if (estimateCount != null) "estimate_count": estimateCount!,
    if (products != null) "products": products!.map((e) => e.toJson()).toList(),
  };
}

/// The product option's details.
class StoreProductOption {
  StoreProductOption({
    this.id,
    this.title,
    this.product,
    this.values,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.products,
    this.isExclusive,
  });

  /// The option's ID.
  final String? id;
  /// The option's title.
  final String? title;
  final Map<String, dynamic>? product;
  /// The option's values.
  final List<StoreProductOptionValue>? values;
  /// The option's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the product option was created.
  final DateTime? createdAt;
  /// The date the product option was updated.
  final DateTime? updatedAt;
  /// The date the product option was deleted.
  final DateTime? deletedAt;
  /// The products that use the option.
  final List<BaseProduct>? products;
  /// Whether the option is exclusive to a product.
  final bool? isExclusive;

  factory StoreProductOption.fromJson(Map<String, dynamic> json) => StoreProductOption(
    id: json["id"] as String?,
    title: json["title"] as String?,
    product: json["product"] as Map<String, dynamic>?,
    values: (json["values"] as List?)?.map((e) => e == null ? null : StoreProductOptionValue.fromJson(e as Map<String, dynamic>)).toList().cast<StoreProductOptionValue>(),
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    products: (json["products"] as List?)?.map((e) => e == null ? null : BaseProduct.fromJson(e as Map<String, dynamic>)).toList().cast<BaseProduct>(),
    isExclusive: json["is_exclusive"] as bool?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (title != null) "title": title!,
    if (product != null) "product": product!,
    if (values != null) "values": values!.map((e) => e.toJson()).toList(),
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (products != null) "products": products!.map((e) => e.toJson()).toList(),
    if (isExclusive != null) "is_exclusive": isExclusive!,
  };
}

/// The product option's details.
class StoreProductOptionResponse {
  StoreProductOptionResponse({
    this.productOption,
  });

  final StoreProductOption? productOption;

  factory StoreProductOptionResponse.fromJson(Map<String, dynamic> json) => StoreProductOptionResponse(
    productOption: json["product_option"] == null ? null : StoreProductOption.fromJson(json["product_option"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (productOption != null) "product_option": productOption!.toJson(),
  };
}

/// The product option value's details.
class StoreProductOptionValue {
  StoreProductOptionValue({
    this.id,
    this.value,
    this.option,
    this.optionId,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.rank,
  });

  /// The value's ID.
  final String? id;
  /// The value.
  final String? value;
  final Map<String, dynamic>? option;
  /// The ID of the option this value belongs to.
  final String? optionId;
  /// The value's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the value was created.
  final DateTime? createdAt;
  /// The date the value was updated.
  final DateTime? updatedAt;
  /// The date the value was deleted.
  final DateTime? deletedAt;
  /// The rank of the value among other option values.
  final num? rank;

  factory StoreProductOptionValue.fromJson(Map<String, dynamic> json) => StoreProductOptionValue(
    id: json["id"] as String?,
    value: json["value"] as String?,
    option: json["option"] as Map<String, dynamic>?,
    optionId: json["option_id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    rank: json["rank"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (value != null) "value": value!,
    if (option != null) "option": option!,
    if (optionId != null) "option_id": optionId!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (rank != null) "rank": rank!,
  };
}

/// The product's details.
class StoreProductResponse {
  StoreProductResponse({
    this.product,
  });

  final StoreProduct? product;

  factory StoreProductResponse.fromJson(Map<String, dynamic> json) => StoreProductResponse(
    product: json["product"] == null ? null : StoreProduct.fromJson(json["product"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (product != null) "product": product!.toJson(),
  };
}

/// The tag's details.
class StoreProductTag {
  StoreProductTag({
    this.id,
    this.value,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.metadata,
    this.externalId,
  });

  /// The tag's ID.
  final String? id;
  /// The tag's value.
  final String? value;
  /// The date the tag was created.
  final DateTime? createdAt;
  /// The date the tag was updated.
  final DateTime? updatedAt;
  /// The date the tag was deleted.
  final DateTime? deletedAt;
  /// The tag's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The tag's external ID used to link it with external systems.
  final String? externalId;

  factory StoreProductTag.fromJson(Map<String, dynamic> json) => StoreProductTag(
    id: json["id"] as String?,
    value: json["value"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    metadata: json["metadata"] as Map<String, dynamic>?,
    externalId: json["external_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (value != null) "value": value!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (metadata != null) "metadata": metadata!,
    if (externalId != null) "external_id": externalId!,
  };
}

/// The paginated list of product tags.
class StoreProductTagListResponse {
  StoreProductTagListResponse({
    this.limit,
    this.offset,
    this.count,
    this.productTags,
    this.estimateCount,
  });

  /// The maximum number of items returned.
  final num? limit;
  /// The number of items to skip before retrieving the returned items.
  final num? offset;
  /// The total number of items available.
  final num? count;
  /// The list of product tags.
  final List<StoreProductTag>? productTags;
  /// The estimated count retrieved from the PostgreSQL query planner, which may be inaccurate.
  final num? estimateCount;

  factory StoreProductTagListResponse.fromJson(Map<String, dynamic> json) => StoreProductTagListResponse(
    limit: json["limit"] as num?,
    offset: json["offset"] as num?,
    count: json["count"] as num?,
    productTags: (json["product_tags"] as List?)?.map((e) => e == null ? null : StoreProductTag.fromJson(e as Map<String, dynamic>)).toList().cast<StoreProductTag>(),
    estimateCount: json["estimate_count"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (limit != null) "limit": limit!,
    if (offset != null) "offset": offset!,
    if (count != null) "count": count!,
    if (productTags != null) "product_tags": productTags!.map((e) => e.toJson()).toList(),
    if (estimateCount != null) "estimate_count": estimateCount!,
  };
}

/// The product tag's details.
class StoreProductTagResponse {
  StoreProductTagResponse({
    this.productTag,
  });

  final StoreProductTag? productTag;

  factory StoreProductTagResponse.fromJson(Map<String, dynamic> json) => StoreProductTagResponse(
    productTag: json["product_tag"] == null ? null : StoreProductTag.fromJson(json["product_tag"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (productTag != null) "product_tag": productTag!.toJson(),
  };
}

/// The product type's details.
class StoreProductType {
  StoreProductType({
    this.id,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.value,
    this.externalId,
  });

  /// The product type's ID.
  final String? id;
  /// The product type's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the product type was created.
  final DateTime? createdAt;
  /// The date the product type was updated.
  final DateTime? updatedAt;
  /// The date the product type was deleted.
  final DateTime? deletedAt;
  /// The type's value.
  final String? value;
  /// The type's external ID used to link it with external systems.
  final String? externalId;

  factory StoreProductType.fromJson(Map<String, dynamic> json) => StoreProductType(
    id: json["id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    value: json["value"] as String?,
    externalId: json["external_id"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (value != null) "value": value!,
    if (externalId != null) "external_id": externalId!,
  };
}

/// The paginated list of product types.
class StoreProductTypeListResponse {
  StoreProductTypeListResponse({
    this.limit,
    this.offset,
    this.count,
    this.productTypes,
    this.estimateCount,
  });

  /// The maximum number of items returned.
  final num? limit;
  /// The number of items to skip before retrieving the returned items.
  final num? offset;
  /// The total number of items available.
  final num? count;
  /// The list of product types.
  final List<StoreProductType>? productTypes;
  /// The estimated count retrieved from the PostgreSQL query planner, which may be inaccurate.
  final num? estimateCount;

  factory StoreProductTypeListResponse.fromJson(Map<String, dynamic> json) => StoreProductTypeListResponse(
    limit: json["limit"] as num?,
    offset: json["offset"] as num?,
    count: json["count"] as num?,
    productTypes: (json["product_types"] as List?)?.map((e) => e == null ? null : StoreProductType.fromJson(e as Map<String, dynamic>)).toList().cast<StoreProductType>(),
    estimateCount: json["estimate_count"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (limit != null) "limit": limit!,
    if (offset != null) "offset": offset!,
    if (count != null) "count": count!,
    if (productTypes != null) "product_types": productTypes!.map((e) => e.toJson()).toList(),
    if (estimateCount != null) "estimate_count": estimateCount!,
  };
}

/// The product type's details.
class StoreProductTypeResponse {
  StoreProductTypeResponse({
    this.productType,
  });

  final StoreProductType? productType;

  factory StoreProductTypeResponse.fromJson(Map<String, dynamic> json) => StoreProductTypeResponse(
    productType: json["product_type"] == null ? null : StoreProductType.fromJson(json["product_type"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (productType != null) "product_type": productType!.toJson(),
  };
}

/// The variant's details.
class StoreProductVariant {
  StoreProductVariant({
    this.options,
    this.product,
    this.length,
    this.title,
    this.metadata,
    this.id,
    this.width,
    this.weight,
    this.height,
    this.originCountry,
    this.hsCode,
    this.midCode,
    this.material,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
    this.productId,
    this.sku,
    this.barcode,
    this.ean,
    this.upc,
    this.allowBackorder,
    this.manageInventory,
    this.inventoryQuantity,
    this.variantRank,
    this.calculatedPrice,
    this.thumbnail,
    this.images,
  });

  /// The variant's options.
  final List<StoreProductOptionValue>? options;
  final Map<String, dynamic>? product;
  /// The variant's length.
  final num? length;
  /// The variant's title.
  final String? title;
  /// The variant's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The variant's ID.
  final String? id;
  /// The variant's width.
  final num? width;
  /// The variant's weight.
  final num? weight;
  /// The variant's height.
  final num? height;
  /// The variant's origin country.
  final String? originCountry;
  /// The variant's HS code.
  final String? hsCode;
  /// The variant's MID code.
  final String? midCode;
  /// The variant's material.
  final String? material;
  /// The date the variant was created.
  final DateTime? createdAt;
  /// The date the variant was updated.
  final DateTime? updatedAt;
  /// The date the variant was deleted.
  final DateTime? deletedAt;
  /// The ID of the product this variant belongs to.
  final String? productId;
  /// The variant's SKU.
  final String? sku;
  /// The variant's barcode.
  final String? barcode;
  /// The variant's EAN.
  final String? ean;
  /// The variant's UPC.
  final String? upc;
  /// Whether the variant can be ordered even if it's not in stock.
  final bool? allowBackorder;
  /// Whether Medusa manages the variant's inventory. If disabled, the variant is always considered in stock.
  final bool? manageInventory;
  /// The variant's inventory quantity. This property is only available if you pass `+variants.inventory_quantity` in the `fields` query parameter.
  final num? inventoryQuantity;
  /// The variant's rank among its siblings.
  final num? variantRank;
  final BaseCalculatedPriceSet? calculatedPrice;
  /// The variant's thumbnail.
  final String? thumbnail;
  /// The variant's images.
  final List<BaseProductImage>? images;

  factory StoreProductVariant.fromJson(Map<String, dynamic> json) => StoreProductVariant(
    options: (json["options"] as List?)?.map((e) => e == null ? null : StoreProductOptionValue.fromJson(e as Map<String, dynamic>)).toList().cast<StoreProductOptionValue>(),
    product: json["product"] as Map<String, dynamic>?,
    length: json["length"] as num?,
    title: json["title"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    id: json["id"] as String?,
    width: json["width"] as num?,
    weight: json["weight"] as num?,
    height: json["height"] as num?,
    originCountry: json["origin_country"] as String?,
    hsCode: json["hs_code"] as String?,
    midCode: json["mid_code"] as String?,
    material: json["material"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
    productId: json["product_id"] as String?,
    sku: json["sku"] as String?,
    barcode: json["barcode"] as String?,
    ean: json["ean"] as String?,
    upc: json["upc"] as String?,
    allowBackorder: json["allow_backorder"] as bool?,
    manageInventory: json["manage_inventory"] as bool?,
    inventoryQuantity: json["inventory_quantity"] as num?,
    variantRank: json["variant_rank"] as num?,
    calculatedPrice: json["calculated_price"] == null ? null : BaseCalculatedPriceSet.fromJson(json["calculated_price"] as Map<String, dynamic>),
    thumbnail: json["thumbnail"] as String?,
    images: (json["images"] as List?)?.map((e) => e == null ? null : BaseProductImage.fromJson(e as Map<String, dynamic>)).toList().cast<BaseProductImage>(),
  );

  Map<String, dynamic> toJson() => {
    if (options != null) "options": options!.map((e) => e.toJson()).toList(),
    if (product != null) "product": product!,
    if (length != null) "length": length!,
    if (title != null) "title": title!,
    if (metadata != null) "metadata": metadata!,
    if (id != null) "id": id!,
    if (width != null) "width": width!,
    if (weight != null) "weight": weight!,
    if (height != null) "height": height!,
    if (originCountry != null) "origin_country": originCountry!,
    if (hsCode != null) "hs_code": hsCode!,
    if (midCode != null) "mid_code": midCode!,
    if (material != null) "material": material!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
    if (productId != null) "product_id": productId!,
    if (sku != null) "sku": sku!,
    if (barcode != null) "barcode": barcode!,
    if (ean != null) "ean": ean!,
    if (upc != null) "upc": upc!,
    if (allowBackorder != null) "allow_backorder": allowBackorder!,
    if (manageInventory != null) "manage_inventory": manageInventory!,
    if (inventoryQuantity != null) "inventory_quantity": inventoryQuantity!,
    if (variantRank != null) "variant_rank": variantRank!,
    if (calculatedPrice != null) "calculated_price": calculatedPrice!.toJson(),
    if (thumbnail != null) "thumbnail": thumbnail!,
    if (images != null) "images": images!.map((e) => e.toJson()).toList(),
  };
}

/// The region's details.
class StoreRegion {
  StoreRegion({
    this.id,
    this.name,
    this.currencyCode,
    this.automaticTaxes,
    this.countries,
    this.paymentProviders,
    this.metadata,
    this.createdAt,
    this.updatedAt,
  });

  /// The region's ID.
  final String? id;
  /// The region's name.
  final String? name;
  /// The region's currency code.
  final String? currencyCode;
  /// Whether taxes are calculated automatically during checkout for carts that belong to this region.
  final bool? automaticTaxes;
  /// The region's countries.
  final List<BaseRegionCountry>? countries;
  /// The region's payment providers.
  final List<AdminPaymentProvider>? paymentProviders;
  /// The region's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the region was created.
  final DateTime? createdAt;
  /// The date the region was updated.
  final DateTime? updatedAt;

  factory StoreRegion.fromJson(Map<String, dynamic> json) => StoreRegion(
    id: json["id"] as String?,
    name: json["name"] as String?,
    currencyCode: json["currency_code"] as String?,
    automaticTaxes: json["automatic_taxes"] as bool?,
    countries: (json["countries"] as List?)?.map((e) => e == null ? null : BaseRegionCountry.fromJson(e as Map<String, dynamic>)).toList().cast<BaseRegionCountry>(),
    paymentProviders: (json["payment_providers"] as List?)?.map((e) => e == null ? null : AdminPaymentProvider.fromJson(e as Map<String, dynamic>)).toList().cast<AdminPaymentProvider>(),
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (name != null) "name": name!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (automaticTaxes != null) "automatic_taxes": automaticTaxes!,
    if (countries != null) "countries": countries!.map((e) => e.toJson()).toList(),
    if (paymentProviders != null) "payment_providers": paymentProviders!.map((e) => e.toJson()).toList(),
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The country's details.
class StoreRegionCountry {
  StoreRegionCountry({
    this.id,
    this.iso2,
    this.iso3,
    this.numCode,
    this.name,
    this.displayName,
  });

  /// The country's ID.
  final String? id;
  /// The country's ISO 2.
  final String? iso2;
  /// The country's ISO 3.
  final String? iso3;
  /// The country's num code.
  final String? numCode;
  /// The country's name.
  final String? name;
  /// The country's display name.
  final String? displayName;

  factory StoreRegionCountry.fromJson(Map<String, dynamic> json) => StoreRegionCountry(
    id: json["id"] as String?,
    iso2: json["iso_2"] as String?,
    iso3: json["iso_3"] as String?,
    numCode: json["num_code"] as String?,
    name: json["name"] as String?,
    displayName: json["display_name"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (iso2 != null) "iso_2": iso2!,
    if (iso3 != null) "iso_3": iso3!,
    if (numCode != null) "num_code": numCode!,
    if (name != null) "name": name!,
    if (displayName != null) "display_name": displayName!,
  };
}

/// The details to remove a gift card from the cart.
class StoreRemoveGiftCardFromCart {
  StoreRemoveGiftCardFromCart({
    this.code,
  });

  /// The gift card code to remove from the cart.
  final String? code;

  factory StoreRemoveGiftCardFromCart.fromJson(Map<String, dynamic> json) => StoreRemoveGiftCardFromCart(
    code: json["code"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (code != null) "code": code!,
  };
}

/// The details of requesting the order transfer.
class StoreRequestOrderTransfer {
  StoreRequestOrderTransfer({
    this.description,
    this.updateOrderEmail,
  });

  /// The transfer's description, which can be shown to the other customer receiving the request.
  final String? description;
  /// Whether to update the order's email to the receiving customer's email. By default, the order's email will not be updated.
  final bool? updateOrderEmail;

  factory StoreRequestOrderTransfer.fromJson(Map<String, dynamic> json) => StoreRequestOrderTransfer(
    description: json["description"] as String?,
    updateOrderEmail: json["update_order_email"] as bool?,
  );

  Map<String, dynamic> toJson() => {
    if (description != null) "description": description!,
    if (updateOrderEmail != null) "update_order_email": updateOrderEmail!,
  };
}

/// The return's details.
class StoreReturn {
  StoreReturn({
    this.items,
    this.status,
    this.id,
    this.displayId,
    this.createdAt,
    this.orderId,
    this.locationId,
    this.canceledAt,
    this.exchangeId,
    this.claimId,
    this.refundAmount,
    this.receivedAt,
    this.createdBy,
    this.requestedAt,
  });

  /// The return's items.
  final List<StoreReturnItem>? items;
  /// The return's status.
  final String? status;
  /// The return's ID.
  final String? id;
  /// The return's display ID.
  final num? displayId;
  /// The date the return was created.
  final DateTime? createdAt;
  /// The ID of the order this return belongs to.
  final String? orderId;
  /// The ID of the location the return items are being returned to.
  final String? locationId;
  /// The date the return was canceled.
  final String? canceledAt;
  /// The ID of the associated exchange.
  final String? exchangeId;
  /// The ID of the associated claim.
  final String? claimId;
  /// The amount to be refunded.
  final num? refundAmount;
  /// The date the return items were received.
  final String? receivedAt;
  /// The ID of the user that created the return.
  final String? createdBy;
  /// The date the return was requested.
  final String? requestedAt;

  factory StoreReturn.fromJson(Map<String, dynamic> json) => StoreReturn(
    items: (json["items"] as List?)?.map((e) => e == null ? null : StoreReturnItem.fromJson(e as Map<String, dynamic>)).toList().cast<StoreReturnItem>(),
    status: json["status"] as String?,
    id: json["id"] as String?,
    displayId: json["display_id"] as num?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    orderId: json["order_id"] as String?,
    locationId: json["location_id"] as String?,
    canceledAt: json["canceled_at"] as String?,
    exchangeId: json["exchange_id"] as String?,
    claimId: json["claim_id"] as String?,
    refundAmount: json["refund_amount"] as num?,
    receivedAt: json["received_at"] as String?,
    createdBy: json["created_by"] as String?,
    requestedAt: json["requested_at"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (items != null) "items": items!.map((e) => e.toJson()).toList(),
    if (status != null) "status": status!,
    if (id != null) "id": id!,
    if (displayId != null) "display_id": displayId!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (orderId != null) "order_id": orderId!,
    if (locationId != null) "location_id": locationId!,
    if (canceledAt != null) "canceled_at": canceledAt!,
    if (exchangeId != null) "exchange_id": exchangeId!,
    if (claimId != null) "claim_id": claimId!,
    if (refundAmount != null) "refund_amount": refundAmount!,
    if (receivedAt != null) "received_at": receivedAt!,
    if (createdBy != null) "created_by": createdBy!,
    if (requestedAt != null) "requested_at": requestedAt!,
  };
}

/// The return item's details.
class StoreReturnItem {
  StoreReturnItem({
    this.id,
    this.quantity,
    this.receivedQuantity,
    this.damagedQuantity,
    this.reasonId,
    this.note,
    this.itemId,
    this.returnId,
    this.metadata,
  });

  /// The item's ID.
  final String? id;
  /// The item's quantity.
  final num? quantity;
  /// The item's received quantity.
  final num? receivedQuantity;
  /// The item's damaged quantity.
  final num? damagedQuantity;
  /// The ID of the item's reason.
  final String? reasonId;
  /// A note with more details on why the item is returned.
  final String? note;
  /// The ID of the item in the order.
  final String? itemId;
  /// The ID of the return this item belongs to.
  final String? returnId;
  /// The item's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory StoreReturnItem.fromJson(Map<String, dynamic> json) => StoreReturnItem(
    id: json["id"] as String?,
    quantity: json["quantity"] as num?,
    receivedQuantity: json["received_quantity"] as num?,
    damagedQuantity: json["damaged_quantity"] as num?,
    reasonId: json["reason_id"] as String?,
    note: json["note"] as String?,
    itemId: json["item_id"] as String?,
    returnId: json["return_id"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (quantity != null) "quantity": quantity!,
    if (receivedQuantity != null) "received_quantity": receivedQuantity!,
    if (damagedQuantity != null) "damaged_quantity": damagedQuantity!,
    if (reasonId != null) "reason_id": reasonId!,
    if (note != null) "note": note!,
    if (itemId != null) "item_id": itemId!,
    if (returnId != null) "return_id": returnId!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The return reason's details.
class StoreReturnReason {
  StoreReturnReason({
    this.id,
    this.value,
    this.label,
    this.description,
    this.metadata,
    this.createdAt,
    this.updatedAt,
  });

  /// The return reason's ID.
  final String? id;
  /// The return reason's value.
  final String? value;
  /// The return reason's label.
  final String? label;
  /// The return reason's description.
  final String? description;
  /// The return reason's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the return reason was created.
  final DateTime? createdAt;
  /// The date the return reason was updated.
  final DateTime? updatedAt;

  factory StoreReturnReason.fromJson(Map<String, dynamic> json) => StoreReturnReason(
    id: json["id"] as String?,
    value: json["value"] as String?,
    label: json["label"] as String?,
    description: json["description"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (value != null) "value": value!,
    if (label != null) "label": label!,
    if (description != null) "description": description!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
  };
}

/// The return reason's details.
class StoreReturnReasonResponse {
  StoreReturnReasonResponse({
    this.returnReason,
  });

  final StoreReturnReason? returnReason;

  factory StoreReturnReasonResponse.fromJson(Map<String, dynamic> json) => StoreReturnReasonResponse(
    returnReason: json["return_reason"] == null ? null : StoreReturnReason.fromJson(json["return_reason"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (returnReason != null) "return_reason": returnReason!.toJson(),
  };
}

/// The return's details.
class StoreReturnResponse {
  StoreReturnResponse({
    this.return_,
  });

  final StoreReturn? return_;

  factory StoreReturnResponse.fromJson(Map<String, dynamic> json) => StoreReturnResponse(
    return_: json["return"] == null ? null : StoreReturn.fromJson(json["return"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (return_ != null) "return": return_!.toJson(),
  };
}

/// The shipping option's details.
class StoreShippingOption {
  StoreShippingOption({
    this.id,
    this.name,
    this.priceType,
    this.serviceZoneId,
    this.providerId,
    this.provider,
    this.shippingOptionTypeId,
    this.type,
    this.shippingProfileId,
    this.amount,
    this.isTaxInclusive,
    this.data,
    this.metadata,
  });

  /// The shipping option's ID.
  final String? id;
  /// The shipping option's name.
  final String? name;
  /// The shipping option's price type. If it's `flat`, the price is fixed and is set in the `prices` property. If it's `calculated`, the price is calculated on check
  final String? priceType;
  /// The ID of the service zone the shipping option belongs to.
  final String? serviceZoneId;
  /// The ID of the fulfillment provider handling this option.
  final String? providerId;
  final BaseFulfillmentProvider? provider;
  /// The ID of the shipping option's type.
  final String? shippingOptionTypeId;
  final StoreShippingOptionType? type;
  /// The ID of the associated shipping profile.
  final String? shippingProfileId;
  /// The shipping option's amount. For a shipping option whose `price_type` is `calculated`, this amount isn't reliable until it's computed by the fulfillment provid
  final num? amount;
  /// Whether the amount includes taxes.
  final bool? isTaxInclusive;
  /// The shipping option's data, useful for the provider handling fulfillment.
  final Map<String, dynamic>? data;
  /// The shipping option's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory StoreShippingOption.fromJson(Map<String, dynamic> json) => StoreShippingOption(
    id: json["id"] as String?,
    name: json["name"] as String?,
    priceType: json["price_type"] as String?,
    serviceZoneId: json["service_zone_id"] as String?,
    providerId: json["provider_id"] as String?,
    provider: json["provider"] == null ? null : BaseFulfillmentProvider.fromJson(json["provider"] as Map<String, dynamic>),
    shippingOptionTypeId: json["shipping_option_type_id"] as String?,
    type: json["type"] == null ? null : StoreShippingOptionType.fromJson(json["type"] as Map<String, dynamic>),
    shippingProfileId: json["shipping_profile_id"] as String?,
    amount: json["amount"] as num?,
    isTaxInclusive: json["is_tax_inclusive"] as bool?,
    data: json["data"] as Map<String, dynamic>?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (name != null) "name": name!,
    if (priceType != null) "price_type": priceType!,
    if (serviceZoneId != null) "service_zone_id": serviceZoneId!,
    if (providerId != null) "provider_id": providerId!,
    if (provider != null) "provider": provider!.toJson(),
    if (shippingOptionTypeId != null) "shipping_option_type_id": shippingOptionTypeId!,
    if (type != null) "type": type!.toJson(),
    if (shippingProfileId != null) "shipping_profile_id": shippingProfileId!,
    if (amount != null) "amount": amount!,
    if (isTaxInclusive != null) "is_tax_inclusive": isTaxInclusive!,
    if (data != null) "data": data!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The list of shipping options.
class StoreShippingOptionListResponse {
  StoreShippingOptionListResponse({
    this.shippingOptions,
  });

  /// The list of shipping options.
  final List<Map<String, dynamic>>? shippingOptions;

  factory StoreShippingOptionListResponse.fromJson(Map<String, dynamic> json) => StoreShippingOptionListResponse(
    shippingOptions: (json["shipping_options"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
  );

  Map<String, dynamic> toJson() => {
    if (shippingOptions != null) "shipping_options": shippingOptions!.map((e) => e).toList(),
  };
}

/// The shipping option's details.
class StoreShippingOptionResponse {
  StoreShippingOptionResponse({
    this.shippingOption,
  });

  final StoreCartShippingOption? shippingOption;

  factory StoreShippingOptionResponse.fromJson(Map<String, dynamic> json) => StoreShippingOptionResponse(
    shippingOption: json["shipping_option"] == null ? null : StoreCartShippingOption.fromJson(json["shipping_option"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (shippingOption != null) "shipping_option": shippingOption!.toJson(),
  };
}

/// The shipping option type's details.
class StoreShippingOptionType {
  StoreShippingOptionType({
    this.id,
    this.label,
    this.description,
    this.code,
    this.shippingOptionId,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
  });

  /// The type's ID.
  final String? id;
  /// The type's label.
  final String? label;
  /// The type's description.
  final String? description;
  /// The type's code.
  final String? code;
  /// The ID of the associated shipping option.
  final String? shippingOptionId;
  /// The date the type was created.
  final DateTime? createdAt;
  /// The date the type was updated.
  final DateTime? updatedAt;
  /// The date the type was deleted.
  final DateTime? deletedAt;

  factory StoreShippingOptionType.fromJson(Map<String, dynamic> json) => StoreShippingOptionType(
    id: json["id"] as String?,
    label: json["label"] as String?,
    description: json["description"] as String?,
    code: json["code"] as String?,
    shippingOptionId: json["shipping_option_id"] as String?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    deletedAt: json["deleted_at"] == null ? null : DateTime.parse(json["deleted_at"] as String),
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (label != null) "label": label!,
    if (description != null) "description": description!,
    if (code != null) "code": code!,
    if (shippingOptionId != null) "shipping_option_id": shippingOptionId!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (deletedAt != null) "deleted_at": deletedAt!.toIso8601String(),
  };
}

/// The store credit account's details.
class StoreStoreCreditAccount {
  StoreStoreCreditAccount({
    this.customer,
    this.id,
    this.customerId,
    this.currencyCode,
    this.credits,
    this.debits,
    this.balance,
    this.metadata,
    this.createdAt,
    this.updatedAt,
    this.code,
  });

  final StoreCustomer? customer;
  /// The store credit account's ID.
  final String? id;
  /// The ID of the customer that the store credit account belongs to.
  final String? customerId;
  /// The store credit account's currency code.
  final String? currencyCode;
  /// The account's credits.
  final num? credits;
  /// The account's debits.
  final num? debits;
  /// The store credit account's balance.
  final num? balance;
  /// The store credit account's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;
  /// The date the store credit account was created.
  final DateTime? createdAt;
  /// The date the store credit account was updated.
  final DateTime? updatedAt;
  /// The store credit account's code.
  final String? code;

  factory StoreStoreCreditAccount.fromJson(Map<String, dynamic> json) => StoreStoreCreditAccount(
    customer: json["customer"] == null ? null : StoreCustomer.fromJson(json["customer"] as Map<String, dynamic>),
    id: json["id"] as String?,
    customerId: json["customer_id"] as String?,
    currencyCode: json["currency_code"] as String?,
    credits: json["credits"] as num?,
    debits: json["debits"] as num?,
    balance: json["balance"] as num?,
    metadata: json["metadata"] as Map<String, dynamic>?,
    createdAt: json["created_at"] == null ? null : DateTime.parse(json["created_at"] as String),
    updatedAt: json["updated_at"] == null ? null : DateTime.parse(json["updated_at"] as String),
    code: json["code"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (customer != null) "customer": customer!.toJson(),
    if (id != null) "id": id!,
    if (customerId != null) "customer_id": customerId!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (credits != null) "credits": credits!,
    if (debits != null) "debits": debits!,
    if (balance != null) "balance": balance!,
    if (metadata != null) "metadata": metadata!,
    if (createdAt != null) "created_at": createdAt!.toIso8601String(),
    if (updatedAt != null) "updated_at": updatedAt!.toIso8601String(),
    if (code != null) "code": code!,
  };
}

/// The store credit account's details.
class StoreStoreCreditAccountResponse {
  StoreStoreCreditAccountResponse({
    this.storeCreditAccount,
  });

  final StoreStoreCreditAccount? storeCreditAccount;

  factory StoreStoreCreditAccountResponse.fromJson(Map<String, dynamic> json) => StoreStoreCreditAccountResponse(
    storeCreditAccount: json["store_credit_account"] == null ? null : StoreStoreCreditAccount.fromJson(json["store_credit_account"] as Map<String, dynamic>),
  );

  Map<String, dynamic> toJson() => {
    if (storeCreditAccount != null) "store_credit_account": storeCreditAccount!.toJson(),
  };
}

/// The paginated list of store credit accounts.
class StoreStoreCreditAccountsResponse {
  StoreStoreCreditAccountsResponse({
    this.limit,
    this.offset,
    this.count,
    this.storeCreditAccounts,
    this.estimateCount,
  });

  /// The maximum number of store credit accounts to return.
  final num? limit;
  /// The number of store credit accounts to skip before retrieving the results.
  final num? offset;
  /// The total number of store credit accounts available.
  final num? count;
  /// The list of store credit accounts.
  final List<StoreStoreCreditAccount>? storeCreditAccounts;
  /// The store credit account's estimate count.
  final num? estimateCount;

  factory StoreStoreCreditAccountsResponse.fromJson(Map<String, dynamic> json) => StoreStoreCreditAccountsResponse(
    limit: json["limit"] as num?,
    offset: json["offset"] as num?,
    count: json["count"] as num?,
    storeCreditAccounts: (json["store_credit_accounts"] as List?)?.map((e) => e == null ? null : StoreStoreCreditAccount.fromJson(e as Map<String, dynamic>)).toList().cast<StoreStoreCreditAccount>(),
    estimateCount: json["estimate_count"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (limit != null) "limit": limit!,
    if (offset != null) "offset": offset!,
    if (count != null) "count": count!,
    if (storeCreditAccounts != null) "store_credit_accounts": storeCreditAccounts!.map((e) => e.toJson()).toList(),
    if (estimateCount != null) "estimate_count": estimateCount!,
  };
}

/// The details to update in the customer.
class StoreUpdateCustomer {
  StoreUpdateCustomer({
    this.companyName,
    this.firstName,
    this.lastName,
    this.phone,
    this.metadata,
  });

  /// The customer's company name.
  final String? companyName;
  /// The customer's first name.
  final String? firstName;
  /// The customer's last name.
  final String? lastName;
  /// The customer's phone.
  final String? phone;
  /// The customer's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory StoreUpdateCustomer.fromJson(Map<String, dynamic> json) => StoreUpdateCustomer(
    companyName: json["company_name"] as String?,
    firstName: json["first_name"] as String?,
    lastName: json["last_name"] as String?,
    phone: json["phone"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (companyName != null) "company_name": companyName!,
    if (firstName != null) "first_name": firstName!,
    if (lastName != null) "last_name": lastName!,
    if (phone != null) "phone": phone!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The properties to update in the address.
class StoreUpdateCustomerAddress {
  StoreUpdateCustomerAddress({
    this.firstName,
    this.lastName,
    this.phone,
    this.company,
    this.address1,
    this.address2,
    this.city,
    this.countryCode,
    this.province,
    this.postalCode,
    this.addressName,
    this.isDefaultShipping,
    this.isDefaultBilling,
    this.metadata,
  });

  /// The customer's first name.
  final String? firstName;
  /// The customer's last name.
  final String? lastName;
  /// The customer's phone.
  final String? phone;
  /// The address's company.
  final String? company;
  /// The address's first line.
  final String? address1;
  /// The address's second line.
  final String? address2;
  /// The address's city.
  final String? city;
  /// The address's country code.
  final String? countryCode;
  /// The address's ISO 3166-2 province code. Must be lower-case.
  final String? province;
  /// The address's postal code.
  final String? postalCode;
  /// The address's name.
  final String? addressName;
  /// Whether the address is used by default for shipping during checkout.
  final bool? isDefaultShipping;
  /// Whether the address is used by default for billing during checkout.
  final bool? isDefaultBilling;
  /// Holds custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory StoreUpdateCustomerAddress.fromJson(Map<String, dynamic> json) => StoreUpdateCustomerAddress(
    firstName: json["first_name"] as String?,
    lastName: json["last_name"] as String?,
    phone: json["phone"] as String?,
    company: json["company"] as String?,
    address1: json["address_1"] as String?,
    address2: json["address_2"] as String?,
    city: json["city"] as String?,
    countryCode: json["country_code"] as String?,
    province: json["province"] as String?,
    postalCode: json["postal_code"] as String?,
    addressName: json["address_name"] as String?,
    isDefaultShipping: json["is_default_shipping"] as bool?,
    isDefaultBilling: json["is_default_billing"] as bool?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (firstName != null) "first_name": firstName!,
    if (lastName != null) "last_name": lastName!,
    if (phone != null) "phone": phone!,
    if (company != null) "company": company!,
    if (address1 != null) "address_1": address1!,
    if (address2 != null) "address_2": address2!,
    if (city != null) "city": city!,
    if (countryCode != null) "country_code": countryCode!,
    if (province != null) "province": province!,
    if (postalCode != null) "postal_code": postalCode!,
    if (addressName != null) "address_name": addressName!,
    if (isDefaultShipping != null) "is_default_shipping": isDefaultShipping!,
    if (isDefaultBilling != null) "is_default_billing": isDefaultBilling!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The details to update in the address.
class UpdateAddress {
  UpdateAddress({
    this.id,
    this.customerId,
    this.company,
    this.firstName,
    this.lastName,
    this.address1,
    this.address2,
    this.city,
    this.countryCode,
    this.province,
    this.postalCode,
    this.phone,
    this.metadata,
  });

  /// The ID of an existing address to update.
  final String? id;
  /// The ID of the customer that this address belongs to.
  final String? customerId;
  /// The address's company.
  final String? company;
  /// The address's first name.
  final String? firstName;
  /// The address's last name.
  final String? lastName;
  /// The address's first line.
  final String? address1;
  /// The address's second line.
  final String? address2;
  /// The address's city.
  final String? city;
  /// The address's country code.
  final String? countryCode;
  /// The address's ISO 3166-2 province code. Must be lower-case.
  final String? province;
  /// The address's postal code.
  final String? postalCode;
  /// The address's phone.
  final String? phone;
  /// The address's metadata, can hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory UpdateAddress.fromJson(Map<String, dynamic> json) => UpdateAddress(
    id: json["id"] as String?,
    customerId: json["customer_id"] as String?,
    company: json["company"] as String?,
    firstName: json["first_name"] as String?,
    lastName: json["last_name"] as String?,
    address1: json["address_1"] as String?,
    address2: json["address_2"] as String?,
    city: json["city"] as String?,
    countryCode: json["country_code"] as String?,
    province: json["province"] as String?,
    postalCode: json["postal_code"] as String?,
    phone: json["phone"] as String?,
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (id != null) "id": id!,
    if (customerId != null) "customer_id": customerId!,
    if (company != null) "company": company!,
    if (firstName != null) "first_name": firstName!,
    if (lastName != null) "last_name": lastName!,
    if (address1 != null) "address_1": address1!,
    if (address2 != null) "address_2": address2!,
    if (city != null) "city": city!,
    if (countryCode != null) "country_code": countryCode!,
    if (province != null) "province": province!,
    if (postalCode != null) "postal_code": postalCode!,
    if (phone != null) "phone": phone!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The details to update in a cart.
class UpdateCartData {
  UpdateCartData({
    this.regionId,
    this.customerId,
    this.salesChannelId,
    this.email,
    this.currencyCode,
    this.shippingAddressId,
    this.billingAddressId,
    this.billingAddress,
    this.shippingAddress,
    this.metadata,
  });

  /// The ID of the associated region. This can affect the prices and currency code of the cart.
  final String? regionId;
  /// The ID of the customer that the cart belongs to.
  final String? customerId;
  /// The ID of the associated sales channel. A product's availability in a sales channel only filters the products you retrieve. Medusa doesn't reject a variant adde
  final String? salesChannelId;
  /// The email of the customer that the cart belongs to.
  final String? email;
  /// The cart's currency code.
  final String? currencyCode;
  /// The ID of the cart's shipping address.
  final String? shippingAddressId;
  /// The ID of the cart's billing address.
  final String? billingAddressId;
  final dynamic billingAddress;
  final dynamic shippingAddress;
  /// The cart's metadata, ca hold custom key-value pairs.
  final Map<String, dynamic>? metadata;

  factory UpdateCartData.fromJson(Map<String, dynamic> json) => UpdateCartData(
    regionId: json["region_id"] as String?,
    customerId: json["customer_id"] as String?,
    salesChannelId: json["sales_channel_id"] as String?,
    email: json["email"] as String?,
    currencyCode: json["currency_code"] as String?,
    shippingAddressId: json["shipping_address_id"] as String?,
    billingAddressId: json["billing_address_id"] as String?,
    billingAddress: json["billing_address"],
    shippingAddress: json["shipping_address"],
    metadata: json["metadata"] as Map<String, dynamic>?,
  );

  Map<String, dynamic> toJson() => {
    if (regionId != null) "region_id": regionId!,
    if (customerId != null) "customer_id": customerId!,
    if (salesChannelId != null) "sales_channel_id": salesChannelId!,
    if (email != null) "email": email!,
    if (currencyCode != null) "currency_code": currencyCode!,
    if (shippingAddressId != null) "shipping_address_id": shippingAddressId!,
    if (billingAddressId != null) "billing_address_id": billingAddressId!,
    if (billingAddress != null) "billing_address": billingAddress!,
    if (shippingAddress != null) "shipping_address": shippingAddress!,
    if (metadata != null) "metadata": metadata!,
  };
}

/// The workflow execution's context.
class WorkflowExecutionContext {
  WorkflowExecutionContext({
    this.data,
    this.compensate,
    this.errors,
  });

  /// The context's data.
  final Map<String, dynamic>? data;
  /// The context's compensate.
  final Map<String, dynamic>? compensate;
  /// The context's errors.
  final List<Map<String, dynamic>>? errors;

  factory WorkflowExecutionContext.fromJson(Map<String, dynamic> json) => WorkflowExecutionContext(
    data: json["data"] as Map<String, dynamic>?,
    compensate: json["compensate"] as Map<String, dynamic>?,
    errors: (json["errors"] as List?)?.map((e) => e as Map<String, dynamic>?).toList().cast<Map<String, dynamic>>(),
  );

  Map<String, dynamic> toJson() => {
    if (data != null) "data": data!,
    if (compensate != null) "compensate": compensate!,
    if (errors != null) "errors": errors!.map((e) => e).toList(),
  };
}

/// The step's definition details.
class WorkflowExecutionDefinition {
  WorkflowExecutionDefinition({
    this.async,
    this.compensateAsync,
    this.noCompensation,
    this.continueOnPermanentFailure,
    this.skipOnPermanentFailure,
    this.maxRetries,
    this.noWait,
    this.retryInterval,
    this.retryIntervalAwaiting,
    this.saveResponse,
    this.timeout,
    this.autoRetry,
    this.maxAwaitingRetries,
  });

  /// Whether the step is async.
  final bool? async;
  /// Whether the compensation function of the step is async.
  final bool? compensateAsync;
  /// Whether the step doesn't have a compensation function.
  final bool? noCompensation;
  /// Whether the workflow should continue executing even if its status is changed to failed.
  final bool? continueOnPermanentFailure;
  final dynamic skipOnPermanentFailure;
  /// The maximum number of times to retry the step.
  final num? maxRetries;
  /// Whether the workflow shouldn't wait for the step to finish before moving to the next step.
  final bool? noWait;
  /// The interval in seconds between retry attempts when the step fails.
  final num? retryInterval;
  /// The interval in seconds to retry a step even if its status is `waiting_response`.
  final num? retryIntervalAwaiting;
  /// Whether the step's response is stored.
  final bool? saveResponse;
  /// The maximum time in seconds to wait for this step to complete. If the step exceeds this time, the step's state is changed to `timeout`, but the step continues e
  final num? timeout;
  /// Whether the step should be automatically retried if it fails.
  final bool? autoRetry;
  /// The maximum number of times to retry the step while it's in the `waiting_response` state.
  final num? maxAwaitingRetries;

  factory WorkflowExecutionDefinition.fromJson(Map<String, dynamic> json) => WorkflowExecutionDefinition(
    async: json["async"] as bool?,
    compensateAsync: json["compensateAsync"] as bool?,
    noCompensation: json["noCompensation"] as bool?,
    continueOnPermanentFailure: json["continueOnPermanentFailure"] as bool?,
    skipOnPermanentFailure: json["skipOnPermanentFailure"],
    maxRetries: json["maxRetries"] as num?,
    noWait: json["noWait"] as bool?,
    retryInterval: json["retryInterval"] as num?,
    retryIntervalAwaiting: json["retryIntervalAwaiting"] as num?,
    saveResponse: json["saveResponse"] as bool?,
    timeout: json["timeout"] as num?,
    autoRetry: json["autoRetry"] as bool?,
    maxAwaitingRetries: json["maxAwaitingRetries"] as num?,
  );

  Map<String, dynamic> toJson() => {
    if (async != null) "async": async!,
    if (compensateAsync != null) "compensateAsync": compensateAsync!,
    if (noCompensation != null) "noCompensation": noCompensation!,
    if (continueOnPermanentFailure != null) "continueOnPermanentFailure": continueOnPermanentFailure!,
    if (skipOnPermanentFailure != null) "skipOnPermanentFailure": skipOnPermanentFailure!,
    if (maxRetries != null) "maxRetries": maxRetries!,
    if (noWait != null) "noWait": noWait!,
    if (retryInterval != null) "retryInterval": retryInterval!,
    if (retryIntervalAwaiting != null) "retryIntervalAwaiting": retryIntervalAwaiting!,
    if (saveResponse != null) "saveResponse": saveResponse!,
    if (timeout != null) "timeout": timeout!,
    if (autoRetry != null) "autoRetry": autoRetry!,
    if (maxAwaitingRetries != null) "maxAwaitingRetries": maxAwaitingRetries!,
  };
}

/// The state of the step's invokation function.
class WorkflowExecutionFn {
  WorkflowExecutionFn({
    this.state,
    this.status,
  });

  /// The invokation step's state.
  final String? state;
  /// The invokation step's state.
  final String? status;

  factory WorkflowExecutionFn.fromJson(Map<String, dynamic> json) => WorkflowExecutionFn(
    state: json["state"] as String?,
    status: json["status"] as String?,
  );

  Map<String, dynamic> toJson() => {
    if (state != null) "state": state!,
    if (status != null) "status": status!,
  };
}
